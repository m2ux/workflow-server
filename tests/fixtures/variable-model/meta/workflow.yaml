id: meta
version: 1.0.0
title: Meta Fixture
description: Minimal meta so the transient-bootstrap path works against this fixture corpus.
initialActivity: bootstrap-activity
variables:
  - name: bootstrap_ready
    type: boolean
    defaultValue: false
