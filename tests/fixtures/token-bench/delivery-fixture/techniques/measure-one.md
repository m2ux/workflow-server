---
metadata:
  version: 1.0.0
---

## Capability

Count what one directory holds.

## Outputs

### entry_total

How many entries `{target_path}` holds directly.

## Protocol

### 1. Count The Entries

- Record how many entries `{target_path}` holds directly as `{entry_total}`.
