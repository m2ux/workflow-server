# Assumptions Log — remediate-vuln ↔ work-package alignment

| ID | Phase | Category | Risk | Assumption | Resolution | Outcome |
|----|-------|----------|------|------------|------------|---------|
| A-001 | requirements | Technique Selection | High | F1 is fixed server-side: borrowed activities' step techniques resolve against their source workflow first (mirroring #166 B10 fragment scoping), not by qualifying/forking content. Rationale: generic-not-overfit — wp's activity-group shorthand is the corpus convention; qualifying ~65 refs for one borrower erodes it; forking 12 activities defeats reuse. | Audit (generic-not-overfit rule; B10 precedent) + user change-request approval | Validated |
| A-002 | requirements | Rule Scope | High | Airtight privacy needs a structural disclosure gate usable by borrowed wp steps. Alternatives: (a) reinstate mode gates on wp's disclosure-capable steps (update-pr::render/create-pr, issue posting) — restores the bypass contract wp dropped; (b) rv-side only via absent variables + prose (status quo — not airtight); (c) fork the disclosure-bearing activities into rv — duplication. Agent favors (a). | User | Corrected: mechanism (a) accepted, but the gate variable is consumer-agnostic `stealth_mode` (not workflow-bound `is_sec_vuln_mode`); wp declares it `defaultValue: false` and gates disclosure steps `stealth_mode != true`; rv declares `defaultValue: true` and retires `is_sec_vuln_mode` |
| A-003 | requirements | Activity Boundaries | Medium | rv 01-start stays minimal: adopt detect-project-type, resolve-reference, verify-signing-precondition, but NOT wp's worktree creation / gitnexus indexing / merge-strategy detection; instead declare `worktree_created:false`, `gitnexus_indexed:false` defaults so downstream gates evaluate false. Rationale: security fixes are developed in a user-chosen private checkout; canonical-worktree formula and GitNexus indexing of a public reference add surface without privacy review. | User | Confirmed, with extension: consumer-agnostic mode gating (`stealth_mode`) may also be applied inside shared wp activities where a step doesn't fit a private run (e.g. plan-prepare gh-auth validate) provided wp's own behavior is unchanged; rv start sets `reference_path`=private checkout, `component_name`=its basename |
| A-004 | requirements | Variable State | Medium | F4 is resolved by mirroring wp's declarations (name, type, description, defaultValue) for every variable the borrowed activities read, and dropping rv-only `has_failures` (dead in wp). `issue_platform` intentionally NOT declared pending A-002 (its absence is the current accidental disclosure guard). | Audit (check:variable-model coherence; wp 3.27.0 declarations as source of truth) | Validated |
| A-005 | requirements | Schema Construct Choice | Medium | Where a rv fork is privacy-neutral, rv's OWN activities re-bind the wp original via qualified cross-workflow ref (`work-package::…` resolves today without the F1 fix): strategic-findings-analysis (replaces rv analyze-findings), manage-artifacts::verify-readme-conforms, dco-provenance::record-attestation. Privacy-variant forks stay local: review-scope (ported #203 diff, branch-range base discovery instead of pr_number), secure-submit ops, security-setup ops. | Audit (cross-workflow reuse tier; verified `work-package::…` refs resolve from rv session) | Validated |
| A-006 | requirements | Rule Scope | Low | Version bump: remediate-vuln 1.1.0 → 2.0.0 (variable-model changes + checkpoint reshape are behavior-changing). | Convention (semver, prior wp majors) | Validated |

## Open Assumptions

*None — all assumptions resolved (A-001, A-004, A-005, A-006 by audit/convention; A-002, A-003 by user interview 2026-07-10).*
