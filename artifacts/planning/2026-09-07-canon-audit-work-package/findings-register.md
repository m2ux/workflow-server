# Canon Audit — `work-package`

**Base ref:** `c1d07a291510eb13d9ec6e49c73a5aed55b3c538` (the prior pass's target) · **Target:** `workflows/work-package` at `56977e2c3d664b3880a833120d1478b569b48842` · **Target surface:** 168 files — read 168 · **unread 0** · **Change surface:** 110 files (touched: 110 whole files · I/O-contract closure: 0 beyond touched · consumers: 0 pulled in) · **Guards:** 36 pass, 0 fail — 2 unmeasured

**Verdict:** 72 findings over the whole target surface. Residual: **0 files `unread`**, **2 criteria units `blocked`** of the homes' 92. Every prose home was walked against every file; the target is audited at this commit once the two guard units are measured.

| Severity | Open | Known |
|----------|-----:|------:|
| Critical | 0 | 0 |
| High     | 11 | 0 |
| Medium   | 43 | 0 |
| Low      | 18 | 0 |

**The dominant pattern in this register is a defect class the prior pass closed on one site and left on its siblings.** Findings 3, 21 and 22 are the effectless-option shape it fixed on `rationale-attestation`; 14 is the `capability-as-op-inventory` it fixed on `finalize-documentation/TECHNIQUE.md`; 30 is the session-interaction defect it fixed on `discuss.md`; 31 is the raw-primitive defect it fixed on `create-issue.md`. Five classes, each fixed once and surviving elsewhere, because the fix scope was the finding's file rather than the class's reach.

## Remediation status

Branch `workflow/work-package-canon-remediation`, worktree `.worktrees/workflow/work-package-canon-remediation`, off `56977e2c`. Corpus guards on the branch: **31 of 32 pass** at `f0c376e0`; the single failure is `binding-fidelity` reporting three ledger items the code branch owns — a stale entry finding 66's fix legitimately closed, and two misplaced cites the `directory-scope` rewrite moved. Both are recorded under Owed below. No live bug and no untriaged finding remains.

| Finding | Severity | Commit | What landed |
|---|---|---|---|
| 1 | High | `9fa99a86` | `comprehension_artifact` declared as the corpus document; `comprehension_artifact_path` added and remapped from `written_artifact` at both write steps; both messages link the path; the survey lands as the document so the first deep-dive reads a populated artifact |
| 5 | Medium | `9fa99a86` | `comprehension_scope` (`same-area` / `new-area`) declared, set from the two continue options, and taken as an input by `deep-dive` |
| 31 | High | `338f429b` | Both `concept-rag://activities` reaches replaced with `knowledge-base-search::load-domain-index`; the sideways citation to the research op dropped |
| 56 | Medium | `338f429b` | Seven fill rules moved from `create-test-plan` into `resources/test-plan.md#rules`; the technique cites them; `finalize-test-plan` repointed off the retired sibling rule |
| 66 | High | `338f429b` | `analysis_findings` / `research_findings` renamed to `analysis_document` / `research_document` in the declaration and the Protocol read |
| 2 | High | `f6c8e51b` | `strategic_findings_disposition` declared with five values and set by all five options; `needs-more-review` exit added and bound in both the work-package graph and `remediate-vuln`'s, which borrows the activity |
| 3 | High | `f6c8e51b` | Summary render and approval gate wrapped in a three-iteration `review-summary-cycle`; `refine` sets `summary_refine_requested`, which the cycle continues on |
| 42 | High | `f6c8e51b` | Split into `lean_audit_findings`, `strategic_review_findings` and `classified_findings`; the ponytail output is remapped at both bind sites; the fix cycle now selects from the classified set it declares |
| 54 | High | `f6c8e51b` | `create-test-plan` declares `plan_document` and scopes from `plan_document.tasks`, produced by the step before it |
| 73 | High | `f6c8e51b` | **Found during remediation.** `classify-and-route-findings` ran *after* the `review-fix-cycle` loop whose continuation test reads the two actionable flags it produces, so the flags sat at their defaults on first evaluation and the cycle could never run on a first pass. Classification now precedes the loop and repeats inside it after each re-review |
| 4, 47, 48, 55 | High, High, Medium, Medium | `d733a143` | The four contract breaks in the review cluster: each value declared on the side that supplies it, and each read reaching a producer |
| 43 | High | `28931b8b` | `view-pr` bound as a step; `pr_body`, `head_sha`, `base_branch`, `reviewed_code_base_url` and `pr_url` declared on both sides of every boundary they cross, across five activities |
| 30 | High | `685c2f90` | Elicitation split into `pose-question`, `record-answer` and `select-domains`, so no op both asks and records |
| 44, 74 | Medium, Medium | `86c9db13` | `force_with_lease` declared on `push-branch` with a `false` default; `question_domains` given the producer it lacked |
| 11, 12, 24 | Medium ×3 | `ce988cac` | `create-issue` split into a container plus `create-github` and `create-jira`, so neither op branches on the platform it was handed |
| 7, 69 | Medium, Medium | `2765fd9e` | `manage-registers` container with `append-deferred-item` and `append-follow-up`; every open-work register reached through the op that owns it |
| 6, 18, 23, 46 | Medium ×4 | `a46cd875` | The unspent diff inputs dropped, the restated commit rule removed, the mode restatements deleted. **Finding 17 withdrawn** — the guard showed the braced read in activity 08's own Protocol was its only consumer, so the output declaration I added was the defect |
| 34, 58–65 | Medium ×2, Low ×7 | `9087e933` | Nine format and citation corrections across the review and close-out ops |
| 9, 52 | Medium, Medium | `a2477822` | `findings-constraint` stated once on the techniques container, worded without a designator so it adds no read to fifteen activities |
| 32, 35 | Medium, Medium | `ccabb1b0` | Resolvability and classification criteria homed in the guides that own them |
| 67, 68, 70, 71, 72 | Medium ×3, Low ×2 | `9d32d534` | The provenance contract stated on its container, and each value set declared where the value is |
| 14, 15, 33, 36 | Medium ×4 | `8a275689` | `manage-git` states its shared contribution and resolves the engineering checkout once, as a table the four restating sites now cite; `triage` reads the reconcilability vocabulary from its resource; `determine-path` drops the activity name. **Two dead outputs closed along the way** — `todo_tasks` and `base_pr_diff`, both left consumerless by findings 66 and 6 |
| 10, 13, 19, 20, 25–28, 39–41, 45, 51, 53, 57 | Medium ×7, Low ×8 | `0aa2e59c` | Symbol provenance becomes `resources/symbol-provenance.md` and the changes-fragment issue-reference forms a `strategic-review` section both the writer and the checker cite; declared ids braced; gitnexus locals bound once and read bare; argument lists parenthesised; rationale tails cut to the operative sentence |
| 16, 29, 37, 38, 49, 50 | Medium ×6 | `74098f86` | `create-worktree` emits the default branch it resolves, giving `sync-branch`'s input a producer; `implement-task` declares its derived edit target as an output; `reconcile` declares the research artifact on both sides; `post-review-comment` declares the review id a later run replaces against; `render` drops the review-mode branch every bind site gates out |
| 8, 21, 22 | Medium, Low, Low | `f0c376e0` | A breached safety floor records itself and keeps the cycle running; the build-artifact gate offers one current-tree option; the review outcome list comes from what the activities report, not a seeded copy of `complete`'s |

**Three regressions my own fixes caused, caught by the guards and fixed before commit** — each is a rule the audit itself names. Adding an exit to activity 12 broke `remediate-vuln`, which borrows it and must bind every exit in its own graph. Moving classification ahead of the loop turned two declared reads on activity 10 into internal reads. And hoisting `changed_files` to the workflow-root container gave all fifteen activities a read they did not need — twice, because wording the hoisted rule with `{changed_files}` reproduced the cascade from the Rules section.

**Owed on the code branch.** Three items for the adoption commit, all in `scripts/binding-fidelity-triage.json` or its stamp:

- Finding 66's rename gives `implementation-analysis/TECHNIQUE.md`'s `analysis_document` a consumer, so the `dead-output` entry for it matches nothing and the guard reports it stale. Delete the entry.
- The `directory-scope` rewrite moved `code-commit-coauthor-trailer` down `manage-git/TECHNIQUE.md`, so four `read-resolution` entries cite line 25 for findings now at line 33. Correct the sites.
- `tests/e2e/option-coverage.json` already carries the merged `build-artifact-check=artifacts-current` id in place of `none-needed` and `user-regenerated`; that edit is uncommitted in the code checkout and belongs to the same commit, alongside the submodule pointer, the walk baseline and `npm run baseline:stamp`.

**Outstanding: none.** All 74 findings are closed — 73 remediated across 18 commits, finding 17 withdrawn with the guard as its resolver.

## Findings

| ID | Severity | Entry | Location | Evidence | Origin | Fix |
|----|----------|-------|----------|----------|--------|-----|
| 1 | High | `reference-without-provenance` | `activities/15-codebase-comprehension.yaml:25-28,54-62,93,101,116` | `comprehension_artifact` is declared "Absolute path to the comprehension artifact for the current codebase area", `defaultValue: ""`. No step assigns a path: `create-comprehension-artifact` binds `manage-artifacts::write-artifact` with no `outputs:` deviation, and that op's sole output is `written_artifact` ("Full path to the written artifact"), landing under its own name. Its only producer is the `concern_document` output `combine-question-challenges` remaps onto it at line 93 — declared in `analyse-challenge/combine.md` as "The concern document with challenge resolutions applied and newly surfaced items appended", a document body. So the action message at line 62 and the checkpoint message at line 116 interpolate `({comprehension_artifact})` as a link target that is empty before the first merge and a document body after it, and `codebase-comprehension/deep-dive.md` takes the same slot as an input. The sibling activities running the identical challenge/combine round-trip bind `assumptions_log`, declared `type: object` as the log itself, so a document-typed slot is the corpus convention and the path-typed declaration here is the divergence | pre-existing | Remap `written_artifact` onto `comprehension_artifact` at the write steps and give the document body its own slot |
| 2 | High | `variable-for-approval` | `activities/12-strategic-review.yaml:165-206` checkpoint `review-findings` | Options `fix-findings`, `selective-fixes` and `more-review` carry no `effect` at all. `review_passed` keeps its `false` default (line 61), so all three resolve to exit `review-failed` (`isDefault`, line 254) and the graph sends every one of them to `plan-prepare`. `selective-fixes` reads "Choose which findings to fix, by priority" — no variable holds that selection — and `more-review` ("Additional areas need examination") re-plans rather than re-reviewing. The sibling review-mode gate `findings-delivery` (lines 207-248) declares `raised_findings_scope` with `values: all / selected / none` for exactly this decision, and `review-summary.md:16,91` spends it | diff | Declare the selection variable and set it from the option effects as `findings-delivery` does; route `more-review` to an exit that re-enters review |
| 3 | High | `checkpoint-requires-decision` | `activities/13-submit-for-review.yaml:182-184` checkpoint `review-summary-approval` option `refine` | Label and description only, no `effect`. `review_posted` keeps its `false` default (line 89), so `persist-review-summary` (line 198) and `post-pr-review` (line 202), both `when: is_review_mode == true && review_posted == true`, are skipped. `generate-review-summary` sits at line 143, *before* the gate, and no later step re-runs it. The activity falls to exit `review-mode` (line 495) and the graph sends it to `complete`. "Refine summary" ends the review with nothing posted, nothing persisted and no refinement — the same terminal path as `skip-posting`, which does record its choice | diff | Record the refine request and loop back over `generate-review-summary`, or drop the option |
| 4 | High | `duplicate-shared-capability` | `techniques/manage-git/push-commits.md` | The op's whole Protocol is "From `{target_path}`, push all local commits on `{branch_name}` to `{push_remote}`. Verify the push succeeded." `meta/techniques/version-control/push-branch.md` owns that capability: same verb, same verification, the same `pushed_branch` output id, and an already-declared `remote_name` input (*optional, default `origin`*) that absorbs the stealth-mode remote this local op exists to carry. No in-tree consumer reads the local `pushed_branch`; the only corpus readers are `workflow-design`'s activities against the meta op. Activity 01 already binds `version-control::resolve-host-repo`, so the meta group is reachable from this workflow | diff | Delete the local op; bind `version-control::push-branch` with `remote_name: push_remote` at `activities/13-submit-for-review.yaml:267-270` |
| 5 | Medium | `variable-for-approval` | `activities/15-codebase-comprehension.yaml:109-137` checkpoint `comprehension-sufficient` | `dive-deeper` and `different-area` both set only `needs_comprehension: true`. The distinction they name — more depth in this area versus a different part of the codebase — has no recorded effect, and `codebase-comprehension/deep-dive.md` declares no area or scope input (`comprehension_artifact`, `comprehension_log`, `gitnexus_indexed`), so the loop re-enters against the same area on either answer | pre-existing | Declare the area slot, set it from the option effects, and take it as a `deep-dive` input |
| 6 | Medium | `declared-input-never-read` | `techniques/review-diff.md:12,16` | `base_pr_diff` and `base_sha` occur nowhere in the file but their own `###` declaration lines — neither `## Protocol` nor `## Rules` names either id. Protocol phase 2 (line 69) re-derives the diff with `git diff {$base_branch}...HEAD`, the work `base_pr_diff`'s own description says it stands "in place of". `review-baseline-state.md:12,24` declares both as outputs, so the producer exists and only the consumer is missing | diff | Reference `{base_pr_diff}` / `{base_sha}` from the phases that would spend them, or delete both declarations |
| 7 | Medium | `procedure-in-io-contract` | `techniques/review-diff.md:48` output `manual_diff_review_report` | Description closes "created here when this review runs first, updated in place when the full code-review report is written" — a conditional population duty plus a sequencing claim about another operation's write, where the entry admits meaning, shape and allowed values only | diff | Move the create-versus-update condition into the Protocol phase that persists the section |
| 8 | Medium | One Authoritative Home (principle; no catalog Detect reaches this spelling) | `activities/01-start-work-package.yaml:332-337` and `activities/14-complete.yaml:192-201` | `seed-review-mode-outcomes` sets `target_workflow_outcomes` to five literal lines, four verbatim copies of `complete`'s `outcome[]` entries. The value is consumed cross-workflow by `meta/activities/04-end-workflow.yaml:12,32` as `outcomes`, so both surfaces are live: `complete` declares nine outcomes, the seed carries five, and editing either leaves the other stale with nothing failing. `bag-value-as-literal` excludes it by its own carve-out — no declared slot carries these strings' meaning | diff | Derive the review-path outcomes from `complete`'s `outcome[]`, or state in one home which subset a review run is measured against |
| 9 | Medium | `cited-home-owns-claim` | `techniques/README.md:7` | "`TECHNIQUE.md` holds shared Inputs, Outputs, Rules, and Errors for every technique here." The cited file declares `## Capability` and `## Inputs` and nothing else. Two workflows in the corpus carry this sentence (`work-package`, `prism-audit`), and **no** workflow-root `TECHNIQUE.md` anywhere in the corpus has an `## Errors` section, so the Outputs/Rules/Errors clause is unsupported here and the Errors clause is unsupported everywhere. The prior pass withdrew this line as "corpus convention (seven workflows carry it)" — that count is wrong | diff | Cut the clause to what the file holds, or add the shared Outputs/Rules the group needs |
| 10 | Medium | `bind-site-is-orchestration-truth` | `REVIEW-MODE.md:52` | "On the create path `review-findings` asks whether to fix now, fix a selection, defer, or accept — the session owns the code and can take all four actions." The gate declares **five** options (`acceptable`, `fix-findings`, `selective-fixes`, `defer-findings`, `more-review`); the prose names four and omits `more-review`. It also asserts the session "can take all four actions" while three of the five record no effect and route identically (finding 2). `activities/README.md:364` renders the same gate correctly, so two docs disagree | diff | Delete the roster; point at the activity YAML |
| 11 | Medium | `alternate-ops-as-protocol-sequence` | `techniques/create-issue.md:48-76` | Phase 1 "Runs only when the user provides an existing issue key", phase 2 "Runs when no existing issue key was given", phase 3 "Runs when `{issue_platform}` is github", phase 4 "Runs when `{issue_platform}` is jira". A call walks {1} or {2,3} or {2,4}; renumbering 3 and 4 changes nothing. The entry's own exemplar names this shape ("create vs update") | diff | Split into a group with one op per platform and gate the bind sites on the mode; or move the slices into `## Rules` keyed by the platform |
| 12 | Medium | `no-duplicate-technique-steps` | `activities/01-start-work-package.yaml:523-529,637-640` | Two steps bind `create-issue`, distinguished only by the sub-mode input `issue_platform` (`create-github-issue-for-jira` passes `github`; `create-issue` passes none) — classification (c), monolith-masking. The mutually-exclusive-`when` carve-out does not apply: a jira run that also needs a GitHub mirror sets both `needs_github_issue_creation` and `needs_issue_creation`, so both fire | diff | Split per finding 11 and bind one op per platform |
| 13 | Medium | `brace-declared-ids` | `techniques/review-mode-detection.md:53-57` | Declared outputs `is_review_mode`, `review_mode_ambiguous`, `review_pr_url`, `pr_number`, `review_pr_missing`, `branch_name` and `review_ticket_ref` appear backticked without braces ("set `is_review_mode` accordingly", "set `review_mode_ambiguous` to `false`") — Detect mode (c). The declared inputs `{user_request}` and `{pr_reference}` in the same Protocol are correctly braced, and the sibling ops brace their output writes (`review-diff.md:76` "set `{reviewed_code_base_url}`", `create-issue.md:53` "Set `{needs_issue_creation}`"), so the corpus convention is braced and this file is the outlier | pre-existing | Brace every declared id |
| 14 | Medium | `capability-as-op-inventory` | `techniques/manage-git/TECHNIQUE.md:8` | Capability is a comma inventory of child ops — "worktrees, path restore from a base ref, edit-side commits, branch sync, repo-root submodule maintenance, and pre-push destination/signature checks" — and has already drifted: the folder holds 13 ops and the list reaches 9, omitting `artifact-commits`, `detect-merge-strategy`, `instruct-merge-strategy` and `verify-feature-branch`. Same defect the prior pass closed on `finalize-documentation/TECHNIQUE.md`, surviving on a sibling container | pre-existing | Rewrite as a shared-domain contribution statement; leave the roster to the folder |
| 15 | Medium | `no-duplicated-guidance` | `techniques/manage-git/TECHNIQUE.md:14,21`, `manage-git/artifact-commits.md:32`, `review-summary.md:80`, `resolve-artifact-publish.md`, `update-pr/render.md` | One resolution rule in five homes: "`{host_repo_path}/.engineering` when that path is a git checkout (submodule or nested clone); otherwise `{host_repo_path}`". `manage-git/TECHNIQUE.md` states it twice itself — once in the `host_repo_path` input description and once in the `directory-scope` rule | diff | Keep the `directory-scope` rule as the one home; cite it from each Protocol that resolves the dir |
| 16 | Medium | `bind-protocol-locals` | `techniques/manage-git/create-worktree.md:26,32` | Line 26 binds `{$default_branch}` with a fallback chain (`symbolic-ref`, then `main`, then `master`) while `default_branch` is a declared **workflow variable**. Line 32 then reads `origin/{default_branch}` — bare, no `$` — which resolves to the workflow variable, so the fallback the local computed is discarded. The entry's do-not-flag is explicit: a `{name}` that is a declared variable should have its `$` stripped, not a bind added | diff | Strip the `$` and fold the fallback into the workflow variable's producer, or read `{$default_branch}` at line 32 |
| 17 | Medium | `technique-outputs-declared` | `techniques/apply-review-fixes.md` | The file has no `## Outputs` section, while Capability claims fixes "implemented and committed" and Protocol line 40 notes "the `{commit_sha}` it returns against the findings the cycle addressed" — a gateable value with no declaration. `activities/08-implement.yaml:38` declares `commit_sha` in its writes, so a consumer exists | diff | Declare `### commit_sha` under a `## Outputs` section |
| 18 | Medium | `no-rule-protocol-restatement` | `techniques/apply-review-fixes.md:40` and rule `commit-is-final-phase` | Protocol line 40 ends "This is the technique's final phase; no separate source commit step follows." The rule restates it: "The commit phase is part of this technique. Fixes and their commit are one atomic operation; callers do not append a separate commit step." Deleting the rule loses no invariant | diff | Delete the rule; the phase carries it |
| 19 | Medium | `rule-as-protocol-step` | `techniques/task-completion-review.md:34-35` | Protocol phase 1 opens with two standing prohibitions and no produce outcome — "Every symbol … MUST have provenance" and "Never fabricate symbols: do not invent type/trait/struct names, reference unimplemented functions or methods, …". Rule `documentation-reflects-code` (line 59-61) already states the invariant, so the phase is a second home for it | diff | Delete the prohibition bullets; the rule owns them |
| 20 | Medium | `operative-criteria-need-a-home` | `techniques/task-completion-review.md:38-45` | A six-row "Check / How to verify" table of symbol-provenance criteria lives only in this Protocol, with no resource or catalog home. The file's other criteria are correctly cited out (`rust-substrate-code-review.md#review-criteria` at line 53), which is what marks this table as the un-homed one | diff | Migrate the table into a resource and leave the technique a walker |
| 21 | Low | `checkpoint-requires-decision` | `activities/09-lean-coding-audit.yaml:82-103` checkpoint `safety-floor-cleared` | `floor-held-cycle-complete` and `floor-breached` both set only `needs_simplification: false` and both leave the loop, so a clean completion and a breached safety floor are indistinguishable to every later activity. The revert `floor-breached` asserts ("is reverted before review") has no step performing it and no variable recording it | diff | Record the breach in its own variable and bind the revert as a step gated on it |
| 22 | Low | `checkpoint-requires-decision` | `activities/13-submit-for-review.yaml:350-387` checkpoint `build-artifact-check` | `none-needed` ("No build-dependent regeneration is owed") and `user-regenerated` ("The build-dependent artifacts in the tree are current") both set only `build_dependent_artifacts_pending: false` | diff | Merge the two options, or record the distinction |
| 23 | Low | Convention Conformance — field ordering | `activities/14-complete.yaml:40-66` | `variables.writes` breaks the alphabetical order every sibling activity keeps: `deferred_item_issue_created` sits after `deferred_item_issue_number` and `deferred_item_issue_url`, and `publishable_files` after `raise_current_deferred_item` | diff | Re-sort the block |
| 24 | Low | `boolean-id-shape` | `techniques/create-issue.md:34-36` | `needs_issue_creation` is declared as an **output** recording what happened ("Whether the run creates a new issue: `true` when no existing issue was supplied, `false` when one was") while its `needs_` shape states a pending requirement, and `activities/01:401,407` sets the same name from a checkpoint as a routing flag that gates this very op at line 640. `activities/14:120-123` remaps it to `deferred_item_issue_created` — the clean form — so the corpus already shows the fix | diff | Rename the output to what it records; keep the gate flag separate |
| 25 | Low | `backtick-code-tokens` | `techniques/stakeholder-overview.md:31` | "Write it into `{planning_folder_path}`/`README.md`" — a fragmented span, which the entry names as a defect ("must be one span") | pre-existing | One span: `` `{planning_folder_path}/README.md` `` |
| 26 | Low | `no-bind-mechanics-as-prose` | `techniques/stakeholder-overview.md:39-41` rule `section-heading-is-an-input` | "The target README section is supplied via `{readme_section_heading}`; the technique does not hardcode a heading, so it serves every call-site uniformly." The rule's whole subject is how the input reaches the op, which variable-binding owns, plus a rationale clause about the caller set | pre-existing | Delete; the declared input is the contract |
| 27 | Low | `no-rationale-in-description` | `techniques/manage-git/artifact-commits.md:38`; `techniques/assess-ticket-completeness.md:37`; `techniques/manage-git/verify-feature-branch.md:24-26`; `techniques/findings-classification.md:49` | Rationale tails on procedure bullets and rules, which the entry names explicitly. "Without this, two work packages running in parallel will produce non-fast-forward push rejections and halt mid-flow. The rebase is cheap because…"; "Documented gaps are persistent findings, not ephemeral status text — they live in the assumptions log so review can proceed…"; the rule `resume-preexisted-worktree`, whose body explains why the op exists ("It exists for resume cases where…"); "A review whose passes have little to separate them — a documentation-only change… — produces these in bulk" | mixed | Delete the justification clauses; keep the operative sentence |
| 28 | Low | `constraint-as-blockquote` | `techniques/naming-conventions.md:59` | Protocol step 6 carries its outcome, a terminology definition and two prohibitions in one sentence — "…set `{target_path}` to `{$checkout_root}/.worktrees/{$wp_slug}/`… From this point on, "inside `{target_path}`" refers to this worktree… Never place `{target_path}` under `{planning_folder_path}` or under `{host_repo_path}`, and never anchor it to a home directory or an install root." | diff | Move the prohibitions to a `>` note or to `## Rules`; leave the step stating its outcome |
| 29 | Low | Separate Contract from Procedure (principle; no catalog Detect reaches this spelling) | `techniques/implement-task.md:20-22,40` | `target_symbol` is declared under `## Inputs` as a bind slot, and Protocol phase 1 derives it: "Determine the primary edit target `{target_symbol}` … from `{current_task}`". `activities/08-implement.yaml:94-96` binds the op with no inputs, so no caller ever supplies it. A caller reading the contract wires a value the op computes for itself | diff | Move it to `## Outputs`, or bind it as a `{$local}` |

| 30 | High | `session-interaction-in-technique` | `techniques/requirements-elicitation/ask-question.md:8,33-37` and `elicit.md:43-45` | Both ops run the interview inside a technique. `ask-question` Protocol: "Pose one question from the current domain", "Record the response and adapt the follow-up based on the answer", "Skip irrelevant follow-ups; probe deeper when needed" — imperatives that require a human audience, with no tool or op owning that channel. `elicit.md:43-45` carries the same three instructions verbatim in substance, so the duplication is a second defect on the same text. The bind site `activities/03-requirements-elicitation.yaml:89-99` is a bare `forEach` loop over `question_domains` whose only body step is `ask-question` — no adjacent checkpoint, no `action: message`. `ask-question`'s Capability also names the surrounding construct ("Single-question unit of the domain-iteration elicitation loop"), which is `technique-stage-agnostic` on the same line. This is the defect the prior pass fixed on the sibling `discuss.md` by moving the ask to checkpoint `stakeholder-discussion-held` | pre-existing | Move the ask to a checkpoint on the activity, as `discuss.md` now does; leave the ops the produce path that consumes the answer |
| 31 | High | `canonical-technique-reference` | `techniques/research/research.md:29-31` and `techniques/research/reconcile.md:39-40` | `research.md` Protocol phase 2 reaches the raw resource URI directly — "Fetch `concept-rag://activities` resource to load its index", "Match research goal to an available entry from the index", "Follow the matched entry's technique workflow and tool sequence" — and `reconcile.md:39` repeats it ("match it to a `concept-rag://activities` entry and follow that technique's tool sequence"). `meta/techniques/knowledge-base-search/load-domain-index.md` is the op that wraps this capability, so the wrapper carve-out does not reach either site. `reconcile.md:39` compounds it by citing "the same mechanisms as the [research](./research.md) operation" — a sideways citation to a fellow consumer rather than to the owner. `WebSearch` is named raw at both sites too. Same shape as the prior pass's finding on `create-issue.md`, which was fixed by replacing raw Atlassian primitives with the `atlassian-operations` wrapping ops | diff | Bind or Apply `knowledge-base-search::load-domain-index`; repoint the sideways citation at the owner |
| 32 | Medium | `operative-criteria-need-a-home` | `techniques/review-assumptions/reconcile.md:78-113` | The rules `code-resolvable` (8 worked examples), `not-code-resolvable` (6 examples), `partially-resolvable` and `convergence-definition` (4 indicators) hold the whole resolvability classification vocabulary, and only this file holds it. `resources/assumptions-review.md#classification-vocabulary` owns Categories and Risk letters but not resolvability; `resources/assumption-reconciliation.md` owns the Resolution statuses and the scorecard and is the natural home. Contrast finding 33: the parallel research loop has the same vocabulary dual-homed rather than missing | diff | Migrate the classification into `assumption-reconciliation.md` beside the Resolution statuses; leave the technique a walker |
| 33 | Medium | `no-technique-resource-dual-home` | `techniques/research/triage.md:52-75` | The rules `research-reconcilable` (4 examples), `research-irreconcilable` (5 examples) and `handoff-targets` restate what `resources/research-reconciliation.md` already owns: its `### Reconcilability statuses` table (lines 18-25) defines Reconcilable as "further knowledge-base or web research could close it", and its `### Handoff targets` table (lines 27-35) lists `stakeholder` / `code-analysis` / `out-of-scope` with the same meanings. The technique loads that resource at lines 28 and 49, so both files hold the same facts | diff | Delete the three rules; cite the resource's two tables from the classify phase |
| 34 | Medium | `stale-restatement-after-change` | `techniques/manage-artifacts/verify-artifact-links.md:53` | Rule `exhaustive-over-sampled` states "**The three classes** are independent — a folder can ship all three at once — so a pass that stops at the first class leaves the others live." The output declares **four** (`missing-target`, `unresolved-anchor`, `worktree-citation`, `resource-id-as-link`, line 20) and Protocol phases 2-5 implement four. A fourth class was added and the count in the rule was not | diff | State the grain rather than the count, or drop the figure |
| 35 | Medium | `no-technique-resource-dual-home` | `techniques/design-philosophy/define.md:24-32` and `classify.md:34-36` | `define.md` links `design-framework.md#design-framework-trizics-approach` at line 20 and then embeds the six-item problem-definition checklist that resource area 1 ("Problem statement, system understanding, impact, constraints, root cause") and its artifact Template sections already carry. `classify.md:34-35` embeds the specific-vs-inventive decision tree that resource area 2 owns. The complexity criteria at `classify.md:36` ("simple = … minor fixes completable in <30 minutes; moderate = …; complex = …") are in neither home, so that clause is `operative-criteria-need-a-home` on the same walk | diff | Delete the restated checklist and tree; move the complexity criteria into `design-framework.md` and cite both |
| 36 | Medium | `technique-stage-agnostic` | `techniques/design-philosophy/determine-path.md:31` | "`{problem_complexity}` also scopes how much of the [design framework](../../resources/design-framework.md#design-framework-trizics-approach) **plan-prepare later applies** — simple: problem definition, conventional solutions, synthesis; moderate: add problem classification; complex: include inventive solutions." The sentence names a downstream activity and what it does later, which is the question the entry's test forbids a technique from answering | diff | State the scoping as a property of the complexity value; leave the applying activity unnamed |
| 37 | Medium | `declared-input-never-read` | `techniques/requirements-elicitation/create-document.md:20-22,43` | `elicitation_log` is declared under `## Inputs` and appears nowhere else in the file. `success_criteria` and `scope_boundaries` appear only as the plain English words "success criteria, scope boundaries" at line 43, not as their ids, which is `brace-declared-ids` mode (a) on the same line. Second instance of the class in finding 6 | diff | Reference each `{id}` from the phase that records it, or delete the declaration |
| 38 | Medium | `technique-inputs-declared` | `techniques/research/reconcile.md:46` | Protocol says "Append the gathered findings to `{research_document}`", and `research_document` is declared only under this op's `## Outputs` (line 26) — and under the container's Outputs, never as an input. So the existing artifact the op augments is not a declared input. The sibling convergence loop declares its written-back artifact on both sides (`review-assumptions/TECHNIQUE.md:20,26` for `assumptions_log`), which is the conforming shape | diff | Declare `research_document` as an input as well, matching the assumptions-log pattern |
| 39 | Medium | `bind-protocol-locals` | `techniques/implementation-analysis/analyze.md:37,40` | `{$symbol}` is bound at line 37 and never read as `{symbol}` — Detect (b), a dead binding. `{$concept}` is bound at line 37 and bound again at line 40 on the same sequential path, and never read bare either; the entry's declare-once rule admits a second `{$name}` only in mutually exclusive producing branches, and line 40 opens "Then apply…". `binding-fidelity` does not reach this: its `proves` line covers reads having producers, not locals being read, and no ledger entry cites this file | diff | Bind each local once at its producer and read it bare at every use |
| 40 | Low | `paren-invocation-args` | `techniques/implementation-analysis/analyze.md:46` | "via [gitnexus-operations](…)::[impact](…) `{target, direction: 'upstream'}`" passes the argument list in braces where the entry requires parentheses on the op reference. Lines 37 and 45 of the same file use the parenthesised form, so the deviation is internal | diff | `(target: …, direction: 'upstream')` |
| 41 | Low | `rule-binds-beyond-its-operation` | `techniques/manage-artifacts/TECHNIQUE.md:90` rule `push-before-linking` | The first clause is an invariant on what the group writes ("Any engineering link … MUST resolve to a committed file on the remote"), which the entry carves out. The second commands an ordering the group does not own — "Commit and push the planning folder BEFORE writing a URL that points into it" — a duty belonging to the activity graph and to `manage-git::artifact-commits`. `activities/13-submit-for-review.yaml:131-141` already sequences the commit before the summary render, so the step order enforces it and the rule restates it | pre-existing | Keep the link-resolves invariant; drop the ordering clause to a pointer at the owning bind site |

| 42 | High | Single Source of Truth (principle; no catalog Detect reaches this spelling) | `activities/09-lean-coding-audit.yaml:25-27`, `activities/12-strategic-review.yaml:55-57`, `techniques/strategic-review/resign-commits.md:26-28`, `techniques/apply-review-fixes.md:12-14` | One bag name, `review_findings`, carries four declared meanings. Activity 09 writes it as "The lean-coding audit's findings, each under a stable `LC-{n}` designator carrying its taxonomy tag, location, simpler alternative and the lines it would save". Activity 12 writes it as "The findings the scope and artifact review produced". `resign-commits` declares it as an output carrying "a re-sign failure among them". `apply-review-fixes` declares it as an **input**: "The user-selected review findings in scope for this fix cycle (code-review and/or test-suite findings)". The consequence is a live mis-binding: `activities/10-post-impl-review.yaml:24` only *reads* `review_findings` and never writes it, so the sole writer reaching activity 10 is activity 09, and the `review-fix-cycle` loop at `activities/10:194-197` hands the lean-coding audit's findings to an op declaring code-review and test-suite findings. The code-review findings live in `classified_findings` and the report artifacts, not here. `check:activity-variables` passes because both declarations agree on `type: array` with no `defaultValue`; it compares types, not meanings | diff | Split into per-pass ids (`lean_audit_findings`, `strategic_review_findings`) and bind the fix cycle to the code/test set it declares |
| 43 | High | `pass-orchestration-in-technique` | `techniques/strategic-review/review-scope.md:78-80` and `techniques/review-existing-feedback.md:34-40,69` | Two techniques run another group's op for work and re-export its outputs. `review-scope` phase 8: "Run [update-pr](../update-pr/TECHNIQUE.md)::[verify-body](../update-pr/verify-body.md) against the live body", then reads `body_conforms` / `body_findings` at line 80. `review-existing-feedback` phase 4: "Run [update-pr](./update-pr/TECHNIQUE.md)::[verify-body](./update-pr/verify-body.md) against it, emitting `{body_conforms}` and `{body_findings}`" — and declares both as its **own** `## Outputs` (lines 34-40), which is the entry's explicit Detect signal, "Outputs that only re-export children". Both bind sites' activities declare those names in `variables.writes` while binding no step that produces them (`activities/01:23-30`, `activities/12:25-32`). The prior pass withdrew this entry as corpus form for `Apply [op]` sites generally; these are distinguishable because `activities/13-submit-for-review.yaml:293-295` binds the very same op as its own activity step, so the bind-site form is available and already in use for it | diff | Bind `update-pr::verify-body` as a step in activities 01 and 12 ahead of its consumers, and let each consumer declare the verdict as an input |
| 44 | Medium | `duplicate-shared-capability` | `techniques/strategic-review/resign-commits.md:38` | "When a push is required, use `` `git push --force-with-lease` `` to `{push_remote}`" — a raw push recipe where `meta/techniques/version-control/push-branch.md` owns pushing. That op's Protocol is a plain `git push {remote_name} {branch}` with no force flag, so it "almost fits but lacks an optional flag", which the entry's near-miss clause says still leaves it owning the capability | diff | Add an optional force-with-lease flag to the meta op and Apply it; see also finding 4 |
| 45 | Medium | `no-duplicated-guidance` | `techniques/strategic-review/changes-folder.md:38` and `verify-fragment.md:35` | The same CI-conformance criterion in two homes, regex included: "must contain a GitHub issue reference matching the regex `github\.com/.+/issues/[0-9]+` or `(Fixes|Closes|Resolves):?\s+#[0-9]+`" at the writer, and "or an equivalent GitHub issue reference matching `github\.com/.+/issues/[0-9]+` or `(Fixes|Closes|Resolves):?\s+#[0-9]+`" at the checker. A change to what CI accepts has to land in both | diff | Home the reference form once — a resource section both cite — and leave each op the write and the check |
| 46 | Low | `no-rationale-in-description` | `techniques/strategic-review/recommend-cleanup.md:20,28` | The output description ends "Source is NOT mutated in review mode" and Protocol closes "Do NOT edit the source in review mode — the recommendations are for the reviewer to act on." Both restate the bind-site gate: `activities/12-strategic-review.yaml:151` binds this op `when: is_review_mode == true`, so the mode is already encoded by adjacent structure | diff | Drop both mode clauses; the gate carries them |

| 47 | High | `cited-home-owns-claim` | `techniques/update-pr/TECHNIQUE.md:52-61` | Rule `pr-body-conformance` declares the resource the sole home: "A rendered body satisfies every criterion in [Rules](../../resources/pr-description.md#rules), **which is their home** — the guide that lays out the body owns what a conforming body looks like." But rule `body-composition` directly above states two criteria the resource's `## Rules` does not carry — `motivation-user-perspective` ("Motivation explains the problem from the user's perspective and the consequences of not addressing it; 1-2 paragraphs") and `todo-tracks-premerge`. The resource's eight criteria are Mandated sections present, Summary length, Engineering link present, Issue line present, Changes grouped by component, Changes carry no file list, Changes carry no code, and Changes state the substance. `verify-body.md:29-30` evaluates only "each remaining criterion in [Rules]" of the resource, so those two criteria are stated as conformance requirements and **nothing ever checks them** | diff | Move the two criteria into the resource's `## Rules`, or drop the claim that the resource is their home |
| 48 | Medium | `reference-without-provenance` | `techniques/update-pr/TECHNIQUE.md:56` | `no-relocated-content` closes "complements no-files-changed-list and no-code-in-changes **below**". Neither slug exists anywhere: the resource names those criteria "Changes carry no file list" and "Changes carry no code", and they are in the resource, not below — this file's remaining rules are `pr-body-conformance`, `draft-first`, `posting` and `remote-git-runs-on-the-host-shell`. The citation is wrong in both its names and its location | diff | Cite the two resource criteria by their headings, or drop the cross-reference |
| 49 | Medium | `technique-outputs-declared` | `techniques/update-pr/post-review-comment.md:31-33` | Protocol sets `{posted_review_id}` from the op at line 31 and reads it on a later run at line 32 ("Where this run already posted a review, supply *review_id*=`{posted_review_id}`"), and reads `{live_review_body}` from the op at line 33. `## Outputs` declares only `review_posted`. A value a later run binds is a bind contract, not a local | diff | Declare `### posted_review_id` under `## Outputs`; take `live_review_body` from the applied op rather than naming it as a bag value |
| 50 | Medium | Maximize Schema Expressiveness (principle; no catalog Detect reaches a bind-unreachable Protocol branch) | `techniques/update-pr/render.md:19` and `techniques/update-pr/TECHNIQUE.md:32-34` | The container declares `is_review_mode` as an optional input and `render.md` phase 1 branches on it: "If `{is_review_mode}` is true → [Review Comment Template]". No bind site can reach that branch — all four gate it out: `activities/06:159`, `activities/12:85` and `activities/13:274` carry `when: is_review_mode != true && stealth_mode != true`, and `activities/13:288-292` sits inside a loop with the same gate. The branch is also the conflation the group's own `posting` rule and `post-review-comment`'s `review-comment-not-body-render` rule both forbid — rendering the Review Comment Template into the PR *description* rather than posting it as a review | diff | Delete the branch and the input; the review path posts through `post-review-comment` |
| 51 | Low | `paren-invocation-args` | `techniques/update-pr/create-pr.md:39` and `mark-ready.md:29` | Argument names are backticked rather than italic on `github-cli-protocol` Apply sites — "with `repo_path` `{target_path}`, `branch_name` `{branch_name}`, `base_branch` the repo default … and `as_draft` true", and "with `repo_path` `{target_path}` and `{pr_number}`". Detect (b) names the backticked form explicitly, and the same op family is cited with italic names at `create-issue.md:51`, `review-diff.md:64`, `review-summary.md:75` and `detect-merge-strategy.md:18`, so the deviation is internal to the family | diff | Italicise the argument names |

| 52 | Medium | `single-rule-authority` | `techniques/review-code.md:107-109`, `techniques/review-test-suite.md:109-111`, `techniques/strategic-review/review-scope.md:88-90`, and as prose at `techniques/review-summary.md:87` | One invariant, four homes, verbatim in three of them: "Every finding names a file within the authored surface `{changed_files}`. Findings on files in `{changed_files}` form the PR's findings; findings on other files form a separate 'pre-existing' grouping." `worker-rule-reach` licenses per-technique duplication only where no shared container every affected technique loads exists; here the workflow-root `techniques/TECHNIQUE.md` merges into all of them and is the natural home — it simply carries no `## Rules` section, which is the same absence finding 9 reports from the README side | diff | Hoist `findings-constraint` to a `## Rules` section on the workflow-root `TECHNIQUE.md`; delete the three copies. This also gives finding 9's README claim something true to point at |
| 53 | Medium | `technique-stage-agnostic` | `techniques/review-existing-feedback.md:70`, `techniques/strategic-review/review-scope.md:79`, `techniques/review-baseline-state.md:43` | Three Protocols name a surrounding pass or the workflow itself. "The **strategic pass** re-reads it for what has changed since"; "A **review run checked the body at intake**, so record here only what has changed since … On a create run this is the first check"; "Check out the PR branch **to continue the workflow**." Each answers *where in the workflow am I?*, which the entry's test forbids. Second, third and fourth sites of the class finding 36 reports | diff | State each op's own contribution; leave the surrounding passes to the activity graph |

| 54 | High | `unproduced-value-read` (ordering variant; the entry's Detect keys on a gate, so the principle **Single Source of Truth** is the sole detector for this spelling) | `activities/06-plan-prepare.yaml:91-93,147-149` with `techniques/create-test-plan.md:12-14` | `create-test-plan` declares `todo_tasks` as a **required** input — "Atomic task breakdown with dependencies and ordering for the work package" — and spends it at Protocol phase 2 ("using the `{todo_tasks}` breakdown to scope coverage to each task and its dependencies"). Its sole producer is `plan-prepare::create-todos`, which activity 06 binds at step 8 (line 147), six steps **after** `create-test-plan` at step 3 (line 91). So on every run the test plan is authored against an unbound input. The value it needs does exist by then — `plan.md:24-26` declares `#### tasks` as a component of `plan_document`, produced at step 2 — so the slot is simply the wrong one. `check:activity-variables` cannot see it: `todo_tasks` appears in neither the activity's `reads` nor its `writes`, so the namespace gate at `src/utils/activity-variables.ts:386` treats it as internal and the guard's before-write path check never reaches it | diff | Bind `create-test-plan` after `create-todos`, or have it declare `plan_document.tasks`, which is produced before it |
| 55 | Medium | `duplicate-shared-capability` | `techniques/respond-to-pr-review.md:63,69` | Protocol posts to the pull request with no op named — "Post the drafted responses to the PR comment thread" and "Post response summary to PR, finishing with a summary of all changes made" — while `meta/techniques/github-cli-protocol/` owns posting and the corpus uses it: `post-review-comment.md:31` applies `post-pr-review`, and `activities/07-assumptions-review.yaml:179` binds `github-cli-protocol::comment-issue`. Every read in this same Protocol is correctly routed through an applied op (lines 37, 39, 48), so the posting steps are the deviation | diff | Apply the owning `github-cli-protocol` op for each post |
| 56 | Medium | Creation Guide for Generated Documents (principle; no catalog Detect reaches this spelling) | `techniques/create-test-plan.md:56-87` against `resources/test-plan.md:112-114` | Seven operative fill rules for `test-plan.md` live on the technique — `test-id-format` (ID shape, hyperlink target, disabled-test suffix and its `> [!NOTE]`), `test-case-content` (objective phrasing, `  <br>` step separator, the five-value Type vocabulary), `unified-test-case-table`, `acceptance-criteria-mapping`, `content-boundaries`, `structure-and-header`, `naming-and-storage` — while the guide's own `## Rules` holds a line budget and nothing else. The principle puts "operative `## Rules` for how to fill it" in the guide, and the corpus agrees: `adr.md`, `token-usage.md`, `provenance-log.md`, `session-trace.md`, `deferred-items.md`, `follow-ups.md` and `prior-feedback-triage.md` all carry their fill rules in the guide, so `test-plan` is the outlier | diff | Move the seven rules into `resources/test-plan.md#rules`; leave the technique citing them |
| 57 | Medium | `no-technique-resource-dual-home` | `techniques/respond-to-pr-review.md:91-93` | Rule `blockquote-paste-format` restates form the cited resource already shows: "Format each response as a blockquote (`>`) … include an '**Optional doc wording:**' block when proposing documentation text changes", where `resources/pr-review-response.md:21-24` renders exactly that blockquote with an `**Optional doc wording:**` block inside its Response Format Template — which this Protocol cites at lines 36 and 61 | diff | Delete the rule; the template carries the form |

| 66 | High | `io-id-shape` (synonym drift) | `techniques/plan-prepare/TECHNIQUE.md:16-22` and `plan.md:38` | Two declared inputs no construct in the corpus produces. `analysis_findings` ("Implementation analysis findings (baselines, gaps)") and `research_findings` ("Research findings from knowledge base and web") appear at exactly three sites corpus-wide: these two declarations and the read at `plan.md:38` — "Review `{design_philosophy_doc}`, `{requirements}`, `{analysis_findings}`, `{research_findings}`". The implementation-analysis group produces `analysis_document`, `located_implementation`, `effectiveness_assessment`, `baseline_metrics`, `gaps_identified`; the research group produces `research_document`, `kb_findings`, `web_findings`, `findings_synthesis`, `applicable_patterns`, `research_candidates`, `synthesis_assumptions`. Neither drifted id is among them, and `canonical-home-map.md:20-21` homes both concepts in the artifacts. `binding-fidelity` passes because both are optional, so it requires no producer; the only work-package `implementation-analysis` ledger entry is a `dead-output` verdict on `analysis_document`, unrelated | diff | Rename to the ids that exist (`analysis_document`, `research_document`) or to the component sets the plan actually reviews |
| 58 | Medium | `stale-restatement-after-change` | `resources/test-suite-review.md:114` | The Method Record Template's Assessment Criteria section reads "if all of Relevance & Business Alignment, Coverage Completeness, and Test Effectiveness PASS, state **'All 3 assessment criteria PASS'**", while `## Review Criteria` (lines 15-25) declares **five**: Relevance & business alignment, Coverage & completeness, Effectiveness & quality, Salience & risk focus, Architecture & organization. A conforming method record therefore certifies three of five and reads as complete. Second instance of the class finding 34 reports, and `codebase-comprehension.md:337` states the corpus's own rule against it (`demonstratives-over-counts`) | diff | Name the grain rather than the tally, or carry all five |
| 59 | Medium | `no-resource-caller-backlink` | `resources/web-research.md:3,17` | The resource names another guide's artifact as its destination: frontmatter "Web research findings template appended to **the research document**", and `## Planning Artifact` — "Append web research findings to the existing research document (**kb-research.md**)". That is the entry's destination-naming signal, placing this form's content into a named artifact file. The creation-guide carve-out does not reach it: `knowledge-base-research.md` is the guide for `kb-research.md`, not this file. `research/document.md:48` already owns the destination correctly ("Record `{web_findings}` per the [web research findings template](…#planning-artifact), appended after the knowledge base findings") | diff | Describe the form; leave the destination to the technique that writes it |
| 60 | Medium | `no-tool-usage-prescription` | `resources/jira-issue-creation.md:3,113` | A non-engine resource names a raw MCP tool and describes its behaviour: "The `createJiraIssue` MCP tool accepts markdown format and handles conversion", with the frontmatter advertising the file as carrying an "MCP tool reference". `meta/techniques/atlassian-operations/create-jira-issue.md` is the wrapping op, and `create-issue.md:71` already binds it, so the engine carve-out does not reach this surface | diff | Drop the tool name and its format claim; the wrapping op owns both |
| 61 | Medium | `cited-home-owns-claim` | `resources/architecture-review.md:15-21` against `:48` | Line 48 declares the ADR guide the home — "The record's skeleton, its per-section fill guidance and **its fill rules** are the [ADR creation guide](adr.md#template)'s, with the guide's [Rules](adr.md#rules) governing what a conforming record carries." Lines 19-21 then state four fill constraints `adr.md#rules` does not carry: exclude "file paths or module names; code snippets or API signatures; implementation steps; testing strategies", plus the ticket-reference form. Nothing verifies ADR conformance, so those four are unchecked. Second instance of the pattern finding 47 reports | diff | Move the exclusions into `adr.md#rules`, or drop the claim that guide owns every fill rule |
| 62 | Medium | `no-dense-prose-after-config-examples` | `resources/rust-substrate-code-review.md:224-230` | `## Review Output Format` re-explains the sections the `## Report Template` fence (lines 40-74) already shows — Summary, Strengths, findings — and has already drifted from it: the prose calls the first field "**Summary:** overall assessment (1-5 stars)" while the Template renders "**Overall Quality:** X/5". One fact, two names, in one file | diff | Keep the Template; cut the restatement, carrying over the star scale as a Template annotation |
| 67 | Medium | `value-set-in-prose` | `techniques/dco-provenance/append-task-row.md:42` and `record-attestation.md:14` | `context_scope`'s description enumerates its members — "One of: `repo-only` (only repository-local sources used) \| `web-retrieval` (external web sources informed the work) \| `mixed` (both)" — while its declaration at `activities/04-research.yaml:32-35` carries a `defaultValue` and no `values`, so the prose is the set's only home. `attestation_option`'s description repeats "One of: `certify` \| `flag-legal`" where `activities/13:35-41` already declares `values`, which the entry's do-not-flag admits only when the description names the value without repeating the members. `prompt_class` and `check_id` are exempt: neither is a workflow variable, and the technique-input schema has no `values` field to hold their sets | diff | Declare `values` on `context_scope`; cut the roster from both descriptions |
| 68 | Medium | One Authoritative Home (principle; the corpus convention is the discriminator) | `techniques/conduct-retrospective/TECHNIQUE.md:31-41` | The group container declares `session_trace_document` with a concrete `#### artifact` (`session-trace.md`) and `#### audience`, and `retrospective.md:26-36` declares the same output with the same filename. Every other group container in the workflow declares its product without a filename and leaves that to the leaf — `design-philosophy`, `requirements-elicitation`, `research`, `finalize-documentation`, `strategic-review`, `codebase-comprehension`, `plan-prepare` and `implementation-analysis` all do — so the artifact contract has two declaration sites here and one everywhere else | diff | Drop the `#### artifact` and `#### audience` from the container; the leaf owns the filename |
| 69 | Medium | `artifact-not-buried` | `techniques/review-assumptions/record.md:38`, `techniques/strategic-review/document-findings.md:44`, `techniques/finalize-documentation/create-complete-doc.md:52` | Two persisted registers are created by prose and declared by nobody. `deferred-items.md` and `follow-ups.md` each have a creation guide, a `resources/README.md` index row, and a `readme-seed.md` Progress row, and the resources state they are "created lazily … when the first deferred item appears" — but no technique carries `#### artifact` for either, so the server's synthesized artifact contract never includes them. The only near-declaration is `raise-deferred-items/collect.md:18`, where `deferred-items.md` is an input's `#### default`, not an output. `artifact-guides` passes because it checks declared artifacts, so an undeclared one is invisible to it | diff | Declare `#### artifact` for each register on the op that creates it first, or on a dedicated op the deferring sites bind |
| 70 | Medium | `technique-stage-agnostic` | `techniques/conduct-retrospective/retrospective.md:60-61` | Protocol phase 4 instructs a wait on an external event and names another activity's work: "**Implementation path:** once that PR has merged, update status; if it has not merged yet, **wait for merge or address review feedback first**." Addressing review feedback is `activities/13-submit-for-review.yaml:442-449`, and `activities/14-complete.yaml` carries no gate for the wait — its only checkpoint is the per-item `deferred-item-raise#{…}` — so the terminal activity blocks on a human merge with no structural home. Fifth site of the class findings 36 and 53 report | diff | Emit the merge state as an output and let the activity gate on it |
| 63 | Low | `no-guide-wrapper-ceremony` | `resources/tdd-concepts-rust.md:201-207` | `## Conclusion and Best Practices` recaps the guide's own body: item 3 restates the 0-1-N and boundary rules from lines 102-103, item 4 restates the legacy strategies from lines 115-123, and item 5 restates the Red-Green-Refactor bullet from line 20. The corpus's own stance against closing recaps is `manage-artifacts.state-once-per-artifact` | pre-existing | Delete the section; the body carries every item |
| 64 | Low | `no-resource-caller-backlink` | `resources/implementation-analysis.md:124` | The Template's closing line is "**Status:** Ready for plan-prepare activity", so the emitted artifact names the consuming activity by id. The sibling template does it correctly — `wp-plan.md:76` ends "**Status:** Ready for implementation", naming a phase rather than an activity | diff | Name the state, not the next activity |
| 65 | Low | Convention Conformance — fill-rule placement | `resources/manual-diff-review.md:73` | The line budget sits as a bullet nested under `### permanent-blob-citations` rather than as a direct `## Rules` bullet, so a consumer citing `#permanent-blob-citations` receives the budget as part of that rule's span. Every sibling guide places it directly under `## Rules` — `strategic-review.md:134`, `adr.md:80`, `token-usage.md:47`, `session-trace.md:42`, `provenance-log.md:26`, `prior-feedback-triage.md:75`, `test-plan.md:114` | diff | Move the bullet up to `## Rules` |
| 71 | Low | `avoidance-voice-in-definitions` | `techniques/conduct-retrospective/TECHNIQUE.md:28` | The output description ends "written as the `## Workflow Retrospective` section of the close-out document **rather than a standalone artifact**" — comparative framing against an alternative design. The leaf carries the positive-present form of the same sentence at `retrospective.md:24` ("written as the `## Workflow Retrospective` section of the close-out document"), so the file pair shows both voices | diff | Drop the trailing clause |
| 72 | Low | State Contract Contribution (principle) | `techniques/dco-provenance/TECHNIQUE.md` | The container is nine lines: a Capability naming a domain ("DCO compliance and AI provenance tracking for the work package") and no Inputs, Outputs, Rules or Errors. The principle has a container Capability name what shared contract it contributes; this one contributes none, and every other group container in the workflow declares at least one of the four | pre-existing | Hoist the shared provenance-log slot the two ops both carry, or fold the ops up and drop the container |

### Withdrawn on adversarial re-derivation

Recorded so a later pass does not re-raise them. Eight of nine fell on opening the resolver rather than on re-reading the file.

| Candidate | Why it fell |
|---|---|
| `activity-variables` omission — `classification_rationale` interpolated at `activities/02:99,256` while absent from that activity's contract | The guard is the resolver and it passes by design: `src/utils/activity-variables.ts:386` adds a produced name to `writes` only when some activity declares it as a read, and a name an earlier step of the same activity produces is an internal read. Omitting intra-activity technique outputs is corpus convention |
| `output-without-destination` on `target_workflow_outcomes` | Read cross-workflow by `meta/activities/04-end-workflow.yaml:12,32` |
| `reference-without-provenance` on `{pr_reference}` at `REVIEW-MODE.md:25` | A declared input of `review-mode-detection.md:16` |
| `bag-value-as-literal` / `link-named-artifacts` on the ~20 hard-coded `NN-` link targets in `resources/readme-seed.md:31-60` | `meta/resources/planning-readme.md:90-91` mandates exactly this: "An artifact link targets the minted filename… so a seeded link resolves on arrival rather than 404ing", and "Seeded links are provisional" |
| `readme-orients-not-transcribes` on the Estimate column in `resources/readme-seed.md:28` | `meta/resources/planning-readme.md:37,76,80` declares Estimate as one of the Template's five columns |
| `bag-value-as-literal` / `factor-repeated-paths` on `.engineering/artifacts/reviews` at `README.md:108` | A single-use literal with no declared slot carrying its meaning — both entries' do-not-flag |
| `no-set-of-technique-output` on the value-less `set` blocks at `activities/01:363-366,486-489,719-731` | Carve-out (b) — caller-specific derivation from a generic tool-wrapper op |
| `no-duplicate-technique-steps` on the four `review-diff` binds in `activities/10:93,109,129,207` | The do-not-flag for distinct-purpose invocations at different pipeline points covers them, and no sub-mode input distinguishes them that classification (c) could key on |
| `validate-message-economy` on the consequence clauses at `activities/06:70,73` | A single subordinate clause naming what is unresolved is not the "trailing consequence essay" Detect names, and the form recurs across sibling activities |
| `resource-id-names-its-content` on the bare-noun resource ids | The convention runs the other way: 34 of 36 ids carry no kind word, so `complete-wp-guide` and `readme-seed` are the outliers and the bare noun is the established form |
| `dotted-rule-address` on the anchored own-rule citations at `review-assumptions/reconcile.md:35,46,59` | The corpus admits both forms — `commit-paths.md:39` and `review-diff.md:105` cite rules by `.md#anchor`, while `activities/13:314` uses the dotted `update-pr.pr-body-conformance`. An anchor is guard-checked by `resource-anchors`, and `phase-cited-by-ordinal` treats an anchor as the safer form |
| `output-without-destination` on `design-philosophy/TECHNIQUE.md:22-38`, whose `design_philosophy_doc` components no bind site consumes | `container-contract-shape` — a named rationale in `scripts/binding-fidelity-triage.json` and the entry's own second do-not-flag: a container output naming the shape the whole group contributes to has no single consumer by design |
| `paren-invocation-args` (b) on the bare gitnexus argument names at `analyze.md:37,45`, `classify.md:37`, `implement-task.md:46`, `apply-review-fixes.md:32`, `review-diff.md:83` | The corpus splits by op family, consistently: italic `*repo_path*=` for `github-cli-protocol` and `atlassian-operations` sites, bare `target:` / `name:` for `gitnexus-operations` sites. A finding here is a finding against that convention, argued corpus-wide |
| `resource-fills-not-does` on `resources/design-framework.md:26,36` ("Always start here", "Move to inventive solutions only if") | Methodology routing inside a design method, not session or workflow routing. The entry's do-not-flag covers reference lexicons and calibration benchmarks, and methodology consumed by a technique other than its filename suggests stays a resource for its real consumer |
| `session-interaction-in-technique` on `manage-git/instruct-merge-strategy.md:24,32` ("instruct the human to merge locally") | The op assembles `{presented_merge_guidance}` and `activities/13:341-349` surfaces it through an `action: message` — the entry's first do-not-flag, assembling a declared output the activity will surface |

## Coverage

| Home | Unit | Status |
|------|------|--------|
| Guards | `binding-fidelity` | `blocked` — `scripts/binding-fidelity-triage.json` stamps `corpusSha 3695f3ab970cefa552cfc38329add4ecbae55e17`, the pointer `main` pins, while the audited tree is `56977e2c…`. 60 work-package files changed between them and 13 ledger entries cite work-package sites, so the clean result says the 64 accepted verdicts are old rather than that nothing is wrong |
| Mechanical checks | Option-coverage walk | `blocked` — not run. The change surface touches step lists, exits, gates and the graph, which is when the canon requires it |
| Anti-Patterns | Creation Rules family | `not-applicable` — its own text scopes it to authoring the catalogue |
| Anti-Patterns | Interaction family | `not-applicable` — keys on session conduct a static definition tree cannot exhibit |
| Anti-Patterns | Execution family, excluding `structure-backed-constraints` | `not-applicable` — authoring-session conduct |
| Anti-Patterns | `cut-comment-jsdoc-verbosity` | `not-applicable` — the corpus holds no code |
**Unit residual: 2 `blocked`** of the homes' 92 units (36 guards + 13 catalog families + 35 principles + 7 inventory sections + 1 conformance) — `binding-fidelity` and the option-coverage walk. Every other unit was walked against the whole target surface; the four `not-applicable` rows above carry their reasons.

## File coverage

read 168 · unread 0 — the whole target surface.

Every definition file under `workflows/work-package` was read whole: `workflow.yaml`, `README.md`, `REVIEW-MODE.md`, all 15 activity YAMLs, all three folder READMEs, the workflow-root and all 15 group `TECHNIQUE.md` containers, all 36 resources, and all remaining technique operation files. No path carries a partial or scan-derived disposition.

The list below records what the earlier passes of this audit had left, for the reconciliation trail.

| Disposition | Paths |
|-------------|-------|
| `read` | **Root and activities (19):** `workflow.yaml`, `README.md`, `REVIEW-MODE.md`, all 15 activity YAMLs, `activities/README.md`. **Container and index (4):** `techniques/README.md`, `techniques/TECHNIQUE.md`, `resources/README.md`, plus every group `TECHNIQUE.md` reached below. **Resources (17):** `canonical-home-map.md`, `readme.md`, `readme-seed.md`, `token-usage.md`, `deferred-items.md`, `follow-ups.md`, `session-trace.md`, `provenance-log.md`, `prior-feedback-triage.md`, `adr.md`, `assumptions-review.md`, `assumption-reconciliation.md`, `design-framework.md`, `research-reconciliation.md`, `pr-description.md`, `pr-review-response.md`, `test-plan.md` (Structure and Rules sections). **Techniques (84):** all of `manage-git/` except five; all of `review-assumptions/`, `analyse-challenge/`, `design-philosophy/`, `requirements-elicitation/`, `research/`, `strategic-review/`, `update-pr/`, `plan-prepare/`; `manage-artifacts/TECHNIQUE.md`, `write-artifact.md`, `verify-artifact-links.md`; `implementation-analysis/TECHNIQUE.md`, `analyze.md`; `finalize-documentation/TECHNIQUE.md`, `create-complete-doc.md`, `ensure-docs.md`; and the standalone `review-diff.md`, `review-summary.md`, `review-mode-detection.md`, `create-issue.md`, `issue-reference-detection.md`, `issue-type-detection.md`, `project-type-detection.md`, `repo-root-resolution.md`, `naming-conventions.md`, `stakeholder-overview.md`, `assess-ticket-completeness.md`, `apply-review-fixes.md`, `implement-task.md`, `findings-classification.md`, `task-completion-review.md`, `review-code.md`, `review-test-suite.md`, `review-baseline-state.md`, `review-existing-feedback.md`, `strategic-findings-analysis.md`, `review-outcome-analysis.md`, `respond-to-pr-review.md`, `summarize-architecture.md`, `create-adr.md`, `create-test-plan.md`, `resolve-artifact-publish.md` |
| `unread` | 44 paths. **Resources (27):** `architecture-review.md`, `architecture-summary.md`, `codebase-comprehension.md`, `complete-wp-guide.md`, `findings-report.md`, `github-issue-creation.md`, `implementation-analysis.md`, `jira-issue-creation.md`, `knowledge-base-research.md`, `manual-diff-review.md`, `requirements-elicitation.md`, `review-mode.md`, `rust-substrate-code-review.md`, `strategic-review.md`, `tdd-concepts-rust.md`, `test-suite-review.md`, `web-research.md`, `wp-plan.md`, `workflow-retrospective.md`, and the remainder of `test-plan.md` (templates). **Techniques (17):** `codebase-comprehension/` ×4, `conduct-retrospective/` ×3, `dco-provenance/` ×3, `validate-build/` ×3, `raise-deferred-items/` ×3, `finalize-documentation/finalize-test-plan.md`, `render-token-usage.md`, `update-adr.md`, `implementation-analysis/document.md`, and the five `manage-git/` files `remove-worktree.md`, `restore-paths-from-ref.md`, `update-repo-submodules.md`, `verify-commit-signatures.md`, `verify-remote-private.md` |

**Existence claims in this register are bounded by that list.** Findings 4, 6, 8, 9, 15, 47, 52 and 54 rest on "no other construct supplies, spends or reads this value"; each was resolved by a corpus-wide grep and by opening the named consumer contract, but absence over a surface with 44 unread paths is weaker than absence over the target. Six of the unread resources — `review-mode.md`, `rust-substrate-code-review.md`, `test-suite-review.md`, `strategic-review.md`, `findings-report.md`, `manual-diff-review.md` — are cited heavily by findings 6, 7, 20, 32, 33 and 47, so a pass over them is the highest-value remaining reading.

## Consumer surface

29 files outside the target reference `work-package/` — `workflow-design` (10), `work-packages` (5), `codebase-wiki` (5), `remediate-vuln` (3), `workflow-authoring` (2), `plain-language` (2), `prism-evaluate` (1), and the corpus root `README.md`. No I/O contract on the change surface changed in a way that reaches them. The `refs` and `resource-anchors` guards pass over all 29.

## Prior-pass reconciliation

All seven Highs from `.engineering/artifacts/planning/2026-09-06-canon-audit-work-package/` are closed in this tree (findings 1-5 and 7 verified by reading the cited constructs; finding 6, on `finalize-documentation/TECHNIQUE.md`, sits in the `unread` region and was not re-derived).

Its 146-path residual is **not** discharged: this pass read 56 files, of which 16 were among the 23 that pass had read whole, so 40 paths came off the residual and 112 remain. Two patterns in the findings above show what the residual costs:

- **Findings 3, 21 and 22** are the defect class the prior pass found and fixed on one gate, surviving on three gates it had no reason to re-open.
- **Finding 14** is the defect class it closed on `finalize-documentation/TECHNIQUE.md`, surviving on `manage-git/TECHNIQUE.md`.
- **Finding 9** survived because the prior pass dismissed it on a miscount — it recorded "seven workflows carry it" where two do.

## Sources

| Label | Path |
|---|---|
| Guard suite | `npm run check:all` against `workflows/` at `56977e2c` — 36 pass, 0 fail |
| Binding-fidelity triage ledger | `scripts/binding-fidelity-triage.json` — `corpusSha 3695f3ab…`, 64 accepted verdicts |
| Reads/writes derivation | `src/utils/activity-variables.ts` — resolver for the withdrawn `classification_rationale` candidate |
| Planning-folder README policy | `meta/resources/planning-readme.md` — resolver for two withdrawn `readme-seed` candidates |
| Shared push capability | `meta/techniques/version-control/push-branch.md` — resolver for finding 4 |
| Criteria homes | `workflows/workflow-design/resources/{design-principles,anti-patterns,schema-construct-inventory,convention-conformance}.md` at `56977e2c` — 166 catalog entries across 13 families, 35 principles, 7 inventory sections |
| Guard registry | `scripts/guards.ts` — 36 entries |
