# Follow-Ups

> Design session · `meta` · updated 2026-07-27

| ID | Surfaced at | Item | Owner / next step | Status |
|----|-------------|------|-------------------|--------|
| F-1 | scope-and-draft | Two `check-binding-fidelity` entries are needed for `detect-resume-intent` — a `dead-output` for `resume_intent_requested` and an `orphan-input` for `user_request` — neither a defect ([why](09-file-review-note.md#binding-fidelity-findings--both-expected-neither-a-defect)) | Both entries added to `scripts/binding-fidelity-baseline.json` at validate-and-commit; the guard now reports 0 NEW. The file lives on the server repo's `main`, outside this session's worktree and PR, so the edit is uncommitted — it needs its own `main`-based branch and commit | open |
| F-2 | post-update-review | Three artifacts still describe shapes the pre-commit audit superseded: [design specification](03-design-specification.md) G3 and § Rules and [A-4](04-assumptions-log.md) specify *replacing* `00-discover-session.yaml`'s activity rule (delivered as deletion of the rule and the `rules:` key, per R-1), and [09-file-review-note.md](09-file-review-note.md) records "Keeping `when:`" for the two derived gates (delivered as structured `condition` with `exists` / `notExists`, per F-1) | Reconcile the three against [08-verified-findings.md § Resolution](08-verified-findings.md#resolution), which is the accurate record. Documentation-only; no definition change | open |
| F-3 | post-update-review | Live `bag-value-as-literal` (AP-127) plus `variable-description-one-line` (AP-126) instance found while validating AP-127's exemplars: `workflow-design/workflow.yaml` `design_dimensions.description` reads "…set per mode by derive-design-dimensions (create elicits all eight." — a hard-coded count of the slot's own cardinality, a producer tail, and an unclosed parenthesis | Outside this change's target workflow and file set; needs its own `workflow-design` change | open |
| F-4 | post-update-review | Remedia edited four already-committed files, so the branch carries uncommitted changes and needs a third commit before retrospective | Re-enter `validate-and-commit`; `needs_recommit` is true and `review_findings_count` is 0 | open |
