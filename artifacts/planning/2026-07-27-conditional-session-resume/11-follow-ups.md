# Follow-Ups

> Design session · `meta` · updated 2026-07-27

| ID | Surfaced at | Item | Owner / next step | Status |
|----|-------------|------|-------------------|--------|
| F-1 | scope-and-draft | Two `check-binding-fidelity` entries are needed for `detect-resume-intent` — a `dead-output` for `resume_intent_requested` and an `orphan-input` for `user_request` — neither a defect ([why](09-file-review-note.md#binding-fidelity-findings--both-expected-neither-a-defect)) | Closed — both entries landed as `728e5dae` on a `main`-based branch, carried by PR [#313](https://github.com/m2ux/workflow-server/pull/313); the guard reports 0 NEW. This one cannot ride PR [#311](https://github.com/m2ux/workflow-server/pull/311): `scripts/` exists only on `main`, and the `workflows` branch holds workflow definitions alone | done |
| F-2 | post-update-review | Three artifacts still describe shapes the pre-commit audit superseded: [design specification](03-design-specification.md) G3 and § Rules and [A-4](04-assumptions-log.md) specify *replacing* `00-discover-session.yaml`'s activity rule (delivered as deletion of the rule and the `rules:` key, per R-1), and [09-file-review-note.md](09-file-review-note.md) records "Keeping `when:`" for the two derived gates (delivered as structured `condition` with `exists` / `notExists`, per F-1) | Closed — spec § Rules, G3, the change-categories line and the Gate 2 sentence now describe removal; A-4 is marked superseded in part; the file review note records that the two derived gates use structured `condition:` | done |
| F-3 | post-update-review | Live `bag-value-as-literal` (AP-127) plus `variable-description-one-line` (AP-126) instance found while validating AP-127's exemplars: `workflow-design/workflow.yaml` `design_dimensions.description` reads "…set per mode by derive-design-dimensions (create elicits all eight." — a hard-coded count of the slot's own cardinality, a producer tail, and an unclosed parenthesis | Closed — `design_dimensions.description` rewritten to one line naming the value, dropping the count, the producer tail and the unclosed parenthesis; landed as `ea998ae1` on the session branch and recorded as manifest row 11 | done |
| F-4 | post-update-review | Remediation edited four already-committed files, so the branch carries uncommitted changes and needs a third commit before retrospective | Closed — landed as `8bdc8f0c` and pushed; the recommit pass through `validate-and-commit` verified the tracked tree clean, re-ran the guards, and refreshed PR [#311](https://github.com/m2ux/workflow-server/pull/311)'s body | done |
| F-5 | post-update-review (2nd) | The second audit pass found and remediated [P-11](10-principle-findings.md) in `meta/activities/README.md`, so the branch again carries an uncommitted change and needs a fourth commit before retrospective | Closed — landed as `ea998ae1` and pushed alongside the X-1 hand-off repair, committed directly rather than through another `validate-and-commit` pass | done |
| F-6 | post-update-review (2nd) | [Scope manifest](07-scope-manifest.md) states `file_count` = 8 and lists 8 rows, while the branch carries 9 files — the user-directed AP-127 entry on `workflow-design/resources/anti-patterns.md` is authorised but unrecorded in the artifact that is the canonical home for the file manifest | Closed — manifest `file_count` is now 11 with rows 9 to 11 added, the target version reads v5.10.0, and `01-initialize-session.yaml` moved out of the out-of-scope list into row 9 | done |
| F-7 | direct fix | The resume hand-off defect recorded as [X-1](05-deferred-items.md) was repaired inside this change rather than deferred: `initialize-session` now binds `client_planning_slug` from `saved_planning_slug` on the resume arm | Closed — landed as `ea998ae1`; manifest row 9 | done |
| F-8 | direct fix | Worktree paths hard-coded to a home directory in `derive-workflows-target-path.md`, `naming-conventions.md` and `work-package/activities/README.md` ([#309](https://github.com/m2ux/workflow-server/issues/309)) | Closed — derived from the checkout as `<checkout>/.worktrees/<slug>/`, landed on the session branch as `e5abc5b5` and carried by PR [#311](https://github.com/m2ux/workflow-server/pull/311), which closes [#309](https://github.com/m2ux/workflow-server/issues/309). First shipped as its own branch and PR #312; consolidated into #311 and #312 closed, since it is the same content tree and belongs on one branch | done |
