# Deferred Items

> Hierarchical Path-Scoped Resource Section References · #141 · updated 2026-07-27

**D-5 is no longer deferred** — brought into scope at the research-convergence gate (candidate RC-2).
Its row is retained per the update-in-place rule; see the Follow-up cell.

| ID | Deferred at | Item | Reason | Follow-up |
|----|-------------|------|--------|-----------|
| D-1 | requirements-elicitation (D4 / [DP-10](02-assumptions-log.md)) | Path-scoped hierarchical section addressing — the issue's Defect 2, its third acceptance criterion, the multi-segment half of its fourth, and the path-ref half of its sixth | Every measured case is reachable without new syntax: all 10 duplicate slugs sit inside single files and yield to GitHub `-N`; the 8-sweep case needs only unique flat anchors. The one file where hierarchy beats `-N` has zero anchored referencers and its adoption is out of scope in the issue. A path ref also passes 5 silent stages before 1 loud failure and needs 4 surfaces edited, not the 1 the issue names | — until raised. Assume delimiter `/` ([DP-5](02-assumptions-log.md)), confined to tool calls and `resources: [...]` entries and never prose links, since no hierarchical syntax is GitHub-clickable |
| D-2 | requirements-elicitation (D3 / [DP-7](02-assumptions-log.md)) | The hidden 4th anchor break: `work-package/resources/architecture-summary.md:214` → `work-package-plan.md#dependencies--risks` | Two faults, not one: the file is `wp-plan.md`, and `## Dependencies & Risks` (wp-plan.md:65) sits inside a fenced template body (fence 16–77), so it stays unresolvable after a filename fix. Needs a content decision about what the link should point at | — until raised. Becomes guard-visible the moment fence hardening lands, so it is coupled to [RE-5](02-assumptions-log.md) / SC-9 |
| D-3 | requirements-elicitation | Nested resource directories are silently misresolved — `resources/<sub>/<id>.md` yields `sub/foo.md#bar`, and `parseResourceRef` reads `sub` as a workflow | Zero occurrences in the corpus today. Implicated in the same three anchor regexes this package touches, so cheap to fix alongside them, but not an acceptance criterion | — until raised |
| D-4 | requirements-elicitation | Widen `extractMarkdownSection` from `string \| null` to a diagnostic `Result`, so a failure can say why | Only needed to distinguish multi-segment failure modes (segment 1 missing, segment 2 missing within segment 1, ambiguous match), which leave with D-1 | — until raised; blocks nothing while paths are deferred |
| D-5 | requirements-elicitation (D2 / [DP-6](02-assumptions-log.md)); **reversed at research (RC-2)** | Slugger fidelity gaps against real `github-slugger`: `[^\w\s-]` strips Unicode letters GitHub preserves, and neither implementation reduces a heading's inline markdown to rendered text (`## [Foo](bar)` slugs as `foobar`; GitHub gives `foo`) | Originally an accepted limitation of owning the slugger rather than adopting the package (offline sandbox, deliberate 5-dependency runtime), on the belief that fidelity would be costly. **Research invalidated that belief:** each half is a one-line fix — a category-equivalent strip class `/[^\p{Alphabetic}\p{M}\p{Nd}\p{Pc} -]/gu` (Unicode property escapes need no dependency) and an inline link/image text reduction. Rejecting `github-slugger` means the project owns GitHub-slug fidelity permanently, so closing the gap costs less than owning a known divergence | **→ IN SCOPE** ([03-requirements-elicitation.md](03-requirements-elicitation.md#in-scope) items 10–11, [SC-11](03-requirements-elicitation.md#success-criteria)). Removes 23 guard-visible divergences; 0 live in `resources/` |
| D-6 | requirements-elicitation | Repoint `injection-pattern-catalog.md`'s consumer (`cicd-pipeline-security-audit/techniques/scan-injection-patterns/load-patterns.md:14`) off whole-file loading onto per-pattern anchors | Referencer refactor, explicitly out of scope in the issue. This is where the value case for hierarchical addressing actually lives — 326 lines loaded to read one pattern — so it is the natural consumer that would justify D-1 | — until raised; depends on D-1 |
