# Change Block Index — refresh workflow-server docs

Branch: `chore/refresh-workflow-server-docs` vs `main` (PR #119).
Total: 22 hunks across 12 files (94 insertions, 33 deletions).

Each row below is a single git diff hunk. The "Rationale" paragraph explains intent and design choices so the reviewer can cross-reference against the side-by-side diff app without re-reading every line.

| # | File | New-line range | Lines (+/-) | Subject | Rationale |
|---|------|----------------|-------------|---------|-----------|
| 1 | `AGENTS.md` | 28–35 | +2 / -2 | Refresh "Boundaries" wording: drop stale "TOON/registry" phrasing; spell out the discover-first bootstrap chain. | The previous note conflated TOON files with a long-gone "registry" concept and listed only `start_session` / `workflow-server://schemas`. The refresh aligns the boundary text with the current bootstrap chain documented in `docs/ide-setup.md` so agents have a single canonical procedure to follow. |
| 2 | `CLAUDE.md` | 28–35 | +2 / -2 | Same "Boundaries" refresh as AGENTS.md, kept verbatim across the two agent files. | `AGENTS.md` and `CLAUDE.md` are intentionally near-identical (one targets generic IDE agents, the other Claude Code). Keeping them in lockstep avoids agents seeing divergent guidance depending on which file their host injects. |
| 3 | `README.md` | 20–27 | +2 / -2 | "How It Works" step 2 + step 3 rewritten to describe the bundled operations model. | Old text claimed `get_skills` "loads behavioral protocols" and `get_workflow` "reveals the workflow structure". After the operations bundling work (op-bundle-in-workflow-response / op-bundle-in-activity-response), `get_workflow` and `get_activity` now ship an `operations` block directly, and `get_resource` lazy-loads referenced resources. The README's top-level pitch is the first thing readers see; it had to be brought in line. |
| 4 | `README.md` | 32–53 | +14 / -1 | Add "MCP Tools at a Glance" table grouping the 17 tools by concern. | The previous README listed no inventory of MCP tools at all. New table groups them into Bootstrap / Session / Navigation / Checkpoint / Skills+Ops+Resources / Trace, matches the categorisation used in `docs/api-reference.md`, and links to it for full signatures. Lowers the cost of "what tools exist?" from grepping source to scanning one table. |
| 5 | `README.md` | 105–112 | +1 / -1 | "Setup IDE Rule" prose rewritten to mention the bootstrap-on-every-request rationale. | Old sentence was a bare link. New sentence explains why the rule exists (keeps bootstrap procedure in sync with the server) so users understand what they are pasting into their IDE. |
| 6 | `SETUP.md` | 67–73 | +1 / -1 | Mirror the README's IDE-rule wording inside SETUP.md. | SETUP.md and README.md present overlapping setup instructions. Aligning their wording prevents users from getting one explanation in one file and a different one in the other. |
| 7 | `SETUP.md` | 90–103 | +6 / -3 | Expand Environment Variables table: add `SCHEMAS_DIR`; spell out what `WORKFLOW_DIR` contains; clarify what `SERVER_NAME` / `SERVER_VERSION` are used for; add lead-in sentence pointing at `src/config.ts`. | `SCHEMAS_DIR` was added during the schemas-as-MCP-resource work but never made it into the docs. The other rows were vague ("Path to workflow directories"); they now describe the directory layout and which response surface uses each value. |
| 8 | `docs/api-reference.md` | 39–46 | +1 / -1 | Mark `get_skills` as deprecated (preferred path: `get_workflow`'s bundled operations); keep the legacy description for workflows still using `skills.primary`. | `get_skills` is the legacy primary-skill entry point. New workflows declare `operations:` on the workflow/activity and rely on the server's bundled response. The doc previously implied `get_skills` was still the recommended path; the deprecation note redirects readers without removing the entry (some workflows still use it). |
| 9 | `docs/checkpoint_model.md` | 36–42 | +1 / -1 | Generalise "Cursor's `AskQuestion`" to "host UI tool" and add Claude Code's interactive prompts as a second example. | The checkpoint model is host-agnostic — any UI that can show options and capture a choice works. Hard-coding Cursor in the prose suggested otherwise. |
| 10 | `docs/checkpoint_model.md` | 54–60 | +1 / -1 | Generalise "Task(resume=…)" language; add an explicit "no-op for inline (single-agent) execution" sentence. | Same host-agnostic goal: hosts that lack a sub-agent primitive still implement the checkpoint protocol — the resume step just becomes a persona switch inside one conversation. Calling that out prevents readers from concluding the protocol only works in Cursor. |
| 11 | `docs/development.md` | 216–222 | +1 / -1 | "Universal skills" step 3 rewritten: explain that meta skills are auto-discovered and referenceable as `skill-id::operation-name` from any workflow. | Old text told contributors to "Access via `get_skills` or `get_skill { step_id }`" — that was the per-step-skill model. The new model uses operation refs on activities/workflows, and the auto-discovery contract is what contributors need to understand. |
| 12 | `docs/development.md` | 225–238 | +5 / -3 | Workflow-specific skills section mirrors the universal section: auto-discovery + reference-by-operation-name. Mentions that `get_skill { step_id }` legacy path remains. | Same refresh, applied to workflow-scoped skills. Calling out that the legacy path is still supported avoids forcing in-flight workflow migrations. |
| 13 | `docs/development.md` | 233–238 | +3 / -1 | "Skill Resolution" paragraph extended to mention `resolve_operations` and the auto-bundling into `get_workflow` / `get_activity`. | The previous paragraph stopped at "checks `{workflow}/skills/` then falls back to `meta/skills/`". After bundled responses, contributors also need to know that the resolution feeds the bundled operations block — otherwise they cannot reason about why their op refs are getting auto-resolved. |
| 14 | `docs/dispatch_model.md` | 2 | +1 / -1 | Lead paragraph: replace "relies heavily on Cursor's `Task` tool" with host-agnostic phrasing and a forward pointer to "Environment Considerations". | The dispatch model is host-agnostic; the doc previously read like a Cursor-only feature. The pointer to "Environment Considerations" gives Claude Code / single-agent readers an early signal that an inline fallback exists. |
| 15 | `docs/dispatch_model.md` | 51 | +1 / -1 | "Meta Orchestrator then uses Cursor's `Task` tool" → "host's sub-agent spawn mechanism (e.g., Cursor's `Task` tool)". | Mechanical refresh applying the same host-agnostic stance to the L0→L1 prose. |
| 16 | `docs/dispatch_model.md` | 64 | +1 / -1 | Same refresh applied to the L1→L2 prose. | Mechanical consistency. |
| 17 | `docs/dispatch_model.md` | 91–95 | +1 / -1 | "Cursor's `Task` tool supports a `resume` parameter" → general resume primitive + parenthetical for Cursor. | Same host-agnostic stance applied to the resume mechanism. |
| 18 | `docs/dispatch_model.md` | 104–108 | +2 / -2 | "Environment Considerations" closer: tighten language and add that the server's enforcement layers (HMAC, checkpoint gates, manifests, trace tokens) function identically in inline mode. | The previous version implied inline execution was a degraded mode; the new closer makes clear that only the persona switch changes. Important for Claude Code users who would otherwise question whether the workflow-fidelity guarantees still hold. |
| 19 | `docs/ide-setup.md` | 1–46 | +43 / -2 | Full rewrite: add "Bootstrap Rule", "What the Bootstrap Returns", "Schema Awareness", "Verifying the Connection", and "Related" sections. Old file was 3 lines. | The previous file was the bare rule text with no context. The rewrite makes ide-setup.md the canonical landing page for IDE configuration (linked from README, SETUP, AGENTS, CLAUDE) and gives the agent the discover-first wording that matches `.claude/rules/workflow-server.md`. Verification steps reduce silent-misconfiguration cases. |
| 20 | `docs/orchestra-specification.md` | 3–9 | +1 / -1 | Header metadata: add "last reviewed 2026-05-14" alongside the existing `Date` field. | The Orchestra spec is the architectural anchor and was last touched 2026-02-10. Adding the review date stamps "we read this on 2026-05-14 and it still describes the system" without pretending the spec itself changed. |
| 21 | `schemas/README.md` | 2–4 | +2 / -0 | Add a one-line note that the JSON schemas are also served at `workflow-server://schemas` and `workflow-server://schemas/{id}` MCP resources. | The MCP resource path existed but was undocumented in `schemas/README.md`. Readers who land here looking for "how do I get the schemas at runtime?" now have the answer without grepping `src/resources.ts`. |
| 22 | `schemas/README.md` | 1637–1643 | +3 / -2 | "Related Documentation" footer: switch hyphens to em-dashes; add links to `docs/resource_resolution_model.md` and `docs/ide-setup.md`. | Brings the related-docs list current with the resource-resolution model doc (added during the operations-bundling work) and links forward to ide-setup so readers who arrived from the schemas page can complete IDE configuration without leaving the docs tree. |
| 23 | `schemas/schema-header.md` | 4–11 | +2 / -2 | Bullet for `Skill` rewritten to describe the container-of-operations model; bullet for `Condition` updated to mention checkpoints alongside transitions/decisions/loops. | The schema header is appended to every `workflow-server://schemas` response — it is the first thing any schema-aware client reads. The Skill bullet matched the legacy "skill is a capability with protocol+tools" wording; new bullet matches the schema fields the client will actually see. Condition bullet adds the missing "checkpoints" use site (added by checkpoint-condition work in 2.x). |

## How to Use This Index

Open the diff in your side-by-side review tool. For each row above, the "New-line range" identifies the hunk's location in the post-change file. If anything reads wrong, reply with the comma-separated row numbers (e.g. `4, 18, 21`) or `none` if everything looks clean.
