# Assumptions Log

> Server-owned session setup · [#528](https://github.com/m2ux/workflow-server/issues/528) · updated 2026-09-11

## Log

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | This run is Landing 1 of #528 W1 and W2 (server-owned session setup), not the whole Safe Ground epic — the issue title is the epic; the [specification](README.md) and the user request name the slice | User | Confirmed |
| DP-2 | Design Philosophy | Complexity Assessment | L | Remaining work is implementation of a known design; complexity is complex because of CRITICAL persist fan-out and architectural alternatives already weighed, not because the approach is unknown — classification-confirmed accepted this | User | Confirmed |
| DP-3 | Design Philosophy | Workflow Path | M | Elicitation and research can be skipped because the [specification](README.md) already states requirements, conventional solutions, and the occupancy / `createSessionFile` / checkout-not-host cuts — complex problems otherwise warrant the full path | User | Confirmed |
| DP-4 | Design Philosophy | Problem Interpretation | H | [`writeSessionFile`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/utils/session/store.ts#L422) writes unconditionally and must not gain occupancy — GitNexus CRITICAL (6 callers, 9 processes); occupancy lives on `createSessionFile` | Code: comment at the definition says it replaces whatever the folder holds; no `sessionFileExists` guard on the write | Validated |
| DP-5 | Design Philosophy | Problem Interpretation | M | [`start_session`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/tools/resource-tools.ts#L116) has no `working_directory` argument; `repo` is an optional caller-supplied hint | Code: Zod properties are `planning_folder`, `repo`, `workflow_id`, `agent_id`, `context_mode`, `user_request` | Validated |
| DP-6 | Design Philosophy | Problem Interpretation | H | Transient [`dispatch_child`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/tools/resource-tools.ts#L563) promote persists onto the durable folder with no occupancy check — this is the measured wipe | Code: `ensurePlanningFolder` then `writeSessionFile` with no `sessionFileExists` test on that branch | Validated |
| DP-7 | Design Philosophy | Problem Interpretation | M | A caller-named `planning_folder` whose folder already holds `session.json` resumes the top-level session — spec keeps this; occupancy must not swallow it | Code: [`sessionFileExists`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/utils/session/store.ts#L875) branch at [`start_session`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/tools/resource-tools.ts#L255) loads and restamps | Validated |
| DP-8 | Design Philosophy | Problem Interpretation | M | Omitting `planning_folder` still opens a transient session under `os.tmpdir()` — Landing 1 keeps this path so today's bootstrap still boots | Code: [`createTransientFolder`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/utils/session/store.ts#L807) | Validated |
| DP-9 | Design Philosophy | Workflow Path | H | Occupancy-refuse, not reattach — continuing a child found in an occupied folder is out of this change; six reattach hazards are recorded in the spec's companion | User | Confirmed |
| DP-10 | Design Philosophy | Problem Interpretation | M | Any existing `session.json` on `start_session` today resumes, including when the slug was derived rather than named — spec occupancy refuses the derived-slug case and keeps named-folder resume | Code: [`sessionFileExists`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/utils/session/store.ts#L875) is the only branch; there is no named-vs-derived split | Validated |
| DP-11 | Design Philosophy | Problem Interpretation | M | [`describeSessionStoreError`](https://github.com/m2ux/workflow-server/blob/c62606b2c26b348f367ecaaa12f8f0009a1c7a87/src/utils/session/resolver.ts#L340) has no `FOLDER_OCCUPIED` arm; `SEAL_MISMATCH` tells the caller to restore from commit and does not name key rotation | Code: switch arms are `INVALID_INDEX`, `NOT_FOUND`, `COLLISION`, `SEAL_MISMATCH`, `STALE_WRITE`, `WORKSPACE_INVALID` | Validated |
| PL-1 | Planning | Design Approach | H | `createSessionFile` throws then calls `writeSessionFile`, so one persist implementation remains — occupancy must not be inlined into `persistSessionFile` | Code: [edit map](15-codebase-comprehension.md#persist-cut--2026-09-11); GitNexus `writeSessionFile` CRITICAL 6 direct | Validated |
| PL-2 | Planning | Scope Decisions | M | Agent-derived-repo sentences that live in this worktree (`start_session`, `discover`, `resolveSessionRoot`, `docs/ide-setup.md`, `docs/api-reference.md`) ship on Landing 1; corpus YAML waits for Landing 2 — the dispatch named that split; the change brief had deferred the derive-repo copy | User: activity-worker spec for this run | Confirmed |
| PL-3 | Planning | Test Strategy | M | Spec cases 1–4 and 10 use real `git init` fixture checkouts with `origin`, not a mocked `execFile` | Code: [test surface](15-codebase-comprehension.md#test-surface--2026-09-11); `src/` has no `child_process` import today | Validated |
| PL-4 | Planning | Dependency Assumptions | L | Rewriting the wipe sentence in `scripts/generate-site-data.ts` requires `npm run build:site` because `tests/docs-drift.test.ts` lists that file | Code: `tests/docs-drift.test.ts` includes `scripts/generate-site-data.ts` | Validated |
| PL-5 | Planning | Design Approach | H | An open decision is a normal handler return (JSON, no `session_index`), not a `throw`; `withSessionStoreErrors` only rewrites thrown `SessionStoreError` | Code: `withSessionStoreErrors` in `src/tools/resource-tools.ts` try/catch; occupancy is the throw (`FOLDER_OCCUPIED`) | Validated |
| PL-6 | Planning | Task Breakdown | L | Tasks 4 and 5 both edit closures inside `registerResourceTools`; they land as sequential commits on that file, not one squash | Repo commit rule; GitNexus CRITICAL on the function is the intended edit | Validated |
| AR-1 | Assumptions Review | Scope Decisions | L | No significant assumptions (plan implicit decisions already logged as DP-1–DP-11 and PL-1–PL-6, all Validated or Confirmed; spec is the plan, Landing 1 only; no new stakeholder-dependent residue) | Collect pass over the [plan](06-work-package-plan.md) and this log | Validated — null collect |
| IM-1 | Implement | Scope Decisions | L | No significant assumptions (Landing 1 followed the spec; occupancy on createSessionFile and the promote check; writeSessionFile still the unconditional persist) | Collect pass over Tasks 1–7 | Validated — null collect |

## Wrap-Up

18 assumptions — all validated/confirmed.
