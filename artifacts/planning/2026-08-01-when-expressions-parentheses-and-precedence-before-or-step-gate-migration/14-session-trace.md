# Session Trace

> when expressions parentheses/precedence (#379 / #383) · resolved at close-out · 2026-08-01

Cost: no `token-usage.md` — session state carried no usage ledger.

## Per-activity summary

**Skip form (empty tokens):** Session trace skipped — no accumulated `trace_tokens`. Mechanical history is available via `inspect_session` / `get_trace` on session `PBEHLM` when the server retains it.

Draft note: meta `end-workflow` may rewrite this artifact if tokens become available after client exit.
