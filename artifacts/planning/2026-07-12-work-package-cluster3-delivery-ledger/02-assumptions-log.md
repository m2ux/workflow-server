# Assumptions Log

> Cluster 3 Delivery Ledger (server impl) · #189 · updated 2026-07-13

## Log

One row per assumption, updated in place.

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | The design spec's cited line numbers and touch points (`resource-tools.ts` ~605-666, `workflow-tools.ts` ~588-696 & 283-348, `technique-loader.ts` 548-554, `delivery.ts:52`/`:685`) still match the current worktree — treated as authoritative requirements — because the spec was authored 2026-07-12 against this same repo. | Code: `delivery.ts:52` (`unchangedMarker`), `resource-tools.ts:605` (`projectTechniqueToYaml` call), `workflow-tools.ts:685` (`{marker,...projectTechnique}` spread) & `:308` (ops-bundle stringify), `technique-loader.ts:33/56` (`projectTechnique`/`ToYaml`) — all anchors present; existing per-block bundle dedup at `workflow-tools.ts:565-579` is the pattern C2 extends | Validated (anchors match; deep fan-out deferred to comprehension) |
| DP-2 | Design Philosophy | Complexity Assessment | L | Complexity is moderate, not complex — because the spec resolves every design judgement and enumerates all touch points, so no gitnexus complexity-signal run was needed. | Code: 3 tool sites + 1 new helper confirmed; no unexpected touch points surfaced in the anchor scan | Validated |
| DP-3 | Design Philosophy | Workflow Path | L | skip-optional is correct — the approved spec fully substitutes for requirements-elicitation and research, and the approach only extends an existing in-repo mechanism. | User (checkpoint `classification-and-path-confirmed`) | Confirmed |
| PL-1 | Planning | Task Breakdown | L | The 6-task decomposition (helper → get_technique wire-in → get_activity wire-in → C12 → tests → docs) faithfully covers every touch point the spec enumerates — because §2.6 lists exactly the `delivery.ts` helper + the two tool call-sites, §3 the get_workflow C12 point, §4 the test extension, and §5 the doc surfaces. | Code: verified during planning against the worktree — `delivery.ts:52`, `resource-tools.ts:605`/`640`/`661-666`, `workflow-tools.ts:299`/`308`/`685`; all anchors present, no extra touch point surfaced | Validated |
| PL-2 | Planning | Dependency Assumptions | L | C12 (Task 4) is independent of the C2 wire-ins (Tasks 2-3) and can land in any relative order — because it uses its own `workflow_bundle:<hash>` channel and only the pre-existing `contentHash`/`deliveredHash`/`recordDeliveries` primitives, not the new `dedupTechniqueBlocks` helper. | Code: `workflow-tools.ts:299-308` (get_workflow ops-bundle path) shares no state with the eager-bundle path at `:685`; channel isolation confirmed in `delivery.ts` header | Validated |
| PL-3 | Planning | Test Strategy | L | Extending `tests/reference-delivery.test.ts` (not a new test file) is the correct home for all new cases, and no snapshot-suite update is needed — because that file already owns persistent-mode/escape/on-disk-ledger cases with the `isUnchangedMarker`/`splitActivityResponse` helpers, and markerization is delivery-time (invisible to composition). | Code: `tests/reference-delivery.test.ts` (716 lines) holds the marker helpers; `binding-fidelity`/`binding-provenance` snapshots assert composed structure, not delivered bytes | Validated |
| PL-4 | Planning | Scope Decisions | L | Docs ride the single impl PR (not a separate docs PR) and the empirical re-walk / eager-budget accounting stay out of scope — because spec §5 lists docs as riding the impl PR, §7 marks the re-walk a validation note, and comprehension Q6 marks budget accounting a later optimisation. | Code: spec §5/§7 + comprehension Q6 authoritative; no code dependency forces earlier | Validated |
| IM-1 | Implement | Implementation Detail | L | The `get_activity` eager-bundle block dedup must be gated on `referenceMode` (not run unconditionally) so full/disposable-worker deliveries stay byte-identical — because the surrounding whole-technique dedup on that path is already `referenceMode`-gated, and un-gated block markering would perturb fresh-mode payloads and the snapshot suites. | Code: `workflow-tools.ts:685` block dedup wrapped in `referenceMode ? dedup : projectTechnique(...)`; the "get_activity default mode is unchanged" and cross-activity full-mode tests stay green (29/29 reference-delivery) | Validated |
| IM-2 | Implement | Implementation Detail | L | Block dedup on the `get_technique` path runs only on the full-delivery branch under `contextMode === 'persistent' && full !== true`, and the whole-technique hash stays on the PRE-marker projected text — because the whole-marker branch (an identical-whole-technique refetch) must still fire on its own hash, and only the not-yet-seen case reaches block dedup. | Code: `resource-tools.ts` — `ordered = projectTechnique(...)`, whole-hash on `projectTechniqueToYaml(...)` (pre-marker, line 605 unchanged), block dedup only in the full-delivery branch; delta-mode + block-dedup tests both green | Validated |
| IM-3 | Implement | Architecture | L | C12 folds `recordDeliveries` into `get_workflow`'s previously mutator-less `advanceSession` (single mutator, no second advance/save) and keeps the ops bundle built+hashed before the advance — because a double-advance would bump seq twice and a post-advance build would miss the ledger commit window. | Code: `workflow-tools.ts:304-332` — `opsText` built, hashed, marker decided, then one `advanceSession(state, draft => recordDeliveries(...))`; C12 full-then-marker test (case e) passes across two calls | Validated |
| IM-4 | Implement | Testing | M | The new C2 tests must discover step IDs on a THROWAWAY session and fetch techniques via `get_technique` WITHOUT calling `get_activity` on the test session — because eager bundling records whole-technique ledger keys in every mode, which would pre-collapse the second technique to a whole-marker and defeat the block-marker case being tested. | Code: `findTwoTechniqueStepIds` runs a probe session; C2 tests call `enterActivity` + `get_technique` only; first attempt (get_activity on test session) failed the assertions, refactor made all 4 C2 cases pass | Validated (empirically — the failing-then-passing refactor is the evidence) |
| IM-5 | Implement | Process | L | The six tightly-coupled tasks land as ONE signed commit rather than six per-task commits — because the plan specifies a single PR #223, the helper and its consumers are one logical change, and per-task commits would fragment a leaf-plus-callers unit. | Within delegated implementation authority; plan states single PR | Confirmed (author decision) |
| IM-6 | Implement | Operational | L | The code commit did not carry a `Co-Authored-By` trailer for the assistant (the headless worker context did not auto-inject it, unlike interactive Claude Code) — surfaced because the PR byline should show both authors. Not code-resolvable (depends on harness commit-time behaviour). | User (checkpoint `implementation-assumption-interview`, option `resolve-inline`): amend the commit to add the trailer. Applied — commit amended `b337a692` → `1c2d379f`, now carrying `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` alongside the DCO `Signed-off-by` and a valid GPG signature. | Resolved (Corrected: amended to add Co-Authored-By trailer) |

## Wrap-Up

12 assumptions — DP-3/IM-5 confirmed; DP-1/DP-2, PL-1..PL-4, and IM-1..IM-4 validated against the worktree and the green test suite; IM-6 resolved by the user at the interview checkpoint (commit amended to add the Co-Authored-By trailer, `b337a692` → `1c2d379f`). No open or deferred assumptions remain. Takeaway: every implementation-detail assumption was code-resolvable and validated (reference-delivery 29/29, typecheck + build clean, `projectTechnique` untouched per gitnexus detect-changes); the one operational item (assistant attribution trailer) was resolved inline.
