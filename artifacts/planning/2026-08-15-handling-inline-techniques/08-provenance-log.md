# Provenance Log

| Task ID | Assistant | Model | Prompt Class | Context Scope | Description |
|---|---|---|---|---|---|
| T0 | claude | claude-opus-5[1m] | mixed | mixed | Merged `origin/main` forward into the feature branch at `e5b12b48` and provisioned the `workflows` corpus submodule at `7f37a2bd`, clearing both plan blockers |
| T1 | claude | claude-opus-5[1m] | mixed | mixed | Added `src/utils/reference-grammar.ts` publishing the ten counting terms with a shape-keyed link classifier and call-site extractor, delegated `src/utils/resource-ref.ts` claiming to it, and added `tests/reference-grammar.test.ts` |
| T2 | claude | claude-opus-5[1m] | mixed | mixed | Pointed all three ancestor-resolution sites in `src/loaders/technique-loader.ts` at the callee's source workflow, with cross-workflow and both-doors parity cases in `tests/technique-loader.test.ts` |
| T4 | claude | claude-opus-5[1m] | refactoring | repo-only | Resolved all three Protocol-section intra-group calls in `workflows/meta/techniques/atlassian-operations/` into their callers, taking the group from 3 logical call sites to 0 and leaving the seven rule-borne references in `TECHNIQUE.md` intact |
| T5 | claude | claude-opus-5[1m] | docs | repo-only | Cited `version-control.infrastructure-submodule-paths` by dotted address at both sites that addressed it as a qualified operation, taking residual container-targeting call sites from 2 to 0, and corrected the `create-pr` path in `workflows/prism-update/techniques/submit-update.md` to resolve inside the corpus root |
| T6 | claude | claude-opus-5[1m] | mixed | mixed | Widened the published verb list to `apply`, `via`, `use`, `follow` and `per` per the `guard-verb-list-width` gate; added `scripts/check-inline-references.ts`, which enumerates every inline call site under the published grammar, fails the two unambiguous classes, reports the argument bins and value-named callees as a disposition worklist, and prints the coverage figure beside every total; registered it as the thirtieth guard with a `check:inline-refs` script; and added `measureOrExit` so the nine corpus guards that resolved their root permissively now reach exit 2 on both an absent and a hollow corpus, taking corpus guards unable to report unmeasured from 9 to 0 |
| T7 | claude | claude-opus-5[1m] | mixed | mixed | Added `tests/fixtures/reference-grammar/` with one fixture corpus per published term, each read by the guard itself and built so the count under the published answer differs from the alternative reading, and `tests/reference-grammar-conformance.test.ts` asserting the totals at corpus pin `12400e85` — 198 logical call sites, 178 deduplicated pairs, 351 raw link occurrences, 78% coverage, zero unresolved targets — with the `container-target` and `counting-unit` overlap asserted as a measured property and the `rawLinkOccurrences` census figure added so the counting-unit term has an assertable alternative |
| T8 | claude | claude-opus-5[1m] | mixed | mixed | Added `src/loaders/reference-traversal.ts`, a depth-first walk carrying a visited set that delivers each referenced body once and continues past a revisit, resolving every call site against the file it was authored in and delivering a qualified pair's operation rather than its container; measured across the corpus at 572 roots, 230 closure members, 110 revisit edges of which 9 are self-loops and 0 unresolved, with the live `verify-index`/`analyze` cycle plus self-loop bound as a named fixture; and fixed a cross-workflow prefix taking a place in the ancestor path, which had the step-bound door deliver 10 rule names where the bundle door delivered 22 for one reference, the twelve dropped being the whole `workflow-engine` container contract |
| T9 | claude | claude-opus-5[1m] | mixed | mixed | Gave rules the own-versus-inherited partition inputs and outputs already take, emitting `inherited_rules` as a block attributing each entry to the contract imposing it; made the decoration pass take a delivery site rather than a step id so a folded call site can state the extent its callee's obligations bind over, scoped to the call and joining no container tree; and keyed delivery on an operation's canonical identity rather than the spelling that reached it, so a folded callee and a step-bound delivery of one operation collapse to one body — measured on the heaviest closure at 15 members, 37,668 characters of operation bodies against 146,769 for the full projection, whose 97,080 of rules text carries once as 28,801 |
| T10 | claude | claude-opus-5[1m] | mixed | mixed | Attached folded bodies at the activity and orchestrator doors, charged to the operations bundle, computing the closure over each door's whole delivered set so a body the door already carries is never folded in beside a copy of itself; documented the orchestrator door as collapsing only as a whole bundle for want of an agent identity; corrected the door-and-counter table in `docs/resource-resolution-model.md` to the twenty-entry orchestrator list; and raised bootstrap-time fixed content to a stated 145,000 characters, the orchestrator door's folded block measuring 33,055 characters over 4 bodies none of which the twenty-entry list named. Completed at the step-bound door, which takes a `context_tokens` budget input and answers in a multi-body envelope carrying the requested technique under `technique:` and the closure beside it under the same `folded_*` keys the bundle doors emit; declaring the window is what asks for the bodies and its absence is stated rather than inferred, and a body past the bound is named in `folded_deferred` with its size. It is the only door taking a bound, the two bundle doors keeping the cannot-drop-content property PL-2's like-for-like retirement rests on. Measured at pin `12400e85` on the basis of summed delivery-event characters: 97 techniques carry a non-empty step-door closure over 1,451,224 characters, the heaviest `workflow-engine::workflow-orchestrator` at 15 bodies over 35 edges and 157,140 characters against its own 13,864 — which does not fit a 50,000-token window and fits from 100,000 up, so the deferral report answers a live case. That closure is cross-derived rather than measured twice: the walk reports 15 bodies and 35 edges where an independent grammar scan of the same files reports 35 resolvable call sites and 15 distinct targets, 0 unresolved on both readings |
| T11 | claude | claude-opus-5[1m] | refactoring | mixed | Retired the core-operations entries standing in for inline references, verified for simultaneous removal rather than one at a time: the orchestrator list goes from 20 entries to 12 and the worker list from 8 to 5, each still delivering every body it delivered before at 21 and 6, with bootstrap-time fixed content falling from 137,916 characters to 133,440 across the same delivered set; residue is reported with the reason folding does not address it, the four harness adapters staying because `resolve-harness-operation` names its callee by a value no link-keyed traversal reaches |
| SC-6 | claude | claude-opus-5[1m] | mixed | mixed | Moved SC-6's enumeration half to the activity layer per the `rescope-to-activity-layer` gate, counting value-named callees on two published bases at corpus pin `12400e85` — the link-keyed 0, a fact about templated link destinations, beside an activity-layer 12 made of 8 callees supplied as a step-binding input value and 4 drawn from the harness kind-to-file table; neither basis stands in for the other and no verb-list or link-grammar width reaches the gap, both being addressed in a different plane from a markdown link. `check-harness-adapter-set` exports its map-row parse so one reading of that table serves both guards, and the criterion's closure half stays as it was |
| T12 | claude | claude-opus-5[1m] | mixed | repo-only | Took the reachable-text scan in `scripts/check-stealth-isolation.ts` onto the closure each reachable step delivers rather than that step's bound technique alone, naming the member a hit sits in and the step that reaches it, and resolving closures once per operation so 69 distinct closures serve the whole scan; and took its scope from a hard-coded workflow id to every workflow the corpus shows seeding `stealth_mode: true`, so a stealth workflow added later is covered without an edit. The widening is a cross-implementation verdict rather than a self-check: 29 techniques in `remediate-vuln` are reachable only by inline call and never as a bound step, so the old scan could not read them, and injecting a `gh issue comment` into one of them — `gitnexus-operations::impact`, reached from five distinct steps — has the pre-change guard report OK where this one reports 5 findings naming each path, two implementations returning opposite verdicts on one corpus. Scanning all 16 workflows was measured and rejected rather than assumed unworkable: it fires 11 times, every hit a sanctioned `gh api -X PATCH` REST call, so the public-write catalog is a stealth catalog and the stealth set is the scope that makes it mean what it says. Gave `scripts/run-token-benchmark.ts` a named-scenario mechanism carrying a `referenced-technique` scenario that has every step-bound fetch declare a window, with `scenario` joining context mode and workflow as a dimension a comparison must match so a cross-scenario delta can never pass the gate. Measured against `solo` on one corpus: total delivery 1,446,744 to 1,524,585 characters and the step door 160,416 to 238,257, the 77,841 difference being 48.5% of that door and 5.38% of the run, over 24 folded bodies across 11 of 23 fetches with 0 deferred at a 200,000-token window. The benchmark isolates its own delta — the other three doors are byte-identical across the two scenarios, which attributes the whole difference to the step door rather than leaving it inferred — and its 0 deferrals agrees with a reading taken the other way, the static sweep putting the heaviest closure at 157,140 characters and fitting from 100,000 tokens up |
| RUN-1 | claude | claude-opus-5[1m] | docs | repo-only | Binding gaps in this run surface at a rate an execution path reveals and a corpus scan does not: every one was met while executing a step and none was found by scanning for it. Past eleven across the run, the implement dispatch adding five — `cargo-operations::test` bound with a `-p {current_task.crate}` scope against a TypeScript project whose plan tasks carry no `crate` field, and `test_filter`, `activity_name`, `issue_key`, `files` and `branch` delivered UNRESOLVED — met by running vitest and the guard suite instead. The rate is the evidence rather than the list, and it is first-hand corroboration of the activity-layer contract gap measured at 82 of 748 bind sites |
| RUN-2 | claude | claude-opus-5[1m] | docs | repo-only | The commit message for `470eeea` records SC-10 as retiring 2 of 11 baseline entries where the measurement is 0 of 11. The history stands as written under the no-amend rule, so anything citing that commit cites the artifact figure and notes the message is superseded |
| RUN-3 | claude | claude-opus-5[1m] | docs | repo-only | The server's `batch:` block is intermittent rather than absent. Every activity before assumptions-review reported none, each worker correctly defaulting to taking no further activity rather than assuming room; it was present on the assumptions-review delivery and again on this implement delivery, reporting 1 of at most 3 activities, 90,519 delivered characters against a 1,400,000 budget and `may_continue: true`. Three sightings, all late in the run, which makes intermittency the finding rather than the suspected server defect the earlier notes recorded |
