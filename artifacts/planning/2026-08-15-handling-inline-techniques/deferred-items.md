# Deferred Items

> Handling Inline Techniques · [#397](https://github.com/m2ux/workflow-server/issues/397) · updated 2026-08-22

| ID | Deferred at | Item | Reason | Follow-up |
|----|-------------|------|--------|-----------|
| D-1 | Requirements elicitation (`stakeholder-transcript`) | Borrowed activities are only ever checked against the workflow that authored them, leaving 143 bind sites unverified in the scope they actually run in | Independent of inline calls and large enough to distort this package's scope | [#491](https://github.com/m2ux/workflow-server/issues/491) — carries the measured finding, written to the repo's plain-language mandate |
| D-2 | Requirements elicitation (re-entered pass, routing) | An activity the run can return to has no way back out. Requirements elicitation chooses its exit by reading a flag that says whether this package's path includes a research activity, and that flag is set once, when the path is first chosen, and cleared by nothing afterwards. So on a second visit the activity routes to research again however long ago research finished, and the exit that skips it is unreachable because no step can clear what it tests. The invariant: an activity that can be re-entered needs an exit that does not depend on a flag no step can clear | A defect in the workflow definition rather than in the inline-technique work, and the second such defect this package has surfaced without going looking for one, after D-1 | [#491](https://github.com/m2ux/workflow-server/issues/491) — written to the repo's plain-language mandate. This run's routing was settled at the meta gate `reentered-elicitation-routing`, which records the decision and leaves the definition as it stands |
| D-3 | Plan preparation (`PL-10`) | The branch-as-step guard has a script and a test but no registry entry, so the suite never runs it and it executes only from its own test file | An incidental defect unrelated to inline references, following the precedent D-1 and D-2 set that such a finding routes to this register rather than absorbing scope | [#491](https://github.com/m2ux/workflow-server/issues/491) |
| D-4 | Assumptions review (register investigation) | A deferred row ends with a column for the issue link that carries it forward, and nothing in any workflow raises one. The register has no owning activity by design, since any activity may be the first to defer something, and its only consumer is the close-out document, which reports how many rows are open. So a deferral is recorded faithfully and goes nowhere | The gap that makes the other rows inert, found by reaching for the filing step D-1 and D-2 were deferred against and finding it absent. Naming it here puts the register's own defect under the same discipline as the items it holds | [#491](https://github.com/m2ux/workflow-server/issues/491) |
| D-5 | Implementation (corpus stage, Atlassian consumer survey) | The Atlassian group's `transitions-are-dynamic` rule requires reading an issue's available transitions before transitioning it, and the only live consumer of the transition operation reads none. The same binding omits the operation's required `status_transition` input, so it is simultaneously one of the omitted-required-input call sites this package counts | Found while establishing the consumer set for the conversion, in an operation the conversion reaches but a defect the conversion does not cause. Repairing a caller's binding is outside the corpus stage's scope, which retires wrapper prose rather than re-authoring step bindings | [#491](https://github.com/m2ux/workflow-server/issues/491) |
| D-6 | Implementation (corpus stage, Atlassian consumer survey) | `resolve-cloud-id-once` is the Atlassian container's first rule and it points at an operation nothing reaches. The caller that needs a cloud id instead asserts one is present and instructs the agent in prose to call the raw tool directly, so the rule names a path no consumer takes while the path consumers do take is the one the sibling GitNexus group's `must-use-operations` rule exists to forbid | A rule made inert by its consumers rather than by its own wording, and the second Atlassian finding from the same survey. It bears on the conversion's premise — a caller already reaching a raw tool in prose is the target state the conversion argues for, arrived at by drift rather than by decision — so it is recorded where that argument can cite it | [#491](https://github.com/m2ux/workflow-server/issues/491) |
| D-7 | Implementation (corpus stage, `gitnexus-conversion-acceptance-unsatisfiable` gate) | Replacing the GitNexus wrapper operations with direct tool calls — the sixth deliverable, withdrawn before any file was edited. Its own acceptance clause and the group container's `must-use-operations` rule cannot both hold: the rule forbids the target state in as many words, with three cross-workflow citations. The premise also failed measurement four times, each correction shrinking it — 41 sites, then a clean-five figure counted over a different population, then zero of eleven retiring whole, then 2 of 23 sites | A conflict between two published clauses rather than an implementation choice, so the shape routes back to a requirements decision rather than being settled in flight. SC-13 stays explicitly open rather than partially delivered, which keeps the guard stage's baseline honest. The halves-become-container-rules design is carried in the plan as available-and-not-taken with its cost — roughly 49 to 75 consumer sites and `must-use-operations` rewritten — since it may be the right architecture found late | [#492](https://github.com/m2ux/workflow-server/issues/492) |
