# Assumptions Log

> Handling Inline Techniques · [#397](https://github.com/m2ux/workflow-server/issues/397) · updated 2026-08-15

## Log

One row per assumption, updated in place.

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | The live record for this work is epic #397's inline-call items rather than the framing of #394 — #394 closed as not planned on 2026-08-02 and consolidated into the epic as W2 and W3, with its body captured verbatim so nothing was lost in the move | Code: `2026-08-02-protocol-structure-consolidation/README.md` consolidation table | Validated |
| DP-2 | Design Philosophy | Problem Interpretation | H | The problem is a platform-integrity defect — references unresolved, unchecked and undelivered — rather than a request for a composition capability the platform lacks. Reading it the other way would classify it inventive and set the bar at "folds work"; reading it as a defect sets the bar at "every edge is resolved-and-checked or scheduled", which is what the observed failures demand | Code: `03-corpus-survey.md`; live delivery failures on `update-pr::verify-body`, `dispatch-activity`'s three references, and `review-assumptions::reconcile` | Validated |
| DP-3 | Design Philosophy | Complexity Assessment | M | Complexity is assessed against the epic's full breadth (W0, W1, W2, W3a, dormant W3b, W4, migration batches) rather than the slice this package finally scopes — a W2-only package is a guard plus a shared grammar module with no loader change, which would read as moderate | User (checkpoint `classification-and-path-confirmed`) | Confirmed: recorded as complex via `full-workflow`. Coupled to [DP-7](#dp-7-how-much-of-the-epic-this-package-delivers) — narrowing scope at elicitation reopens this assessment, since complex scopes the full design framework at plan-prepare |
| DP-4 | Design Philosophy | Complexity Assessment | L | The call-graph impact on `resolveTechniques` — 3 direct callers, 11 execution flows, 4 modules, critical — stands as an objective change-risk signal for the composition path. The same analysis returns zero upstream callers for `composeTechnique`, which is the step-bound delivery door and demonstrably reachable, so the graph under-resolves at least one call path | Code: upstream impact queries on `resolveTechniques` and `composeTechnique` | Partially Validated: directionally sound, and a floor rather than a measure |
| DP-5 | Design Philosophy | Workflow Path | M | Research is scoped as corroboration over ground the doctrine record settled across seven review rounds, not re-derivation of it, with a nil finding reported plainly rather than padded. The scope widens only if requirements elicitation reopens the doctrine | User (checkpoint `classification-and-path-confirmed`) | Confirmed: `full-workflow` selected, research scoped as corroboration |
| DP-6 | Design Philosophy | Workflow Path | M | Classification proceeds with the visibility rule in view and does not re-litigate the hoist-versus-inline-mechanism choice; that question belongs to requirements elicitation, which the chosen path makes live | User (checkpoint `classification-and-path-confirmed`) | Confirmed: `needs_elicitation` true |
| DP-7 | Design Philosophy | Problem Interpretation | H | The success criteria written into the philosophy document are the epic's criteria, which assumes this package carries the arc through to canon amendment. A narrower delivery leaves the canon-versus-specification contradiction standing with a checker now pointing at it | User (stakeholder transcript, `stakeholder-transcript`) | Confirmed: the full arc is this package's delivery, so the epic's criteria are this package's criteria — restated in [03-requirements-elicitation.md](03-requirements-elicitation.md#success-criteria), where two of them are corrected. The arc is four deliverables rather than three; the added one is tracked at [RE-2](#re-2-whether-a-wait-agreed-is-a-build-agreed) |
| DP-8 | Design Philosophy | Problem Interpretation | M | The 2026-08-02 survey figures describe the corpus as it stands. They anchor every scale claim in the classification, and the epic's "why now is cheap" argument rests on their freshness | Code: re-count 2026-08-15 — 571 technique `.md` files under `workflows/*/techniques`, against 554 surveyed; 137 raw `Apply`-link occurrences across 88 files | Partially Validated: the order holds, the exactness does not — the corpus grew 3.1% in thirteen days, so the edge inventory is re-run before disposition rather than carried forward |
| RE-1 | Requirements Elicitation | Requirement Interpretation | H | Choosing the full arc puts W0 into this package's own delivery rather than a wait on someone else's — read the other way, "blocked on W0" would describe an external dependency and cost this package nothing | Code: #405 closed on filing as subsumed into #397; no open issue, branch or pull request delivers W0; the epic's tracking row for it is unchecked and its stated convention is one pull request per work item; W3a's text requires W0 first | Validated: nothing external is in flight, so the full arc means this package delivers W0 |
| RE-2 | Requirements Elicitation | Scope Boundaries | H | Accepting a dependency wait extends to accepting W0 as this package's fourth deliverable — the stakeholder agreed to wait, which is not the same as agreeing to build | User (checkpoint `elicitation-complete`) | Confirmed: W0 accepted into scope, the package carrying four deliverables. Taken knowingly against the epic's one-pull-request-per-work-item convention; if planning finds four work items in one pull request untenable for review, that is a planning finding for its own gate rather than licence to re-cut scope |
| RE-3 | Requirements Elicitation | Success Criteria Interpretation | H | The epic's W2 criterion "the guard reproduces the 118-edge inventory" is testable as written | Code: re-derivation at corpus `34cd5429` — no verb list reproduces 118; holding every other stated criterion fixed, the conservative edge count runs 148 to 235 deduplicated caller-callee pairs across five verb lists, a 59% spread under wordings identical in prose | Invalidated: the target number reproduces under no definition, and the definition does not fix its own terms |
| RE-4 | Requirements Elicitation | Success Criteria Interpretation | M | An acceptance criterion keyed to the guard's own normative grammar plus an asserted total serves the stakeholder's intent better than one keyed to a historical count | User (checkpoint `elicitation-complete`) | Confirmed: [SC-3](03-requirements-elicitation.md#success-criteria) replaces the epic's 118-edge reproduction criterion — reproducible by construction rather than by agreement |
| RE-5 | Requirements Elicitation | Requirement Interpretation | M | Execute-with-correction amends exactly one of the doctrine record's twenty-five decisions — the cycle rule — and leaves the other twenty-four standing | User (stakeholder transcript) | Confirmed: the visibility rule is kept and the fail-the-load rule for reference cycles is replaced by deliver-each-body-once |
| RE-6 | Requirements Elicitation | Scope Boundaries | L | The borrowed-activity ticket is filed through the follow-ups activity rather than opened out of band from elicitation | User (stakeholder transcript) | Confirmed: out of scope, ticket routed through follow-ups — [D-1](deferred-items.md) |
| RE-7 | Requirements Elicitation | Implicit Requirements | M | Retiring the core-operations workarounds is complete only when the delivery path serves every baseline entry those comments attribute to inline-reference non-delivery; an entry still unserved has to stay | Code: `src/loaders/core-ops.ts` — 11 of the 28 orchestrator-baseline entries carry that attribution in their comments (compose-prompt, the two version-control commit operations, sync-progress-status, spawn-agent, continue-agent, resolve-harness-operation, and the four harness adapter files). The worker baseline's entries are role delivery, a different gap | Validated: the retirement is per-entry against a countable list, recorded as [SC-10](03-requirements-elicitation.md#success-criteria) |
| RE-8 | Requirements Elicitation | Implicit Requirements | M | W0's own design choice — whether a borrowed technique carries its home containers or the executing workflow's — is settled by the full-arc choice rather than left open for W0 to decide | Code: the #405 capture offers two coherent end states; the epic's W3a text composes "under the home-ancestry rule", and the fold doctrine assumes a folded callee carries its home group's rules | Validated: home-tree ancestry, chosen by implication and now recorded explicitly |
| RE-9 | Requirements Elicitation | Implicit Requirements | M | The shared slug computation tracked as #398 W1 is a blocking dependency for this package's reference scan | Code: #398 is open with its W1 unchecked and no shared slugger module in the tree; both epic bodies scope #397 W2 to the reference kinds carrying *no* anchor and keep anchor semantics in #398 | Invalidated: not blocking. What it generates is a negative requirement — the grammar module built here must not grow an anchor slugger beside #398's — recorded as [SC-2](03-requirements-elicitation.md#success-criteria) |
| RE-10 | Requirements Elicitation | Scope Boundaries | L | Migration batches stay out: the guard's mechanical bins seed the disposition worklist, and no edge is dispositioned in this package | User (stakeholder transcript — the full-arc option excludes them by its own definition) | Confirmed |
| RE-11 | Requirements Elicitation | Success Criteria Interpretation | M | The epic's "554 files load with unchanged meaning" criterion is restated against the corpus commit this package delivers at, rather than carried as a literal file count | Code: 571 technique `.md` files under `workflows/*/techniques` at corpus `34cd5429`, against the 554 the criterion names | Validated: the literal count is already stale by 17 files, so the criterion is keyed to the delivered submodule commit — [SC-12](03-requirements-elicitation.md#success-criteria) |
| RE-12 | Requirements Elicitation | Requirement Interpretation | M | The "activation staged behind a server flag so the corpus never half-folds" reassurance recorded against this package's scope covers W3a | Code: epic #397 body — flag-gated activation is stated for W3b, which is dormant and evidence-gated; W3a carries no flag and states that call sites keep prose semantics | Invalidated: the reassurance describes W3b. W3a needs no flag because it is additive delivery; the half-fold risk belongs to the migration batches, which are out of scope |
| RE-13 | Requirements Elicitation | Requirement Interpretation | M | Correcting the cycle rule touches the doctrine record only, the epic body being a summary that does not itself state the rule | Code: epic #397 body — W3a's own description states bodies are "followed transitively with reference cycles failing the load", and its acceptance criteria carry the same assumption | Invalidated: the epic body and its acceptance criteria are amended alongside the doctrine record |
| RE-14 | Requirements Elicitation | Requirement Interpretation | H | A folded callee is keyed in the delivery ledger as a technique, collapsing it against a step-bound delivery of the same operation, rather than as a block inside its caller | User (checkpoint `elicitation-complete`) | Confirmed: technique-keyed, with the call site's binding annotations carried as a separate block so the collapse across the 31 dual-reached techniques survives — the existing split of inherited blocks from their parent technique being the precedent |

Resolution: how it was settled — `Code:` with evidence, `User` (checkpoint or interview), or `—` while open. Outcome: Validated / Invalidated / Partially Validated (code-resolved) · Confirmed / Corrected: &lt;change&gt; / Deferred: &lt;follow-up&gt; (user-resolved) · Open (&lt;reason&gt;).
