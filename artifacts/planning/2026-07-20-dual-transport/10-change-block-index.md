# Change Block Index

> Dual Transport Support · issue skipped · updated 2026-07-20

Manual diff review of `feat/dual-transport-support` vs `main` (11 files, +1008/-11; commits `1a4821fb`, `24a596ed`, `407fa541`, `767f531a`). One row per logical change block, in commit order.

| # | Block | Rationale |
|---|-------|-----------|
| 1 | `src/config.ts:31-50, 135-231, 242-249` — `ServerConfig.transport/port/host`, `Transport` type, `parseTransportFlag`/`resolveTransport`, `parsePortFlag`/`resolvePort`, `parseHostFlag`/`resolveHost` | Extends the existing `--workspace` CLI/env-precedence pattern (`parseWorkspaceFlag`/`resolveWorkspaceDir`) to three new flags, per Task 1. Fields are optional on the interface (not required) because 7 pre-existing files construct `ServerConfig` literals without them (IT-1); `loadConfig` still always populates them for real CLI startup. |
| 2 | `src/index.ts:1-44` — reduced `main()` to `loadConfig` + a `switch (config.transport)` dispatch to `startStdioServer`/`startHttpServer` | Task 2: `index.ts` becomes a thin CLI router; the barrel exports are unchanged (plus the new `Transport` type export). No behavior change for stdio users — `transport` defaults to `'stdio'`. |
| 3 | `src/transports/stdio.ts:1-15` — `startStdioServer(config)` | The original `main()` body (create server, connect `StdioServerTransport`, log ready), moved verbatim into its own module for symmetry with `http.ts` (PL-1). |
| 4 | `src/transports/http.ts:1-60` — `createHttpApp(config)`: express app via the SDK's `createMcpExpressApp`, request-id + logging middleware wired in, health/ready + MCP routes registered, catch-all 404 → shared error handler | Task 2/3: the app-building logic is split from `startHttpServer`'s `listen()` call specifically so tests can drive routes via `supertest` without binding a real socket. |
| 5 | `src/transports/http.ts:62-77` — `startHttpServer(config)`: binds `app.listen(port, host)`, logs ready, installs graceful shutdown | Task 2/4: the actual process entry point for HTTP mode; returns the `http.Server` handle (mainly for tests). |
| 6 | `src/transports/http.ts:79-95` — `registerHealthRoutes`: `/health` (liveness, no dependency checks) and `/ready` (readiness: `existsSync` on `workflowDir`/`schemasDir`/`workspaceDir`) | Task 3 deliverable. Liveness/readiness kept deliberately distinct — a liveness probe should only fail when the process itself is wedged, not when a dependency is briefly unavailable. |
| 7 | `src/transports/http.ts:97-152` — `registerMcpRoute`: per-session `Map<sessionId, StreamableHTTPServerTransport>`, `POST`/`GET`/`DELETE /mcp` all routed through one handler that creates a new `McpServer`+transport pair on a valid `initialize` request and reuses the stored transport by `mcp-session-id` otherwise | Task 2/3, following the MCP SDK's own documented session-management pattern (`examples/server/simpleStreamableHttp.js`) rather than inventing one. `StreamableHTTPServerTransport` chosen over the plan's `SSEServerTransport` sample per DP-4 (current SDK's recommended class; the SSE-only transport's DNS-rebinding options are `@deprecated`). No `EventStore` (resumability) — recorded as deferred item D-2 (IT-3), acceptable for this work package's single-process, trusted-network scope. |
| 8 | `src/transports/http.ts:154-190` — `shutdownHandler`/`installGracefulShutdown`: closes the `http.Server` on `SIGTERM`/`SIGINT`, injectable `exit` callback, bounded force-exit timeout | Task 4. `shutdownHandler` is exported standalone (and takes an injectable `exit`) specifically so it's directly unit-testable without dispatching a real OS signal or letting a test call the real `process.exit`. |
| 9 | `src/middleware/request-id.ts` — `requestId` middleware + `express-serve-static-core` module augmentation for `req.requestId` | Task 3 deliverable: generates/propagates `x-request-id`. |
| 10 | `src/middleware/logging.ts` — `requestLogging` middleware | Task 3 deliverable: one `logInfo` line per completed request (method/path/status/durationMs/requestId), reusing the existing stderr-JSON logger (CC-2) rather than `console.log`. |
| 11 | `src/middleware/error-handler.ts` — `errorHandler` (4-arg Express error middleware) | Task 3 deliverable: the shared `{ error, message, requestId, timestamp }` JSON error body, logged via `logError`. Used both for thrown/`next(err)` errors and the catch-all 404. |
| 12 | `package.json`/`package-lock.json` — `express@^5.2.1` + `@types/express` (runtime/dev); `supertest`/`@types/supertest` (dev, for block 13's tests); `dev:http`/`start:http` scripts | Task 5. `express` was already resolved transitively via the MCP SDK's own dependency on it (`^5.0.1`); now pinned as a direct dependency since our code imports it directly. Version choice validated in PL-2. |
| 13 | `tests/config.test.ts:95-170` — 12 new cases: transport default/CLI/env precedence/invalid-value, port/host default/CLI/env precedence | Task 1 test coverage, extending the existing suite in place (PL-4) rather than a new file. |
| 14 | `tests/http-transport.test.ts` (full file, 160 lines post-simplification) — health/ready, request-id propagation, error shape (unknown route, missing/unknown session), a live `initialize`→`tools/list` round-trip, graceful shutdown (incl. idempotent-close) | Task 3/4 test coverage. One outer `describe('HTTP transport', …)` with a shared `app` fixture, per the lean-coding-audit simplification applied in commit `767f531a`. |

**Provenance attestation:** every block above traces to a specific implementation task in [06-work-package-plan.md](06-work-package-plan.md) and is covered by the per-task rows in [08-provenance-log.md](08-provenance-log.md). No block introduces an undocumented symbol or behavior beyond its stated task.
