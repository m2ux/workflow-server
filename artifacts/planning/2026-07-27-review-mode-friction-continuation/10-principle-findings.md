# Design Principles Findings — `work-package`

**Mode:** update · **Date:** 2026-07-27
**Pass:** principles
**Target:** `work-package` v3.35.4

Scored the committed change surface (`dd1521ba` — 4 files, +22/-20) against each stance in [design-principles](../../../../workflows/workflow-design/resources/design-principles.md). **All 29 principles pass except the five below.** Reported divergences-only per `exception-only-verdict-tables` — an all-green row per principle would carry one bit in 29 rows.

Notably improved by this change: **13 Separate Contract from Procedure** (the fallback resolution recipe moved out of two Inputs descriptions into Protocol § 2), **6 One Authoritative Home** and **29 Cite Resource Policy** (the base URL and its UPPERCASE slots stay resource-resident; `review-summary.md:57` cites `#header-fields` and explicitly disclaims slot ownership), **19 Name Symbols Affirmatively** (`eng_git_dir` / `eng_branch` / `eng_publish_ref` — snake_case, head-noun-last, no direction encoding), and **14 Single Source of Truth** (`repo_root` is the declared workflow variable; no shadow introduced).

## Findings

| ID | Severity | Finding | Location | Fix |
|----|----------|---------|----------|-----|
| F-1 | High | **18 Prefer Shared Capability — violating.** A shared op already owns "commit and push planning artifacts in the engineering checkout": `manage-git::artifact-commits` resolves `{$eng_git_dir}` itself and runs `git -C {eng_git_dir}` for add / commit / push, including the `pull --rebase origin {branch}` that keeps parallel work packages from non-fast-forward rejections. Protocol step 2 instead applies `version-control::commit-regular-files`, whose Capability scopes it to "a regular (non-submodule) directory of the **parent repo**", which declares **no `repo_path` input**, and whose step 4 pushes with `repo_path` = the parent-repo working tree (`.`) and forbids branch creation there. So step 1's `{eng_branch}` (read from the engineering checkout — branch `engineering`) is handed to an op that pushes from the product checkout (on `main`). | `techniques/publish-review-artifacts.md:30` → `meta/techniques/version-control/commit-regular-files.md:8,26,29` | Route the write through the engineering checkout. Cleanest under `pass-orchestration-in-technique` (AP-114) is an activity-13 bind of `manage-git::artifact-commits` rather than a Protocol-level Apply; alternatives are giving `commit-regular-files` a `repo_path` input, or swapping the Apply target in place. **All three exceed the confirmed scope manifest** — see Notes. |
| F-2 | High | **23 Close the Loop — partially compliant.** The re-target is two-thirds done at the operation level: step 1 resolves the branch from the engineering checkout and step 3 reads `rev-parse HEAD` from it, but step 2 — the step that actually *publishes* the artifacts — still addresses the parent repo. The emitted `{artifact_publish_ref}` is therefore a correct ref into a checkout the artifacts were never pushed to, which is precisely the failure [A-2](03-assumptions-log.md) names ("a SHA off the branch the artifacts were never committed to") and which breaks `manage-artifacts.push-before-linking`. | `techniques/publish-review-artifacts.md:29-31` | Same as F-1. The goal statement G-2 ("the engineering checkout is the git target") is met for reads, not for the write. |
| F-3 | Medium | **3 Define Complete Scope Before Execution — partially compliant.** The manifest's drafting-order rationale (`06-scope-manifest.md:74`) commits to an AP-129 sweep "so no restatement of the retired name survives the pass". The sweep was executed for the retired *symbol* — `reference_path`, verified 0 occurrences tree-wide — but not for the retired *behavioural claim* (parent repo as the artifact git target), which is what AP-129's Detect actually specifies ("take the pre-change phrasing as the search key"). The delegated op in step 2 was never enumerated as part of the change surface, which is how F-1 survived to commit. | `06-scope-manifest.md:74` vs `techniques/publish-review-artifacts.md:30` | Sweep the altered behavioural claim, not only the renamed symbol, and enumerate delegated ops as part of the surface when a technique's git target changes. |
| F-4 | Low | **5 Maximize Schema Expressiveness — partially compliant.** Every variable part of the base URL is a slot except the layout-dependent path segment, which is prose. Detail in [expressiveness findings F-1](08-expressiveness-findings.md). | `resources/review-mode.md:39` vs `:42` | Constrained by G-3 / [A-4](03-assumptions-log.md) (slots stay resource-resident) — fix without promoting a new slot. |
| F-5 | Low | **7 Convention Over Invention — partially compliant.** "artifacts checkout" against the tree's established "engineering checkout". Detail in [conformance findings F-1](08-conformance-findings.md). | `resources/review-mode.md:42` | Adopt the established term. |

**Finding count:** 5 (per-principle classifications; 3 distinct defects — see Notes)

## Notes

- **Deduplication.** Rows are per-principle classifications, as this pass requires, so one defect can appear under two stances. F-1 and F-2 are the *same* defect scored against principles 18 and 23. F-4 and F-5 restate defects owned by the [expressiveness](08-expressiveness-findings.md) and [conformance](08-conformance-findings.md) satellites. Distinct defects originating in this pass: **3** — the parent-repo-scoped publish write (High), the incomplete behavioural-claim sweep (Medium), and nothing further. Deduplicated total across all passes: **5**, tabulated in [post-update review](10-post-update-review.md).
- Schema cross-reference (`workflow.schema.json`): the edited `artifact_publish_ref` declaration keeps `name` / `type` / `defaultValue` intact and its `description` is a single line, conforming to `variable-description-one-line`. No `activity.schema.json`, `technique.schema.json`, or `condition.schema.json` surface is touched by this change — no activity, step, checkpoint, transition, or condition was added, removed, or reordered.
- **Why F-1/F-2 are not remediated in place.** Every available fix crosses the boundary the confirmed scope manifest draws. An activity-13 bind is excluded explicitly (`06-scope-manifest.md:25`: "All three bind sites in `activities/13-submit-for-review.yaml` — no edit needed"); adding a `repo_path` input to `commit-regular-files` edits `meta`, a second workflow, the same widening D-10 was deferred for; and swapping the Apply target in place changes the commit-message contract, since `artifact-commits` carries the canonical `docs(work-package): {activity_name} artifacts for {issue_key}` pattern rather than the literal message this op specifies. This is a scope and impact decision with real alternatives, not a mechanical repair — it belongs to a fresh intake/impact/Gate 2 pass, which is where the `needs_audit_fixes` transition routes.
- F-1 is this branch's own defect, not inherited: `techniques/publish-review-artifacts.md` is **new on this branch** (37 insertions, 0 deletions against `origin/workflows`), so the choice of delegated op is the branch's. This commit re-aimed steps 1 and 3 and left step 2 on the original model.
