# Anti-Pattern Catalog Findings — `work-package`

**Mode:** update · **Date:** 2026-07-27
**Pass:** anti-patterns
**Target:** `work-package` v3.35.4

Walked every catalog subsection in [anti-patterns](../../../../workflows/workflow-design/resources/anti-patterns.md) against the committed change surface (`dd1521ba` — 4 files), applying each entry's Detect and honouring its Do-not-flag carve-outs. Tool-Technique-Doc entries were applied against surface evidence: the delegated op's declared Inputs and Protocol were opened and read, not skimmed. One new violation; one cross-referenced classification of a defect owned by the principles satellite.

## Findings

| ID | Severity | Finding | Location | Fix |
|----|----------|---------|----------|-----|
| F-1 | Low | **`io-agnostic-contract`** (AP-42). The Input description names a workflow-internal producer: "a publish commit SHA **from `publish-review-artifacts`**". Detect fires on exactly this shape ("An input/output entry names or links a workflow-internal producer/consumer — another technique"); no carve-out applies, since the slot's value is a git ref, not a technique id, and the naming is neither intrinsic/external origin nor a resource-section link. The producing technique's own Outputs entry (`publish-review-artifacts.md:24`) already states the same meaning without the caller name, so the two contracts for one value disagree in form — and this commit authored both lines. | `techniques/review-summary.md:34` | Drop "from `publish-review-artifacts`": "a publish commit SHA, or a branch name; empty when no publish ref has been produced." |

**Finding count:** 1

## Notes

- **Cross-reference, not a second count.** The High defect at `techniques/publish-review-artifacts.md:30` also classifies here, under **`duplicate-shared-capability`** (AP-110) — a shared op (`manage-git::artifact-commits`) already owns commit-and-push in the engineering checkout, and the local Protocol delegates to a parent-repo-scoped op instead — and under **`pass-orchestration-in-technique`** (AP-114), since the delegation is a Protocol-level `Apply` of a sibling op for work, which AP-114 directs to an activity `steps[]` bind. It is filed once, with its remediation analysis, in [principle findings F-1/F-2](10-principle-findings.md).
- **`stale-restatement-after-change`** (AP-129) — applied, no finding attributable to this change. The retired *symbol* sweep is clean: `reference_path` has **0 occurrences tree-wide**. The retired *behavioural claim* (parent repo as artifact git target) does survive at four sites, but each was verified byte-identical to `origin/workflows` for the offending content, and each already contradicted `techniques/update-pr/render.md:54` and `techniques/manage-git/artifact-commits.md:42` **before this branch existed** — so AP-129's Detect ("a change alters a behavioural claim … flag each *surviving* occurrence") does not attribute them here. Carried as follow-up candidates: `techniques/update-pr/TECHNIQUE.md:76` (`engineering-link-mandatory`), `resources/pr-description.md:143`, `techniques/manage-artifacts/TECHNIQUE.md:70-72` (`committed-to-parent`), `meta/techniques/agent-conduct.md:98`.
- Entries applied and **passing** on this surface, where a plausible exposure existed: `bind-protocol-locals` (AP-62) — `{$eng_git_dir}`, `{$eng_branch}`, `{$eng_publish_ref}` each bound once at the producing step and read bare thereafter, no dead bindings, no unbound reads; the carve-out barring a local over declared I/O is respected, which is why the local is `eng_publish_ref` and not `artifact_publish_ref`. `backtick-code-tokens` (AP-63) — every designator and git command in one span. `snake-case-symbols` (AP-58) and `io-id-shape` (AP-66) — all three locals and the renamed `repo_root` input conform. `brace-declared-ids` (AP-52) — no bare or angle-wrapped declared ids. `escape-literal-dollar` (AP-57) — every `$` sits inside a code span, correctly unescaped. `procedure-in-io-contract` (AP-119) and `contract-not-procedure` (AP-111) — the fallback recipe now lives in Protocol § 2, not in the two Inputs descriptions. `no-technique-resource-dual-home` (AP-102) and `cited-home-owns-claim` (AP-103) — `review-summary.md:57` cites `review-mode.md#header-fields` for the base URL and disclaims slot ownership, and the cited home does own the claim. `variable-description-one-line` (AP-126) — `workflow.yaml:341` is one line. `unproduced-value-read` (AP-128) — the guard now tests non-emptiness, the shape that discriminates a seeded default from a produced value. `bag-value-as-literal` (AP-127) — no hardcoded `main`, branch, or remote survives on the change surface.
- Not applicable to this surface (no such construct added, removed, or reordered): the Interaction family (AP-05…AP-08), the Schema Expressiveness step/checkpoint/loop/decision entries (AP-09…AP-14, AP-17, AP-18, AP-31…AP-39), Rule Hygiene (AP-19…AP-25), and the Output Economy checkpoint-message entries (AP-88, AP-89, AP-97…AP-101).
