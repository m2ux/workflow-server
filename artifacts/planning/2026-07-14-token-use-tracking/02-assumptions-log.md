# Assumptions Log

> Token Use Tracking and Cost Estimation · #232 · updated 2026-07-15

## Log

One row per assumption, updated in place. IDs: two-letter phase prefix + sequence
(DP-1, RE-1, RS-1, IA-1, PL-1) or task number (1.1, 2.3).

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | "Token use" means the LLM tokens the *agent* consumes executing activities/workflows, not the server's own compute — the ticket frames it as work-package cost, and the server has negligible compute cost of its own (ambiguous problem statement) | User (elicitation, 2026-07-15) | Confirmed: tracked cost = the agent's LLM token cost, not server compute |
| DP-2 | Design Philosophy | Problem Interpretation | H | "Use the model's native capabilities" means consuming harness/model-reported usage metadata rather than the server re-tokenizing text itself — the server sees only tool-call payloads, not the agent's full context, so it cannot faithfully re-measure (requirement reading) | User (elicitation, 2026-07-15) | Confirmed: agent-relayed native usage via a declared channel accepted in principle; exact channel left to research ([DI-1](deferred-items.md)) |
| DP-3 | Design Philosophy | Complexity Assessment | L | The server holds no token/usage/cost concept today and observes only tool calls, so a new channel is required to obtain usage — driving the "complex" rating | Code: `src/schema/session.schema.ts`, `src/schema/state.schema.ts` — no usage/token/cost fields; `context_tokens`/`trace_token` are unrelated | Validated |
| DP-4 | Design Philosophy | Problem Interpretation | L | "The appropriate planning artifact" means the per-work-package planning folder (its `README.md` or a dedicated usage/cost artifact), written on completion — consistent with how the workflow records durable per-work-package output (recall) | User (elicitation, 2026-07-15) | Confirmed: both — dedicated `NN-token-usage.md` (per-activity table + per-workflow totals) plus a one-line README summary + link |
| DP-5 | Design Philosophy | Workflow Path | L | The full workflow path (elicitation + research + all optional activities) fits a complex feature with a genuine unknown — proactively confirmed at checkpoint | User (classification-and-path-confirmed checkpoint) | Confirmed |
| RE-1 | Requirements Elicitation | Success Criteria Interpretation | M | The recorded figures are input, output, total, plus cache-read/cache-write when the harness reports them; a figure absent from the harness report is omitted, not fabricated | User (elicitation, 2026-07-15) | Confirmed |
| RE-2 | Requirements Elicitation | Requirement Interpretation | M | Cost is a single total per activity and per workflow (with the input/output/cache breakdown living in the table), frozen at activity time from a config price table stamped with model id + price-table version; unknown/unpriced models record tokens with cost marked `unknown` and never block | User (elicitation, 2026-07-15) | Confirmed |
| RE-3 | Requirements Elicitation | Scope Boundaries | M | v1 captures at per-activity granularity (the `next_activity` transition seam) with per-workflow roll-up including child (`triggeredWorkflows`) sessions; finer per-checkpoint/per-tool-call/dispatch-window attribution is out of v1 scope | User (elicitation, 2026-07-15) | Confirmed; finer attribution deferred ([DI-2](deferred-items.md)) |
| RE-4 | Requirements Elicitation | Implicit Requirements | M | v1 accepts that native usage depends on the agent/harness relaying its figure to the server (no self-measurement) and degrades gracefully — records what is provided, omits when absent, run always completes | User (elicitation, 2026-07-15) | Confirmed |
| RS-1 | Research | Synthesis Decisions | M | The declared `usage` param on `next_activity` is the recommended channel over request `_meta` — explicit, Zod-validated, keeps a missing figure visibly missing, mirrors `context_tokens`. Alternatives: `_meta` via SDK `extra`; Claude Code OTEL ingest (see RS-4, which subsumes the full channel decision) | User (assumptions-review, PL-1) | Confirmed — subsumed by PL-1 channel decision |
| RS-2 | Research | Pattern Applicability | L | The config price table stores only base-input + output per model and derives 5m-write / 1h-write / cache-read via the documented universal multipliers (×1.25, ×2, ×0.1) rather than storing all five rates. Alternative: store all five rates explicitly | Code + docs: config precedent `config.ts:49-50` (env-overridable numeric knobs w/ in-code fallback); multipliers universal across models per [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing.md) | Validated — derive cache rates from base input |
| RS-3 | Research | Risk Assessment | H | Native usage is orchestrator/harness-relayed, NOT worker-self-measured — the agent cannot read its own mid-turn usage (Messages API `usage` is post-hoc; hooks lack usage; `.session-usage.json` is an unshipped feature request). Whichever channel is chosen, a workflow/technique instruction must make the orchestrator the populator | Research-validated (Agent SDK cost-tracking, Claude Code monitoring/hooks docs, MCP spec) — sharpens RE-4/DP-2 | Validated |
| RS-4 | Research | Synthesis Decisions | H | The native-usage channel decision is a three-way (declared param / request `_meta` / Claude Code OTEL ingest) or hybrid; v1 recommendation is the declared param + config price table, with OTEL as an optional future complement — its metrics carry no per-activity identifier (only `session.id`), its cost is an explicit estimate (meaningless on Pro/Max), and ingesting OTLP forces new infra onto a stdio-only MCP server. Alternatives: `_meta` channel; OTEL-primary; hybrid (OTEL-when-present, param-fallback) | User (assumptions-review, PL-1) | Confirmed: declared `usage` param on `next_activity` + config price table; OTEL deferred as optional enrichment ([DI-1](deferred-items.md) resolved) |
| RS-5 | Research | Source Relevance | L | Anthropic pricing captured 2026-07-15 is treated as current/authoritative to seed the price table; the price-table version must handle scheduled changes (Sonnet 5 introductory $2/$10 → standard $3/$15 on 2026-09-01) | Research-validated (Anthropic pricing docs); re-confirm figures at build time | Validated — re-confirm at build time (implementation gate, not stakeholder) |
| IA-1 | Implementation Analysis | Dependency Understanding | H | The server writes NO `.md` artifacts — its only file-write surface is the sealed `session.json`/`.session-token` store; therefore the dedicated `NN-token-usage.md` + README summary must be rendered by a WORKFLOW-CORPUS technique (agent-side), with the server only persisting the structured usage/cost record in `session.json`. Alternative considered: give the server a filesystem-writing role (rejected — new responsibility it has none of today, breaks the server-owns-structured-state / agent-owns-artifacts division). | Code: grep `writeFile`/`.md` in `src/tools/`, `src/loaders/` → 0 hits; only `store.ts` `writeAtomic` writes `session.json`+`.session-token` | Validated — split confirmed; the completion renderer is a second, corpus-side task |
| IA-2 | Implementation Analysis | Dependency Understanding | H | The `next_activity` `usage` param is INERT without a workflow-corpus instruction that makes the ORCHESTRATOR populate it from harness usage (the worker cannot self-measure). So the feature is two changes: (1) server (param + schema + aggregation + cost), (2) corpus (populate instruction + completion renderer). Alternative: server-only (rejected — no usage would ever arrive). | Code (RS-3 confirmed): handlers single-arg, `extra`/request `_meta` unread; Messages API `usage` is post-hoc | Validated — both changes scoped in the analysis |
| IA-3 | Implementation Analysis | Current Behavior | M | Child (`triggeredWorkflows`) usage rolls up by pure in-tree traversal of the recursive embedded `SessionFile` at workflow completion (`sumUsageTree`), rather than an incremental fold at the parent-notification path — the whole tree already lives in one `session.json`, so completion-time traversal needs no extra I/O. Alternative: incremental fold at the parent-notify block (`workflow-tools.ts:463-485`) for pre-completion visibility (carried as a plan-prepare micro-decision) | Code: `session.schema.ts:120,213` (recursive `state`), `resolver.saveSessionForTool` `replacePath` persists the whole tree | Validated — recommend traversal for v1; incremental fold noted as the alternative |
| IA-4 | Implementation Analysis | Baseline Interpretation | L | A new optional `SessionFile.usage` field + `usage_recorded` history event is fully back-compatible: existing `session.json` files parse unchanged (optional-with-absent, per `status`/`contextMode`/`deliveredContent`), and the e2e snapshots are not perturbed (`snapshotWalk` excludes sessionIndex + the variable bag); the only regeneration is the generated JSON schemas via `build:schemas`. Alternative: a required field / migration (rejected — unnecessary and breaking) | Code: `session.schema.ts:113,145,156` (optional precedents); `tests/e2e/snapshot.ts:33-42`; `scripts/generate-schemas.ts:26,28` + `tests/generated-schemas.test.ts` | Validated — additive-only, no migration |
| PL-1 | Planning | Design Approach | H | The native-usage channel is the declared `usage` object param on `next_activity` + config price table (RS-4/DI-1 carried recommendation); OTEL deferred as optional enrichment. Alternatives: request `_meta` via SDK `extra` (strictly worse — unvalidated, silent-when-absent); OTEL-primary (no per-activity identifier, cost estimate meaningless on Pro/Max, forces OTLP infra + harness coupling); server re-tokenization (rejected, out-of-scope #1) | User (assumptions-review interview, 2026-07-15) | Confirmed: declared `usage` param + config price table |
| PL-2 | Planning | Design Approach | L | The renderer reads the rolled-up `SessionFile.usage` field (via `inspect_session`/`get_workflow_status`), not a re-scan of `usage_recorded` events (MD-1) — one read, already child-inclusive; events stay the audit trail. Alternative: re-scan history events | Design decision (analysis rec); code-resolvable at implementation | Validated — rolled-up field is the read path |
| PL-3 | Planning | Design Approach | L | Child (`triggeredWorkflows`) roll-up is completion-time in-tree traversal (`sumUsageTree`) for v1 (MD-2); the whole tree is in one `session.json`, so traversal needs no extra I/O. Alternative: incremental fold at the parent-notification path (`:463-485`) for pre-completion visibility — noted, not built | Code: `session.schema.ts:120,213` (recursive `state`); IA-3 | Validated — traversal for v1 |
| PL-4 | Planning | Task Breakdown | L | `SessionFile.usage` is absent-until-first-use (not seeded in `createInitialSessionFile`, MD-3), matching `deliveredContent`; `total_tokens` is derived `input+output` with a relayed value accepted only as a cross-check (MD-4). Alternatives: seed empty at creation; trust relayed total | Code: `session.schema.ts` (`deliveredContent` absent-until-use precedent) | Validated |
| PL-5 | Planning | Scope Decisions | L | Price-table override is a version-string env override (`PRICE_TABLE_VERSION`) only, the table itself a build-time constant (MD-5); the cost/roll-up helpers live in a dedicated `src/utils/usage.ts` (MD-6) for isolated unit tests. Alternatives: full-table env/file override (over-engineering for v1); helpers co-located in schema/handler | Design decision (RS-5); config precedent `config.ts:49-50` | Validated — minimal override surface |
| PL-6 | Planning | Dependency Assumptions | M | The two corpus changes (orchestrator-populate instruction + completion renderer) are load-bearing and sequenced server-first: the populate instruction targets the Task 6 `usage` param; the renderer reads the Task 2 rolled-up field. Alternative: server-only (rejected — no usage would ever arrive; no human-facing record) | Research/analysis (RS-3/IA-2); Tasks 7-8 scoped as first-class | Validated — both corpus tasks planned |
| AR-1 | Assumptions Review | — | — | No significant assumptions (review gate surfaces pre-existing open items; no new implicit decisions in this activity) | — | — |

## Wrap-Up

21 assumptions logged across six phases — all validated or confirmed. PL-1 (native-usage channel) confirmed at assumptions-review interview: declared `usage` param on `next_activity` + config price table; OTEL deferred. DI-1 resolved. RS-5 re-confirms pricing at build time (implementation gate). Ready for implementation.
