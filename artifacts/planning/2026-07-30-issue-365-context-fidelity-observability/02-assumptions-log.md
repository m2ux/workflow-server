# Assumptions Log

> Context Fidelity and Observability · #365 · updated 2026-07-31

## Log

One row per assumption, updated in place. IDs: two-letter phase prefix + sequence
(DP-1, RE-1, RS-1, IA-1, PL-1) or task number (1.1, 2.3).

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| DP-1 | Design Philosophy | Problem Interpretation | M | The four items share one root cause — the server records claims and magnitudes but never reconciles or aggregates them — and so may admit a shared mechanism at plan time. Rationale: S2 (trust a self-reported manifest), S3 (store rows, never roll up) and S5's resource gap (treat a resolution failure as a skip, not a finding) are the same shape. Alternatives considered: treat them as four fully independent tasks, which the issue's own sequencing note supports ("S2, S4 and S5 are independent and can be taken in any order"). | Code: `src/tools/workflow-tools.ts:467-490, 860-863`, `src/loaders/technique-loader.ts:33-54` | Partially Validated — not a shared *mechanism* (a set-diff, a reduction over events, and an error-channel change are three different operations), but a real shared *seam*: `_meta.validation` / `bundlingWarnings` is already the discrepancy channel all three would report through. Plan for one channel, three mechanisms. |
| DP-2 | Design Philosophy | Problem Interpretation | M | The package is an inventive goal (improvement), not a specific problem, despite S2 having a demonstrated failure in the #141 run and S5's `validateTechniqueFetches` agent-blindness being a live false-negative. Rationale: the absence of a check that was never built reads as incompleteness rather than malfunction. Ambiguity source — requirement reading: the issue is labelled `enhancement` and framed as backlog follow-through, which biases the reading toward improvement. Alternatives considered: classify as specific-cause-known and foreground the two defects; or split the package, classifying S2 plus the S5 validation defect as specific-cause-known and S3 plus S4 as inventive-improvement. | User (checkpoint `classification-and-path-confirmed`) | Confirmed — `revise-classification` was offered and not taken; the two defects stay tracked as findings rather than reclassifying the package |
| DP-3 | Design Philosophy | Complexity Assessment | M | The gitnexus CRITICAL ratings (9 affected execution flows each for `projectUsage` and `dedupTechniqueBlocks`) are evidence of genuine change difficulty. Rationale: high fan-out normally implies a wide verification surface. Counter-signal noted at collection time: the d=1/d=2 edges to `registerWorkflowTools` / `registerResourceTools` carry confidence 0.5, and the counted flows are largely server-bootstrap paths that every tool registration transitively reaches, so the rating may reflect graph shape rather than coupling to these symbols. | Code: gitnexus `byDepth` for both symbols; `src/tools/resource-tools.ts:782-793`, `src/tools/workflow-tools.ts:812-815`, `scripts/run-token-benchmark.ts:56-84` | Partially Validated — real coupling is two callers per symbol, all inside the tool layer, and the shared "9 flows" are bootstrap paths every tool registration reaches, so the raw CRITICAL rating must **not** be quoted as plan evidence. But the coupling it gestures at is real for S4: the benchmark's `Metrics` reads `deliveredContentKeys`, `unchangedTechniqueAnswers` and `history.technique_bundled`, so any change to block coverage moves the frozen baseline and must clear the gate. Complexity `complex` stands on the architectural-decision, cost-gate and requirement-contradiction grounds plus benchmark/oracle coupling. |
| DP-4 | Design Philosophy | Complexity Assessment | M | The issue's "~25% of technique delivery" figure still describes the prize available from S4. Rationale: it is quoted in the issue as the justification for the item. Counter-signal: the figure originates in #189 C2 and predates the block dedup that has since shipped (`DEDUP_BLOCKS`), so the share still recoverable may be far smaller — possibly near zero if the three existing blocks already capture most repeated contract/rules content. | Code: `src/loaders/technique-loader.ts:33-54`, `src/utils/delivery.ts:90-123` | Partially Validated and **reframed**. The share may still be large despite dedup existing, for two structural reasons: (1) `provenance_note` is byte-identical across every technique and is not a member of `DEDUP_BLOCKS`; (2) `dedupTechniqueBlocks` hashes each block whole, so `inherited_inputs` / `inherited_outputs` — which mix an invariant `note` with per-technique `items` — yield a different hash per technique and rarely collapse, whereas `rules` (uniform across siblings) does collapse as designed. Evidence is n=1, this activity's own `get_activity` payload: 7 step techniques, `provenance_note` identical 7 times, `inherited_inputs.note` identical 7 times with differing `items`, `rules` shared across the 4 design-philosophy and the 2 review-assumptions techniques. **Suggestive only — no figure may be quoted until S4's own fresh-mode benchmark arm measures it.** |
| DP-5 | Design Philosophy | Workflow Path | M | Research is warranted because the resumed-worker usage convention has external precedent worth consulting (how provider APIs and agent harnesses report usage across resumed and streamed turns) and the cost estimate needs current model pricing. Rationale: neither question is answerable from this codebase. Alternatives considered: skip research and fold a narrow pricing lookup into elicitation. | User (checkpoint `classification-and-path-confirmed`) | Confirmed — `full-workflow` set `needs_research: true` |
| DP-6 | Design Philosophy | Workflow Path | M | Requirements elicitation is warranted even though the user pre-scoped the package precisely and named the exclusions with reasons. Rationale: scope is settled but per-item success criteria are not — the undeclared-file response (warn / block / auto-declare), the usage convention and cost model, the `DEDUP_BLOCKS` extension set, and the reach of per-agent filtering are all open. Alternatives considered: treat the user's scoping message as sufficient and go straight to comprehension and planning. | User (checkpoint `classification-and-path-confirmed`) | Confirmed — `full-workflow` set `needs_elicitation: true` |
| DP-7 | Design Philosophy | Problem Interpretation | H | S2 can be closed server-side, as the issue's checkboxes imply ("diff the planning folder … at `next_activity`", "surface undeclared files to the orchestrator"). Rationale: both checkboxes name server behaviour. Counter-signal: the staging that causes the harm lives in corpus prose (`workflows/meta/techniques/workflow-engine/commit-and-persist.md` commits everything under `.engineering/artifacts/`), not in server code — `artifacts_produced` has zero hits across `src/`, `tests/` and `scripts/` — so a server-side diff surfaces undeclared files without preventing their commit, and the corpus half is nominally companion issue #338's territory. Alternatives recorded in the Open Assumptions entry. | User (stakeholder interview) | Confirmed — option (b), server-side only: the diff runs at `next_activity` and surfaces undeclared files into `_meta.validation`, with no corpus change and no hard gate, on the grounds that it breaks no existing run. Option (c) blocking rejected as a behavioural break; auto-declare rejected as laundering unreviewed files into apparently-reviewed ones. Corpus amendment deferred ([deferred-items](deferred-items.md) D-1) |
| DP-8 | Design Philosophy | Problem Interpretation | L | The three documentation drifts found during inspection are in scope as incidental corrections: `next_activity`'s tool description (`workflow-tools.ts:437`), `inspect_session`'s view-enum description (`:1571`) and `src/utils/dispatch.ts:6-8` all claim usage arrives on `next_activity` when only `record_usage` records it; and `session.schema.ts:158-159` lists an incomplete set of delivery-ledger key namespaces. Rationale: each is a one-line fix adjacent to code S3 and S4 already touch. Alternatives considered: defer to a separate docs issue; leave untouched. | Code: `src/tools/workflow-tools.ts:55, 1153` (only references to `usageSchema`) | Validated — S3's own checkbox ("pick and **document** the resumed-worker convention") cannot be satisfied while the tool contract documents a parameter that does not exist, so the three corrections are inside S3's boundary; the ledger-namespace list sits in a file S4 touches. |
| DP-9 | Design Philosophy | Workflow Path | M | A cost estimate surfaced through `inspect_session view: usage` satisfies S3's third checkbox, and the price table can be hard-coded. Rationale: the checkbox asks only for "cost estimate derived from the aggregate" and names no consumer. Counter-signal (challenge, stakeholder-gap): with no named consumer there is no acceptance test, and the one consumer the epic does name — writing the record into the planning artifact — is explicitly #338 W3, i.e. the corpus half, the same boundary DP-7 raises. Hard-coded prices also create a staleness liability with no owner. Alternatives recorded in the Open Assumptions entry. | User (stakeholder interview) | Corrected: the usage half was settled ahead of the cost half — delta counting, an `agent_id` parameter on `record_usage`, and a per-workflow aggregate that is a plain sum. Neither a price source nor a consumer was named, so the cost half was narrowed and carried as RE-4. RE-4 closed at research interview: stakeholder "defer price capture. token usage is sufficient" — price capture = D-4; SC-5 = token aggregate; planning-artifact record remains D-2 |
| DP-10 | Design Philosophy | Complexity Assessment | M | S4 remains an "M"-sized item that this package should carry as scoped. Rationale: the user scoped it in against the issue's own sizing. Counter-signal (challenge, rejected-paths): if DP-4's reframing holds, S4 is not "measure what remains" but "change block granularity so invariant preamble hashes separately from variant payload" — a redesign of `dedupTechniqueBlocks` plus a moved frozen baseline, materially larger than the sizing the user agreed to. This alternative was never put to the user because the reframing post-dates the scoping message. Alternatives recorded in the Open Assumptions entry. | User (stakeholder interview) | Corrected: option (b), not the agent's gated (a)-then-(b). S4 stays in scope carrying both the fresh-mode measurement arm and the two targeted coverage fixes (`provenance_note` as a dedup candidate; the invariant `note` hashed separately from per-technique `items`), with the reference baseline re-frozen. The re-scoping was put to the stakeholder: they were shown the corrected 3.9% marginal share instead of the issue's ~25% and kept S4 on that basis |
| DP-11 | Design Philosophy | Problem Interpretation | L | The #141 failure cited in the issue (two plan documents and 20 `PL-*` assumption rows entering an unreviewed commit) occurred as described, and S2's High severity rests on it. Rationale: it is the issue author's own record of their own run. Counter-signal (challenge, evidence-strength): it is a citation, not a reproduction — no independent verification was attempted, yet it is the sole empirical basis for the severity rating. | Accepted on the issue's own record | Validated by provenance, not by reproduction — flagged so the plan states S2's severity as cited rather than measured. Re-deriving it is not worth the archaeology; S2's own regression test should reproduce the *shape* (undeclared file reaching the staged set) rather than the incident. |
| RE-1 | Requirements Elicitation | Requirement Interpretation | M | The accumulated declared manifest that S2 diffs the planning folder against is persisted in a new session-state field, and no new `HistoryEventTypeSchema` member is needed because the discrepancy channel is `_meta.validation` rather than a history event. Rationale: the stakeholder decision named the surfacing channel but not the storage, and a diff at `next_activity` needs an accumulation that survives across dispatches. Ambiguity source — requirement reading: the decision specifies where the discrepancy appears, not where the declared set lives. Alternatives considered: accumulate in memory per call (impossible across dispatches); add an artifact event type to the closed enum and reduce over history instead of storing a field. | Code: `src/schema/session.schema.ts:59-164`, `src/schema/state.schema.ts:6-46`, `src/utils/validation.ts:255-265` | Validated — `SessionFileSchema` carries no artifact or manifest field, and `deliveredContent` (`:164`, an optional `z.record(z.record(z.string()))` keyed by agent) is the standing precedent for adding one; `HistoryEventTypeSchema` has no artifact event type, so storing a field avoids a closed-enum addition entirely; and `buildValidation` is variadic over warning strings, so surfacing the undeclared set costs no change to the validation shape. Two refinements carried in from comprehension: the comparison key is the declared **`id`**, not a filename — `composeActivityArtifacts` emits `{id, name}`, the envelope carries `{id, name, path}`, and ids are prefix-, template- and presentation-free — and at least three declaration families legitimately write outside the planning folder, so a declared id the diff cannot locate must read as *unknown* rather than *missing* |
| RE-2 | Requirements Elicitation | Success Criteria Interpretation | M | S2's success criterion is that an undeclared file is *surfaced* before staging, not that it cannot be committed — so the #141 failure shape is verified as "the undeclared file appears in the warning set", and end-to-end prevention is not a criterion of this package. Rationale: the stakeholder chose warn over block explicitly because blocking is a behavioural break, and the staging that causes the harm lives in corpus prose this package does not amend. Alternatives considered: make end-to-end prevention the criterion, which requires DP-7 option (a)'s corpus amendment. | User (stakeholder interview, S2 decision) | Confirmed — warn-not-block entails a surfacing criterion; end-to-end prevention deferred ([deferred-items](deferred-items.md) D-1) |
| RE-3 | Requirements Elicitation | Implicit Requirements | M | The new `agent_id` parameter on `record_usage` is optional, so existing callers stay valid and rows recorded without it aggregate under an unattributed bucket. Rationale: the delta decision requires the rows already recorded this session to remain valid, and none of them carries an `agent_id`; a required parameter would contradict that. Alternatives considered: make it required and migrate existing rows; infer it server-side from the session's current dispatch record. | Code: `src/tools/workflow-tools.ts:1149-1186, 256-260`, `src/schema/state.schema.ts:38-45` | Validated — `record_usage` takes `{session_index, activity, usage}` and writes `data: { usage }`, while `projectUsage` reads only `e.data?.['usage']`, so an added optional `agent_id` is strictly additive and existing rows project unchanged into an unattributed bucket. `activity_dispatched` already carries `{ agentId, dispatch }` in `data`, so `agentId` is the established field name to follow |
| RE-4 | Requirements Elicitation | Requirement Interpretation | H | S3's cost estimate is satisfied by a figure returned from `inspect_session view: usage`, priced from a table whose source and ownership are still unnamed. Rationale: carries forward DP-9's residue — the stakeholder settled the usage mechanism and the counting convention but named neither a price source nor a human consumer for the money figure. Ambiguity source — requirement reading: #365's checkbox says only "cost estimate derived from the aggregate". Alternatives recorded in the Open Assumptions entry. | User (checkpoint `research-assumption-decision#RE-4`, resolve-inline): "defer price capture. token usage is sufficient" | Corrected: price capture (config/hard-coded table, derived money figure, SC-5 as cost) deferred out of this package ([deferred-items](deferred-items.md) D-4); S3 ships DELTA rows + plain-sum token aggregate (+ optional `agent_id`) only — SC-5 reframed to token aggregate sufficiency |
| RS-1 | Research | Pattern Applicability | M | The DELTA-per-dispatch + plain-sum aggregate pattern from provider APIs and agent harnesses applies directly to this server's multi-dispatch `record_usage` model. Rationale: OpenAI/Anthropic return per-request usage; Cursor SDK exposes turn deltas and sums them into `run.usage`. Alternatives considered: leave convention undocumented and let each harness choose. | Code: `workflow-tools.ts:250-259` (projection is per-event, no rollup; comment notes harness disagreement); Research: OpenAI advanced usage, Cursor SDK token usage | Validated — code stores one row per dispatch with no sum; imposing DELTA makes the plain-sum aggregate correct and matches external default |
| RS-2 | Research | Pattern Applicability | H | A normalize-then-price step is required before cost math because `usageSchema` is an open record and providers disagree on cache field inclusion. Rationale: Spanlens/Anthropic docs show opposite cache math; pricing raw blobs silently mis-costs. Alternatives considered: price only `total_tokens` at a single blended rate; require harnesses to pre-normalize. | User RE-4: price capture deferred | Deferred: <[deferred-items](deferred-items.md) D-4> — pattern remains valid research for a later cost feature; not implemented in this package |
| RS-3 | Research | Source Relevance | L | Official OpenAI and Anthropic list prices fetched 2026-07-31 are suitable seeds for a default config table, not invoice truth. Rationale: both pages are vendor-authoritative; negotiated and platform fees differ. Alternatives considered: scrape third-party aggregators only; omit numeric seeds and ship an empty map. | User RE-4: price capture deferred | Deferred: <[deferred-items](deferred-items.md) D-4> — snapshot retained in [04-kb-research.md](04-kb-research.md) as reference only |
| RS-4 | Research | Synthesis Decisions | M | SC-5 is met when `inspect_session view: usage` returns a derived cost from a config price table over the plain-sum aggregate, with no planning-artifact write in this package. Rationale: research + D-2 + S2 server-only boundary. Alternatives considered: hard-coded table (a); require #338 W3 artifact in-package (c). | User RE-4: "defer price capture. token usage is sufficient" | Corrected: SC-5 is token-aggregate sufficiency (plain sum of DELTA rows); derived cost out of package (D-4); artifact write still D-2 |
| RS-5 | Research | Risk Assessment | M | v1 cost math may use base input/output rates only, omitting cache-write/read differentials and long-context tiers, and must label the figure as an estimate. Rationale: harness payloads may lack cache fields today; full tier matrix is a larger config surface. Alternatives considered: full Anthropic/OpenAI multiplier matrix in v1; no cost until cache fields are universal. | User RE-4: price capture deferred | Deferred: <[deferred-items](deferred-items.md) D-4> |
| RS-6 | Research | Synthesis Decisions | L | Price-table currency is owned by server maintainers on a best-effort basis (update when rates move or when estimates drift noticeably), not a formal SLA. Rationale: closes RC-5 without inventing process machinery; matches other `src/config.ts` knobs. Alternatives considered: mandatory calendar refresh; external live fetch at runtime (LiteLLM-style). | User RE-4: price capture deferred | Deferred: <[deferred-items](deferred-items.md) D-4> — no price table to own in this package |
| RE-5 | Requirements Elicitation | Success Criteria Interpretation | M | S4's ship gate is set to the honestly measured post-fix saving rather than to the issue's ~25% or the pre-fix marginal 3.9%, and re-freezing the token-benchmark reference baseline after the coverage change is inside S4's scope. Rationale: the stakeholder asked for the recovered share to be reported honestly against the frozen baseline, and the two coverage fixes necessarily move that baseline. Alternatives considered: keep the old baseline and report a raw delta; set `--min-saving-pct` to the issue's figure and let the gate fail. | Code: `scripts/run-dispatch-benchmark.ts:106, 170-172, 202-203`, `scripts/run-token-benchmark.ts:167, 170, 337-358` | Partially Validated and **corrected** — the stakeholder's phrase names one instrument where there are two. `bench:dispatch --gate --min-saving-pct=<n>` gates on `savingPct = (freshChars − resumeChars)/freshChars` (default 50, exit 3), a resume-vs-fresh delivered-chars measure that is **not** dedup-isolated; the frozen `token-benchmark-a0-reference.json` baseline and the fresh-mode gate belong to `bench:token` (`DEFAULT_MAX_REGRESSION_PCT = 1`; `evaluateGate` refuses to pass unless `modeMatched && workflowMatched`). The recovered dedup share must be read as a before/after delta on the same `bench:dispatch` arm rather than from its absolute `savingPct`. The **re-freeze half is Invalidated**: per the comprehension deep-dive, `dedupTechniqueBlocks` runs only under `if (referenceMode)` while the A0 fixture records `contextMode: "fresh"`, so the two named coverage fixes cannot move fresh-mode delivered chars and no `bench:token` re-freeze is owed for them. Caveat to verify before concluding: ledger *writes* are mode-independent (`workflow-tools.ts:665-667`) while reads are not, so a new `DEDUP_BLOCKS` member may still shift key-count metrics — settle with a dry `bench:token` comparison rather than by assuming either way. This supersedes DP-3's "any change to block coverage moves the frozen baseline" |
| RE-6 | Requirements Elicitation | Scope Boundaries | M | S5's per-agent dimension reaches exactly the four surfaces the stakeholder named and no other read path, and the oracle-parity cost of extending `inspect_session`'s history and usage views is accepted inside this package. Rationale: "every read path" was rejected on oracle-reconciliation cost, which implies the cost for the two views that were accepted is accepted; `projectSummary` / `projectActivities` / `projectCheckpoints` therefore gain no agent parameter. Alternatives considered: add the parameter to every projection; leave `inspect_session` untouched and filter only in `get_trace`. | Code: `tests/fixtures/inspect-session/inspect_session.py:10-13, 70, 120-144`, `src/tools/workflow-tools.ts:214-228` | **Invalidated** as stated, and the situation is worse than assumed — there is no oracle obligation on `usage` to discharge. Per the comprehension deep-dive, the parity test iterates a **hardcoded seven-view literal that omits `usage`** (`tests/mcp-server.test.ts:2564`) while the server lists eight, so the view is excluded from the assertion rather than merely unimplemented: nothing fails today and nothing would fail after an extension. The criterion must therefore include deriving that loop from the exported `INSPECT_SESSION_VIEWS`, or SC-12 is untestable. The `history` half stands: the oracle implements it and the array includes it, so an agent filter there is a live parity obligation, and since `projectHistory` reduces to `count`/`byType`/`milestones` reading no `data`, the filter means narrowing on `e.data?.agentId` before the reduce — a shape the oracle must mirror exactly. The four-surface reach itself is unchallenged by code |
| RE-7 | Requirements Elicitation | Implicit Requirements | H | Re-addressing a technique's resource links against the technique's own workflow must not regress refs that resolve today under the activity's workflow, and the "17 of 29" reduction is re-verified in implementation rather than taken as given. Rationale: the stakeholder decision changes an existing resolution rule, so it is a behaviour change and not purely additive; the figure predates the change. Alternatives considered: try the technique's workflow first and fall back to the activity's (strictly additive); leave resolution alone and only surface the warning. | Code: `src/tools/workflow-tools.ts:635, 764, 861-862`, `src/loaders/resource-loader.ts:31-32, 78-95` | Partially Validated — the bug is confirmed and the regression risk is real. `sourceWorkflowId` is bound from the **activity's** raw load at `:635` and passed to every resource resolution at `:861` and to `composeActivityTechnique` at `:764`, while `resource-loader` resolves exclusively at `<workflowDir>/<workflowId>/resources/<id>/SKILL.md` with no fallback. Because resolution is single-source today, *replacing* the source could move a ref from resolvable to unresolvable — but the comprehension deep-dive supplies a mechanism that cannot: **qualify the extracted id** with the technique's own workflow only when the composed technique came from a workflow other than the delivering one (`meta/planning-readme#template`), a form `parseResourceRef` already accepts. That adds a qualifier where none exists rather than changing a lookup root, so the regression concern is answered by construction. The "17 of 29" figure needs no re-verification — it was already corpus-measured (1,273 extracted ids over 15 workflows and 691 steps, 29 unresolvable across 14 ids, 17 of them `planning-readme#*`), and the defect was reproduced live on `get_resource`. One consequence changes a criterion: in **full mode**, production's default, the whole resolution loop is skipped and the id ships in `resource_refs` unvalidated, so a check placed only on the resolution path would leave the common case unchecked. The silent skip at `:862` is confirmed |
| RE-8 | Requirements Elicitation | Implicit Requirements | M | S5's step-granularity events are derived server-side from the `step_manifest` that already arrives on `next_activity`, rather than from a new worker-facing per-step call. Rationale: no worker contract changes, `validateStepManifest` already reads the manifest, and neither per-agent decision touched this gap. Counter-signal: retrospective emission at activity close cannot show a run in progress, which is part of what step granularity is for. Alternatives considered: a new `record_step` tool; derive the events from the `get_technique { step_id }` fetches already recorded. | Code: `src/utils/validation.ts:60-63`, `src/schema/state.schema.ts:15-28` | **Invalidated** as stated — `StepManifestEntry` is `{ step_id, output }` with no timestamps, and it arrives once at `next_activity`, so both derived events would carry the same activity-close timestamp and yield no in-flight visibility, which is most of what step granularity is for. The only per-step signals with real timestamps already reaching the server are `technique_fetched` (`data.stepId`) and `technique_bundled` — and an eagerly bundled step makes no `get_technique { step_id }` call at all, which is exactly the coverage hole. Reframed: the emission source is an internal contract choice (derive-at-close · a worker-emitted per-step signal · `step_started` from step-bound fetches with a hole for bundled steps), carried to implementation-analysis. Not stakeholder-dependent, so it does not join the open residue |

Resolution: how it was settled — `Code:` with file:line evidence, `User` (checkpoint or
interview), or `—` while open; implementation-task rows append the commit hash for
assumption-to-commit traceability. Outcome: Validated / Invalidated / Partially Validated
(code-resolved) · Confirmed / Corrected: <change> / Deferred: <follow-up> (user-resolved)
· Open (<reason>). When an interpretation difference contributed to an assumption, name
the ambiguity source (observation, recall, requirement reading, ambiguous problem
statement) in the rationale.

## Open Assumptions

*(none — RE-4 closed at individual interview; residual research assumptions RS-2…RS-6 follow the same deferral as D-4)*
