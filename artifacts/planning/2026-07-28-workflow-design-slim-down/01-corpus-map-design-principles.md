BASE = `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation`

Absolute paths for every file cited below:
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/resources/design-principles.md` (137 lines, 30 principles)
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/resources/anti-patterns.md` (1711 lines, AP-01…AP-129 + MR-1…MR-4)
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/resources/README.md` (planning-artifact→guide map at :39-58)
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/workflow.yaml` (265 lines, 63 variables)
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/activities/{01-intake-and-context,03-requirements-refinement,04-pattern-analysis,05-impact-analysis,06-scope-and-draft,08-quality-review,09-validate-and-commit,10-post-update-review,11-retrospective}.yaml`
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/workflow-design/techniques/audit-{principles,anti-patterns,expressiveness,conformance,rule-hygiene,rule-enforcement,schema-validation}.md`
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/meta/activities/patterns/README.md`
- `/home/mike1/projects/dev/workflow-server/.worktrees/2026-07-27-review-mode-friction-continuation/remediate-vuln/workflow.yaml`
- `/home/mike1/projects/dev/workflow-server/schemas/workflow.schema.json`

Short form below = basename + line. All line numbers verified by read, not inferred.

---

# PART 1 — Map of all 30 stances

Verdict key: **SUPPORT** = actively licenses consolidation · **CONSTRAIN** = binds/limits it · **BOTH** · **NEUTRAL** = notation/hygiene only.

| # | Name (`design-principles.md` line) | One-line claim | Bearing on merge / artifact cut / audit consolidation |
|---|---|---|---|
| 1 | Workflows Ossify Patterns (:17-19) | Workflows exist to make fixed, well-understood patterns reusable; novelty enters by design update or outside the graph. | **SUPPORT.** The graph is only warranted for *fixed* patterns. Any activity whose reason to exist is hosting on-the-fly judgement is novelty inside the graph, which §1 says belongs outside it. Mechanism: an activity that cannot be named as a reusable pattern has no §1 claim on its own node. |
| 2 | Internalize Before Producing (:21-23) | Demonstrate understanding of Goal→Activity→operation, schema-vs-runtime, inline-vs-modular, and existing conventions before producing content. | **CONSTRAIN (weak, artifact-level only).** §2 requires the *demonstration*, not files. `format-conventions.md` + `applicable-constructs.md` (paths at `workflow.yaml:113-120`) are the current evidence; `01-intake-and-context.yaml:210-223` already auto-confirms literacy by `action: set`. Mechanism: the gate must survive the cut; the two artifacts need not. |
| 3 | Define Complete Scope Before Execution (:25-27) | Enumerate every file to create/modify/remove before starting, include `{target_path}`, re-verify after completion. | **CONSTRAIN (hard, ordering).** Requires a strict before/after pair: enumeration then re-verify. Currently `scope_manifest` (`workflow.yaml:210-212`) + `scope-verified` checkpoint (`09-validate-and-commit.yaml:103`), backed by AP-07 `scope-reverify-completion` (`anti-patterns.md:158`). Mechanism: a merge may relocate both ends but cannot put them in the same unguarded block — `06-scope-and-draft.yaml:37` already separates enumerate from draft with an intra-activity checkpoint, proving the ordering survives merges only when a checkpoint carries it. |
| 4 | Clarify Before Assuming (:29-31) | When intent admits materially different interpretations, ask one clarifying question before acting. | **CONSTRAIN (gate placement).** Gate 1 `design-intent-batch` (`01-intake-and-context.yaml:36-88`) is §4's structural home; it must stay *before* execution, so it cannot be batched into a later gate. Note the tension with §12's one-decision rule is already resolved by gap-batching (`{intent_needs_confirmation}`, `workflow.yaml:37-40`) — that is the sanctioned way to reduce gates. |
| 5 | Maximize Schema Expressiveness (:33-35) | Prefer the most specific formal construct the schema provides; prose only in `description`/`outcome`; I/O contracts stay portable. | **BOTH.** Supports cutting prose-carried procedure. Constrains *where* consolidation lands: collapsing six audit passes into one technique's Protocol converts structure into prose orchestration — AP-114 `pass-orchestration-in-technique` (`anti-patterns.md:1478`). Mechanism: the reduction must land in activity `steps[]`, not inside a technique. |
| 6 | One Authoritative Home (:37-39) | Operative criteria/facts/fill-consult content have exactly one home; other layers cite, not re-author, and do not invent pass inventories that drift from activity bind sites. | **SUPPORT (strongest for audits).** See Part 2 quote and Part 5 arithmetic. Mechanism: fewer passes ⇒ fewer pass inventories that can drift; enforced by AP-105 `no-shadow-audit-pass` (:1366) "keep at most one walker per home" and AP-107 `bind-site-is-orchestration-truth` (:1390). Also **CONSTRAIN**: Detect criteria must stay in `anti-patterns.md`/`schema-construct-inventory.md`/`convention-conformance.md`; a merged walker must remain a walker. |
| 7 | Convention Over Invention (:41-43) | Search for existing conventions before introducing new patterns. | **CONSTRAIN.** The library convention is *fine-grained* activities: `remediate-vuln/workflow.yaml:322-330` borrows eight `work-package/` activities by path. A 4-activity shape diverges from sibling shape and must be justified as a convention change, not asserted. |
| 8 | Confirm Before Irreversible Changes (:45-47) | Classify by reversibility; semi-/irreversible changes require explicit confirmation with impact analysis. | **CONSTRAIN.** `impact-analysis.md` + `impact-and-preservation-confirmed` (`05-impact-analysis.yaml:39`) and Gate 2 `approve-to-commit` (`09-validate-and-commit.yaml:146`) are §8's enforcement. Mechanism: relocatable, not deletable — impact analysis is the *content* §8 requires at the confirmation, so cutting the impact artifact removes §8's substrate. |
| 9 | Encode Constraints as Structure (:49-51) | Critical constraints must be backed by checkpoints/conditions/validate actions, not rule text alone. | **CONSTRAIN — the primary brake on 9→4.** The 13 declared checkpoints (`01:36`, `03:10`, `03:74`, `04:19`, `05:39`, `06:37`, `06:265`, `06:286`, `06:304`, `08:78`, `09:55`, `09:103`, `09:146`) are the load-bearing set. Mechanism: every activity boundary that currently gates an outcome must reappear as an intra-activity checkpoint/condition, or the constraint degrades to prose — AP-79 `structure-backed-constraints` (`anti-patterns.md:1046`), which `audit-rule-enforcement.md:32` names as its sole criterion. |
| 10 | Non-Destructive Updates (:53-55) | Compare new against existing, flag removals, content-reducing updates require explicit user approval. | **CONSTRAIN — applies to issue #321 itself.** A 9→4 / 17→5 cut *is* a content-reducing update to workflow-design. Mechanism: §10 requires a removal inventory (`{removal_count}`, `workflow.yaml:85-88`) and explicit approval; AP-80 `preserve-readme-content` (:1058). The proposal cannot be self-authorizing. |
| 11 | Complete Documentation Structure (:57-59) | README at root and in each construct subfolder; a completeness verdict names the enumeration grounding it, not the instances inspected. | **BOTH (weak).** Fewer constructs ⇒ fewer READMEs (support). But planning `README.md` is one of the persisted artifacts (`resources/README.md:43`) and is not cuttable; and AP-40 `readme-orients-not-transcribes` (:566) means every README must be *rewritten* on an activity-count change, not trimmed — a cost of the merge, not a saving. |
| 12 | Output Economy (:61-63) | Design artifact contracts/checkpoints for the reader who must act: one canonical home per fact, declared audience, exception-only status, lean templates, one close-out doc, one decision per checkpoint. | **SUPPORT — strongest for 17→5.** See Part 2 quote and Part 5. Mechanism: enforced by AP-84 `single-closeout-artifact` (:1110), AP-86 `exception-only-verdict-tables` (:1134), AP-87 `omit-null-sections` (:1146), AP-88 `one-decision-one-checkpoint` (:1158), AP-93 `canonical-fact-home` (:1218). |
| 13 | Separate Contract from Procedure (:65-67) | Technique Inputs/Outputs are bind contracts stating *what*; Protocol orders *when/how*; any HOW attached to an I/O entry migrates to a Protocol step. | **CONSTRAIN (implementation quality of the merge).** A consolidated audit technique's Outputs must still declare each finding-kind's meaning without HOW, and must not carry trailing "Set …" projection phases. Enforced by AP-111 `contract-not-procedure` (:1442) and AP-119 `procedure-in-io-contract` (:1538). |
| 14 | Single Source of Truth (:69-71) | Each fact of session state has exactly one authoritative variable; gates compare that source, no parallel derived shadows. | **SUPPORT.** Mechanism: `workflow.yaml:61-84` declares six per-pass count variables (`expressiveness_/conformance_/rule_hygiene_/enforcement_/principle_/anti_pattern_finding_count`) alongside `review_findings_count` (:53-56) — a merged audit collapses these to one authoritative count and deletes the shadows. AP-112 `no-derived-state-shadow` (:1454). Same logic on the 18 `*_path` variables (:109-180). |
| 15 | Phase by Sequenced Outcome (:73-75) | A Protocol index marks a distinct outcome that must complete before the next begins; co-aspects stay as bullets; reorderable topic partitions get no number. | **SUPPORT — the decisive test for 9→4.** See Part 2 quote and Part 5. Mechanism: the reorder-or-drop test, lifted from Protocol indexes to activity nodes. |
| 16 | Distinguish Designators from Parameters (:77-79) | Braced `{id}` for declared values, italic *arg* for operation arguments, args in parens on the op reference. | **NEUTRAL.** Notation only. Cost, not constraint: merged Protocols must preserve the three namespaces (AP-56 `paren-invocation-args` :762, AP-52 `brace-declared-ids` :714). |
| 17 | Document in Positive Present (:81-83) | Definition prose states what the system *is*/*does* in positive declarative present — not avoidance or comparison against a prior design. | **CONSTRAIN (documentation of the merge).** Merged activities' `description`/`outcome` cannot read "no longer runs six separate passes" — AP-41 `avoidance-voice-in-definitions` (:578). Mechanism: the nine current `outcome:` blocks (`04:37`, `05:62`, `10:244`, `08:526`, `03:147`, `11:50`, `09:239`, `01:261`, `06:330`) must be re-authored from scratch for four nodes, not merged textually. |
| 18 | Prefer Shared Capability (:85-87) | When a meta/shared technique owns a capability, reuse it by binding that op from an activity or borrowing an activity that already binds it; invent local recipes only after the shared surface fails. | **SUPPORT (specific mechanism).** See Part 2 quote. Mechanism: the local *authored* activity count can drop without dropping structure, by borrowing meta pattern activities. Directly on point for the 12-worker dispatch: §18 names `meta/techniques/orchestration-patterns` and `meta/activities/patterns/` as the preferred fan-out/consolidate surface over local spawn-concurrent recipes. |
| 19 | Name Symbols Affirmatively (:89-91) | Symbol ids state what the value *is*, head-noun-last snake_case; booleans predicates, collections plural, I/O direction-free. | **NEUTRAL.** Mechanical. AP-58/64/65/66/67 (:786, :858, :870, :882, :894). |
| 20 | Keep Orchestration in Structure (:93-95) | Activities own stage, checkpoints, transitions, graph progress; techniques stay stage-agnostic and never name surrounding flow or consuming gates. | **CONSTRAIN — hard; forbids the cheap version of #321.** See Part 2 quote. Mechanism: 9→4 cannot be achieved by pushing sequencing into technique Protocols; AP-68 `technique-stage-agnostic` (:906), AP-114 (:1478). |
| 21 | Match the Harness Surface (:97-99) | Tool names, return shapes, bootstrap paths match the real harness; behavioural tool guidance lives in one authoritative place. | **SUPPORT (weak).** Fewer nodes ⇒ fewer copies of harness names to drift; AP-73 `consistent-tool-names` (:970), AP-74 `no-duplicated-guidance` (:982). |
| 22 | Modular Over Inline (:101-103) | Constructs live in their own files; parents reference siblings and never embed activity/technique/resource bodies. | **CONSTRAIN.** Merging nine activity *files* into four is legal (still files). Folding technique bodies into activities, or resource bodies into techniques, is AP-01 `no-inline-content` (:82). Mechanism: reduction must be by deletion or by grouping into `group::op` files, never by inlining. |
| 23 | Close the Loop (:105-107) | When implementation is in scope, a recommendation is followed by action or an explicit stop gate; analysis is not the terminal deliverable. | **CONSTRAIN.** `08-quality-review.yaml:468` `audit-fix-cycle` loop and `10-post-update-review.yaml:245-246` ("findings never wait on an accept/iterate/revert choice"; "Remediation edits reach the open pull request from here") are §23's enforcement. Mechanism: consolidating audits must preserve a fix cycle bound to the surviving pass; AP-78 `follow-through-on-recommend` (:1034). |
| 24 | Keep Session Interaction in Activities (:109-111) | Techniques are session-blind; activities own when/how technique products reach the user. | **CONSTRAIN (caps the saving).** The ~10 `action: message` steps in intake alone (`01-intake-and-context.yaml:89-157`) exist *because* techniques cannot speak. Merging activities does not remove them; folding them into techniques is AP-113 `session-interaction-in-technique` (:1466). Mechanism: step count is largely invariant under an activity merge. |
| 25 | Bind Sibling Operations as Steps (:113-115) | All multi-technique work lives in activity `steps[]`; bind each sibling/shared op as its own step; a technique owns one capability's produce path. | **CONSTRAIN — reframes the metric.** See Part 2 quote. Mechanism: six audit passes may not become one technique; they may become fewer *steps in one activity*. The legitimately reducible units are **activities, artifacts, and variables** — not steps. |
| 26 | Atomic Techniques; Compose at Activities (:117-119) | Techniques are small atomic capabilities over tools/resources; activities compose them; **activity→activity borrow/bind/include is allowed**; technique→technique work calls stay forbidden. | **SUPPORT — the key reconciliation clause.** See Part 2 quote. Mechanism: borrowed activities are not locally authored, and the borrow is a bare path entry in `activities:` (`remediate-vuln/workflow.yaml:322-330`; mechanics at `meta/activities/patterns/README.md:29-37`), so "4 activities" is achievable as 4 *local* activities plus borrows, satisfying #321's count without deleting the structure §9/§20 require. |
| 27 | State Contract Contribution (:121-123) | Container `TECHNIQUE.md` Capability names contributed shared Inputs/Outputs/Rules/invariants; loader composition lives in workflow-canonical and the construct inventory. | **CONSTRAIN (conditional).** Only binds if the merge creates an `audit::*` technique group. Its container Capability must state contributed contract, not explain the merge — AP-115 `platform-semantics-in-capability` (:1490), AP-123 `capability-as-op-inventory` (:1586). |
| 28 | Creation Guide for Generated Documents (:125-127) | Every persisted planning artifact has a creation-guide resource with `## Template` (+ operative `## Rules`); **shared shapes may share one guide**; every bare filename still maps to a guide. | **SUPPORT (mechanism for the resource-count drop) + CONSTRAIN (floor).** Mechanism: the shared-guide clause is already exercised — `findings-satellite.md` covers all seven findings files (`resources/README.md:58`) and `compliance-report.md` covers both report files (:57). So cutting 17→5 drops guides roughly in step, which is where most of the 24-resource count goes. Constraint: each surviving artifact still needs its guide — AP-116 `no-template-creation-guide` (:1502), AP-31 `no-hand-authored-artifacts` (:458). |
| 29 | Cite Resource Policy; Do Not Restate It (:129-131) | Protocol operates on semantic fields and cites resource sections in house style; representation/layout may change without Protocol change. | **SUPPORT — the enabler for audit consolidation.** See Part 2 quote. Mechanism: the **Separation test** means walker count is decoupled from criteria count. The canon (`anti-patterns.md`, `design-principles.md`, `schema-construct-inventory.md`, `convention-conformance.md`) is unchanged by how many techniques walk it, so merging citers loses no criteria. |
| 30 | Resources at the Abstract Level; Split for Section Delivery (:133-137) | Resources hold abstract kinds/roles/skeletons (technique owns concrete names and bindings); multi-part resources split into per-category sections fetched individually — a resource's sections are its delivery units. | **SUPPORT — supplies the concrete design for 17→5.** See Part 2 quote. Mechanism: `get_resource { resource_id: "<resource>#<category-anchor>" }` gives per-category context isolation *without* per-category files. The same economy applied to *artifacts* collapses the seven findings satellites (`workflow.yaml:153-180`) into one sectioned findings artifact at 1/7 the count, with no loss of per-pass isolation. |

---

# PART 2 — Verbatim quotes (the ten decisive stances)

**§6 One Authoritative Home** — `design-principles.md:37-39`
> ## 6. One Authoritative Home
>
> Operative criteria, reusable facts, and fill/consult content have exactly one home. Resources hold fill/consult (templates, vocabularies, criteria, policy tables); does (protocol, behavioural rules) lives outside resources — “does” means operational cadence and HOW, not normative criteria / vocabulary / policy matrices (those remain resource consult even when a technique Applies them). Other layers cite or walk the home — they do not re-author Detect, duplicate guidance, or invent pass inventories that drift from activity bind sites.

**§12 Output Economy** — `design-principles.md:61-63`
> ## 12. Output Economy
>
> Design artifact contracts and checkpoints for the reader who must act on them — one canonical home per fact, declared human vs agent audience, exception-only status, lean templates, one close-out document, one decision per checkpoint, statement-form messages with artifact links where named.

**§13 Separate Contract from Procedure** — `design-principles.md:65-67`
> ## 13. Separate Contract from Procedure
>
> On techniques, Inputs and Outputs are **bind contracts** — each declares *what* the bound value is (meaning, shape, allowed values; Outputs may include derivation/recognition criteria). Protocol orders *when* and *how* work runs and references `{id}`; it does not restate identity tables, and I/O descriptions do not carry HOW. Any how associated with an input or output — constraints, sequencing, fallbacks, side duties — migrates to a dedicated Protocol step (or a true cross-cutting Rule), not the I/O entry. Protocol also does not host trailing "Set …" phases for pure projections of another output.

**§15 Phase by Sequenced Outcome** — `design-principles.md:73-75`
> ## 15. Phase by Sequenced Outcome
>
> A Protocol index marks a distinct outcome that must complete before the next begins. Co-aspects of the same act — facets of one survey, constraints on one write, mode branches of one apply — stay as elaborating bullets under that phase. Topic partitions that can be reordered or dropped without changing the phase sequence do not get their own numbers.

**§18 Prefer Shared Capability** — `design-principles.md:85-87`
> ## 18. Prefer Shared Capability
>
> When a meta or shared-workflow technique already owns a capability, reuse it by binding that op from an activity (or borrowing an activity that already binds it). Invent a parallel local recipe only after the shared surface cannot absorb the caller's diversity. For mid-phase multi-agent fan-out and consolidate, prefer the meta [`orchestration-patterns`](../../meta/techniques/orchestration-patterns/TECHNIQUE.md) ops and borrowable [`activities/patterns/`](../../meta/activities/patterns/README.md) before local spawn-concurrent / merge recipes.

**§20 Keep Orchestration in Structure** — `design-principles.md:93-95`
> ## 20. Keep Orchestration in Structure
>
> Activities own stage, checkpoints, transitions, and graph progress. Techniques stay stage-agnostic: they produce values and durable evidence — they do not name the surrounding activity flow or the gates that consume their outputs.

**§25 Bind Sibling Operations as Steps** — `design-principles.md:113-115`
> ## 25. Bind Sibling Operations as Steps
>
> All multi-technique work lives in activity `steps[]` (and checkpoints/loops). Bind each already-defined sibling or shared operation as its own activity step. A technique owns one capability's produce path — its Protocol does not `Apply` sibling or meta ops for work. Loader ancestor wrap (`Initial`/`Final`) and container I/O merge are platform composition, not technique→technique work calls.

**§26 Atomic Techniques; Compose at Activities** — `design-principles.md:117-119` (the borrow/bind/include clause is in the third sentence)
> ## 26. Atomic Techniques; Compose at Activities
>
> Techniques are small, relatively atomic capabilities: a short produce path over tools and resources, without complex conditional/branching orchestration and without invoking other techniques to do work. Activities are the composition layer — they bind techniques (and checkpoints/loops) into useful work. Activity→activity composition is allowed: borrow, bind, or include activities to reuse standalone orchestration patterns — including the meta pattern library under [`meta/activities/patterns/`](../../meta/activities/patterns/README.md) (orchestrator-workers, supervisor, plan-and-execute, isolated-fan-out, lead-researcher). Technique→technique work calls remain forbidden — techniques stay atomic over tools and resources. Reuse a shared capability by binding it from an activity (or borrowing that activity), not by `Apply [other-technique]` inside a Protocol.

**§29 Cite Resource Policy; Do Not Restate It** — `design-principles.md:129-131`
> ## 29. Cite Resource Policy; Do Not Restate It
>
> Resources own vocabularies, criteria/policy, and how fields are represented or matched on consult surfaces. Technique Protocol operates on **semantic fields** and cites those sections in house style — e.g. `per [Section Title](../resources/example.md#section-title)` (link text is the section title; the URL includes the `#` anchor) — it does not re-author the vocabulary, policy matrix, or representation/matching rules. **Separation test:** representation or layout of a consult surface may change without Protocol change when the underlying fields are unchanged. This is the cite-don't-restate half of technique↔resource policy; it complements [One Authoritative Home](#6-one-authoritative-home) and [Creation Guide for Generated Documents](#28-creation-guide-for-generated-documents) without replacing either.

**§30 Resources at the Abstract Level; Split for Section Delivery** — `design-principles.md:133-137`
> ## 30. Resources at the Abstract Level; Split for Section Delivery
>
> A resource treats artifact names and variables at the abstract level — the *kind* of artifact, the *role* a variable plays, the template skeleton with placeholders. Concrete artifact filenames, variable bindings, and the per-run instances of both are owned by the technique (and the activity that binds the variables). The resource describes the shape; the technique instantiates it. A resource does not name the concrete artifact files or variables a specific technique produces or consumes — that coupling belongs to the technique's I/O contract and Protocol. This is the abstraction-level half of [One Authoritative Home](#6-one-authoritative-home) and [Cite Resource Policy; Do Not Restate It](#29-cite-resource-policy-do-not-restate-it).
>
> Split a multi-part resource into per-category sections, each carrying the fragment pertinent to one category (its table fragment, population rules, prefix), with any whole-document skeleton (header, per-category placeholders, footer) in its own section. The technique that renders one category fetches only its section (`get_resource { resource_id: "<resource>#<category-anchor>" }`); the consolidating step fetches only the skeleton section. No consumer loads the whole resource to read one category. Group shared fragments — scales, mappings, reference tables consulted across categories — under a single shared section, so a consumer fetches them as one unit rather than section-by-section. This is the section-grained half of the section-or-whole economy — a resource's sections are its delivery units.

---

# PART 3 — Principles a 9→4 activity merge RISKS violating

Ordered by severity. Each names the mechanism, not just the principle.

**R1 — §9 Encode Constraints as Structure (:49-51). Severity: highest.**
Mechanism: an activity boundary is itself a structural gate (transition + `initialActivity`/`transitions` graph progress). Nine activities currently carry 13 declared checkpoints and 63 variables' worth of conditions. Deleting five boundaries deletes five ordering guarantees unless each reappears as an intra-activity checkpoint or `condition`. If any migrates to `rules[]` prose instead, `audit-rule-enforcement` will flag it against its own sole criterion — AP-79 `structure-backed-constraints`, cited at `audit-rule-enforcement.md:32`. The merge is self-flagging.

**R2 — §20 Keep Orchestration in Structure (:93-95) + §25 (:113-115) + §26 (:117-119). Severity: highest.**
Mechanism: the cheapest route to "4 activities" is to make each surviving activity bind one fat technique that internally sequences what used to be an activity's steps. That is exactly AP-114 `pass-orchestration-in-technique` (`anti-patterns.md:1478`), whose Detect test is: "if moving each invoked op to its own activity `steps[]` entry … preserves behavior, flag it." §25's "All multi-technique work lives in activity `steps[]`" makes this non-negotiable. Consequence: **the merge cannot reduce step count** — 08-quality-review.yaml's 530 lines of audit/persist/message steps have to land somewhere, and the only legal home is `steps[]`. #321's activity-count metric therefore does not translate into a definition-size reduction unless artifacts are cut too.

**R3 — §10 Non-Destructive Updates (:53-55). Severity: high; applies to the proposal itself.**
Mechanism: 9→4 and 17→5 is a content-reducing update. §10 requires comparison against existing content, a flagged removal inventory (`{removal_count}`, `workflow.yaml:85-88`), and explicit user approval. AP-80 `preserve-readme-content` (:1058) extends this to the READMEs. #321 must run through workflow-design's own preservation gate (`05-impact-analysis.yaml:39` `impact-and-preservation-confirmed`) rather than around it.

**R4 — §3 Define Complete Scope Before Execution (:25-27). Severity: high.**
Mechanism: §3's before/after pair spans activities today (`scope_manifest` bound in 06, re-verified at `09-validate-and-commit.yaml:103`). If scope and validate merge into one node, "re-verify after completion" loses the boundary that made "after" meaningful; AP-07 `scope-reverify-completion` (:158) then applies. Requires an explicit intra-activity checkpoint to carry the ordering.

**R5 — §4 Clarify Before Assuming (:29-31) + §8 Confirm Before Irreversible (:45-47). Severity: high.**
Mechanism: §4 needs a gate *before* execution and §8 needs one *before* commit. Merging intake into refinement risks Gate 1 (`01:36`) firing after some drafting has begun; merging validate into commit risks Gate 2 (`09:146`) losing the impact/attestation payload it batches (`09-validate-and-commit.yaml:242`). Both gates are hard-interactive by design (`workflow.yaml:18` — Gate 1, Gate 2, `preservation-check`, `review-disposition` "stay interactive" even in headless mode), so they cannot be soft-merged.

**R6 — §7 Convention Over Invention (:41-43). Severity: medium.**
Mechanism: the library's convention is many small activities, evidenced by `remediate-vuln/workflow.yaml:322-330` borrowing eight `work-package/` activities by path. A 4-node shape is the *invention*; §7 requires searching the sibling corpus first and justifying divergence.

**R7 — §17 Document in Positive Present (:81-83) + §11 Complete Documentation Structure (:57-59). Severity: medium (cost, not blocker).**
Mechanism: all nine `outcome:` blocks and every README must be re-authored in positive present for the new shape (AP-41 :578, AP-40 :566). This is real work the proposal must budget, and it is where a rushed merge leaks comparative framing ("replaces the former six passes").

**R8 — §23 Close the Loop (:105-107). Severity: medium.**
Mechanism: `08-quality-review.yaml:468` `audit-fix-cycle` and `10-post-update-review.yaml:245-246` guarantee findings get remediated, not merely reported. Collapsing quality-review and post-update-review into one node must preserve *both* fix cycles or the post-commit loop silently becomes report-only — AP-78 (:1034).

**R9 — §24 Keep Session Interaction in Activities (:109-111). Severity: low, but caps the expected saving.**
Mechanism: `01-intake-and-context.yaml:89-157` is ~10 `action: message` steps that exist solely because techniques are session-blind. They are invariant under an activity merge. Any projected line-count saving that assumes they disappear is wrong; moving them into techniques is AP-113 (:1466).

**R10 — §22 Modular Over Inline (:101-103). Severity: low.**
Mechanism: bounds *how* the 38 techniques and 24 resources may shrink — by deletion or by grouping into `group::op` files, never by inlining bodies into activities (AP-01 :82).

**R11 — §13 Separate Contract from Procedure (:65-67) + §27 State Contract Contribution (:121-123). Severity: low, conditional.**
Mechanism: only bites if the merge produces a consolidated audit technique or an `audit::*` group. Then its I/O must carry no HOW (AP-119 :1538, AP-111 :1442) and its container Capability must not explain the consolidation (AP-115 :1490, AP-123 :1586).

---

# PART 4 — Principles that ACTIVELY SUPPORT consolidation

**S1 — §15 Phase by Sequenced Outcome (:73-75). This is the canon's own test for the number 4.**
Mechanism: "Topic partitions that can be reordered or dropped without changing the phase sequence do not get their own numbers." §15 is written about Protocol indexes, but it is the corpus's only stated criterion for *when a distinct node is earned*, and the criterion is outcome-sequencing, not topic. Applied to activities: `04-pattern-analysis.yaml` (38 lines, 1 checkpoint) and `05-impact-analysis.yaml` (65 lines, 1 checkpoint) are both surveys feeding the same downstream write, and neither's outcome is a precondition of the other's — they are reorderable topic partitions of "understand the target before writing." By §15's test they are co-aspects of one phase. Corroborating evidence that the corpus already accepts this: activity filenames are numbered `01, 03, 04, 05, 06, 08, 09, 10, 11` — **02 and 07 are absent**, so two prior merges have already happened without renumbering.

**S2 — §6 One Authoritative Home (:37-39) + §29 Separation test (:129-131) + §12 (:61-63). This is the canon-derived case for collapsing audit passes, and it is arithmetic, not judgement.**
Mechanism: each audit technique names its *sole* criteria home in its own Protocol step 1:
- `audit-principles.md:28` → `design-principles.md`
- `audit-anti-patterns.md:28` → `anti-patterns.md` (full catalogue walk)
- `audit-rule-hygiene.md:32` → `anti-patterns.md` ("sole source of Rule Hygiene detect…")
- `audit-rule-enforcement.md:32` → `anti-patterns.md`, **a single entry**, `structure-backed-constraints`
- `audit-expressiveness.md:32` → `schema-construct-inventory.md`
- `audit-conformance.md:32` → `convention-conformance.md`

That is **six walkers over four homes, with three walkers on one home.** AP-105 `no-shadow-audit-pass` (`anti-patterns.md:1366`) states the rule directly: *"keep at most one walker per home (or a scoped thin walker that does not re-author criteria)."* The three `anti-patterns.md` walkers survive AP-105 only via its thin-walker carve-out — and `audit-rule-enforcement` is a whole pass, variable, artifact, and four activity steps dedicated to **one catalogue entry out of 129**. §29's Separation test then removes the last objection: because these techniques cite rather than author, merging them changes no criterion. **Canon-implied floor for audit passes: 4 (one per home), or 3 if the Rule Hygiene section and AP-79 fold back into the full `anti-patterns.md` walk — down from 6, with zero criteria loss.**

**S3 — §12 Output Economy (:61-63) + §30 section delivery (:133-137) + §28 shared guides (:125-127). The case for 17→5.**
Mechanism, concretely against the corpus:
- `workflow.yaml:153-180` declares **seven findings-artifact path variables** (`expressiveness_`, `conformance_`, `rule_hygiene_`, `enforcement_`, `verified_`, `principle_`, `anti_pattern_findings_path`). `resources/README.md:58` maps all seven to **one** guide, `findings-satellite.md`. Seven files sharing one shape *is* one artifact kind split seven ways — §12's "one canonical home per fact" plus §30's "a resource's sections are its delivery units" and `get_resource { resource_id: "<resource>#<anchor>" }` deliver identical per-pass context isolation from **one** sectioned findings artifact. Net: −6 artifacts, −6 path variables, and (with S2) −5 count variables.
- `resources/README.md:57` maps `compliance-review.md` **and** `post-update-review.md` to one guide, `compliance-report.md`. Two files, one shape, one fact-set. §12's "one close-out document" and AP-84 `single-closeout-artifact` (:1110) point at the same collapse; `COMPLETE.md` (`resources/README.md:44`) plus `follow-ups.md` are a second instance of the same smell.
- `06-scope-and-draft.yaml` persists `drafting-plan.md` and `file-review-note.md` **per file** (`workflow.yaml:145-152`) — per-item satellites that §30's section-delivery pattern replaces with sections of one plan document.
- AP-86 `exception-only-verdict-tables` (:1134) and AP-87 `omit-null-sections` (:1146) hit the four `*-clean` message steps directly: `08-quality-review.yaml:143-161` is an entire conditional step whose payload is `"Schema expressiveness review complete — 0 findings."` Four such all-green announcements (`expressiveness-clean` :144, `conformance-clean` :221, `rule-hygiene-clean` :298, `enforcement-clean` :375) are precisely §12's "exception-only status" violation — one bit each, ~19 YAML lines each.

**S4 — §26 Atomic Techniques; Compose at Activities (:117-119) + §18 Prefer Shared Capability (:85-87). This is how 9→4 and §9/§20 are reconciled — the most useful finding for #321.**
Mechanism: §26's third sentence — *"Activity→activity composition is allowed: borrow, bind, or include activities to reuse standalone orchestration patterns — including the meta pattern library under `meta/activities/patterns/`"* — and §18's *"(or borrowing an activity that already binds it)."* The borrow is a **bare path string in the workflow's `activities:` list**: `remediate-vuln/workflow.yaml:322-330` borrows eight `work-package/*` activities that way; mechanics at `meta/activities/patterns/README.md:29-37` ("Borrow the activity into a client `workflow.yaml` `activities:` list… Wire your own `transitions` in a thin local wrapper"). Five borrowable patterns exist today: `meta/activities/patterns/{01-orchestrator-workers,02-supervisor,03-plan-and-execute,04-isolated-fan-out,05-lead-researcher}.yaml`.
Therefore: **#321's "4 activities" is satisfiable as 4 locally-authored activities plus borrowed pattern activities**, which keeps every gate as structure (§9), keeps orchestration in activities (§20/§25), and reduces the *authored* surface — the thing that actually costs tokens to load and audit. This is the only route to 9→4 that no principle blocks.
§18 also speaks directly to the 12-worker/~1.43M-token dispatch: it says to prefer `meta/techniques/orchestration-patterns` ops and the borrowable pattern activities *before* local fan-out recipes. If the 12 dispatches were locally recited, §18 is already violated — AP-110 `duplicate-shared-capability` (:1430).

**S5 — §14 Single Source of Truth (:69-71).**
Mechanism: `workflow.yaml` carries 63 variables for 9 activities, including six per-pass finding counts (:61-84) beside `review_findings_count` (:53-56), and eighteen `*_path` variables (:109-180). A merged audit yields one count; a sectioned findings artifact yields one path. AP-112 `no-derived-state-shadow` (:1454) treats the per-pass set as shadows of one authoritative fact.

**S6 — §1 Workflows Ossify Patterns (:17-19).**
Mechanism: the graph earns a node only for a *fixed, well-understood, shareable* pattern. An activity that exists to host per-run judgement is novelty, which §1 places outside the graph. This is the stance-level warrant for asking each of the nine "is this a pattern, or a prompt?" — and it is the principle that explains the token asymmetry in the brief: 12 dispatches over nine nodes bought structure where two dispatches over the raw canon bought findings, because the canon (`design-principles.md` + `anti-patterns.md`) is where the ossified pattern actually lives.

**S7 — §5 Maximize Schema Expressiveness (:33-35), §21 Match the Harness Surface (:97-99), §11 (:57-59) — supporting, second-order.**
Mechanism: each duplicated node is another copy of harness names, README orientation, and prose that can drift (AP-74 :982, AP-40 :566). Fewer nodes ⇒ fewer drift sites.

---

# PART 5 — Bottom line, grounded

- **9→4 activities: not blocked by any principle, but only via §26's borrow clause.** §15's reorder-or-drop test independently licenses merging `04-pattern-analysis` and `05-impact-analysis` (both surveys, mutually reorderable, 38 and 65 lines, one checkpoint each), and the absent `02`/`07` filenames show the corpus already merged twice. What §9 and §20/§25 forbid is achieving the count by *moving orchestration into techniques*; what §26/§18 permit is achieving it by *borrowing activities*. #321 should specify which route it means — that single choice determines whether it violates two principles or satisfies six.
- **17→5 artifacts: the best-supported half of the proposal.** The canon supplies the mechanism, not just permission: §30's section-delivery replaces the 7 findings satellites (`workflow.yaml:153-180`, all mapped to one guide at `resources/README.md:58`) with one sectioned artifact; §12/AP-84 collapse the two report files and `COMPLETE.md`+`follow-ups.md`; AP-86/AP-87 delete the four all-green `*-clean` message steps (`08-quality-review.yaml:143-161, 220-240, 297-317, 374-394`). Floor is set by §28 (each survivor keeps a guide) and §11 (planning `README.md` is not cuttable) — so ~5-7 is reachable, not 0.
- **6→3-or-4 audit passes: the canon already implies this and #321 does not ask for it.** Six walkers over four homes, three of them on `anti-patterns.md`, one of them (`audit-rule-enforcement`) dedicated to a single catalogue entry — against AP-105's explicit "at most one walker per home" (`anti-patterns.md:1366`). §29's Separation test guarantees no criteria are lost. This is the largest defensible cut in the corpus and it is orthogonal to the activity count.
- **Meta-constraint: §10 (:53-55) makes #321 subject to workflow-design's own preservation gate.** A content-reducing update needs a flagged removal inventory (`{removal_count}`, `workflow.yaml:85-88`) and explicit approval at `05-impact-analysis.yaml:39`. The proposal must be executed *through* the workflow, not applied to it.
