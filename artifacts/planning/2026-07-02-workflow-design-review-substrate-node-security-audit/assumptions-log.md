# Assumptions Log

> substrate-node-security-audit remediation · workflow-design UPDATE mode · updated 2026-07-02

## Log

One row per assumption, updated in place. IDs use the two-letter phase prefix + sequence: `RR` = requirements-refinement.

| ID | Phase/Task | Category | Risk | Assumption — rationale | Resolution | Outcome |
|----|------------|----------|------|------------------------|------------|---------|
| RR-1 | Requirements Refinement | Activity Boundaries | L | No activity is added, removed, or renamed — every fix is content/structure within the existing 14 activities. Rationale: intake classified the change as "structural refactor + metadata/rule hygiene + resource decomposition"; no finding requires a new phase or removal of one. | Code: `01-intake-and-context.md` §change-request; worktree `activities/` (14 files, unchanged set) | Validated |
| RR-2 | Requirements Refinement | Checkpoint Necessity | L | Zero user checkpoints are added — the workflow stays FULLY AUTOMATED; F-05/F-06/F-08 add structural `condition`/`validate` enforcement, not human decision gates. Rationale: the FULLY-AUTOMATED design predicate is preserved; findings ask for structural enforcement of self-declared gates, which is `condition`/`validate`, not `checkpoint`. | Code: `workflow.yaml:19` (FULLY AUTOMATED rule); verification F-05/F-06/F-08 | Validated |
| RR-3 | Requirements Refinement | Rule Scope | L | F-01 is a false positive and takes no action — the four worker rules already sit in `rules.activity` (worker-facing bucket). Rationale: adversarial verification read the file and confirmed lines 37-40 are under `rules.activity`. | Code: `workflow.yaml:37-40`; `08-quality-review-verification.md` F-01 REFUTED | Invalidated (the finding, not the code) |
| RR-4 | Requirements Refinement | Rule Scope | M | F-02 dedupe drops the severity-rubric.md / severity-calibration.md cites (category slip — rubric tables are reference content, not duplicated rules); the two authoritative homes to fix are DEFENSE-IN-DEPTH and weights.rs only. Rationale: verification narrowed the confirmed duplication to `workflow.yaml:36↔apply-checklist.md` and `:37↔apply-checklist.md`. | Code: `08-quality-review-verification.md` F-02; `apply-checklist.md:39,40,54` | Partially Validated |
| RR-5 | Requirements Refinement | Schema Construct Choice | L | F-07 finalize `set` flags are reshaped to `target`+`value` for schema hygiene only — no functional gate is restored (AP-67(c) exempt; action-step `set`s are never engine-evaluated). Rationale: verification confirmed these `*_complete` flags are read by no condition and mutation flows only through checkpoint effects. | Code: `08-quality-review-verification.md` F-07; `workflow.yaml:82-116` finalize flags | Validated |
| RR-6 | Requirements Refinement | Schema Construct Choice | L | F-09 keeps the `decisions[]` construct and drops the duplicate conditional `transitions[]` for the routing in 05/06. Rationale: `decisions[]` is the richer, single-home routing construct; duplicate transitions restate it. | Code: `05-report-generation.yaml:40-76`, `06-ensemble-pass.yaml:24-46` | Validated |
| RR-7 | Requirements Refinement | Variable State | M | F-08 deletes only genuinely-unread gate variables; any gate variable newly wired into an F-05 `condition` is RETAINED and set structurally. Rationale: F-05 and F-08 interact — a variable read by no condition is either wired (F-05) or pruned (F-08), never both. | Code: `workflow.yaml:82-116` variables; verification F-05/F-08 | Validated |
| RR-8 | Requirements Refinement | Rule Scope | L | F-03/F-04/F-18 are mechanical rule-hygiene transforms (de-CAPS, rationale→CHANGELOG, grouped keyed arrays, affirmative slugs) that preserve every rule's semantics and audience bucket. Rationale: hygiene transforms do not change enforcement or scope. | Convention: AP-24-29 rule-hygiene; `08-quality-review.md` F-03/F-04/F-18 | Validated |
| RR-9 | Requirements Refinement | Technique Selection | M | F-13 (`map-vulnerability-domains` binding gap) and F-14 (`score-severity` per-call rename) are resolved by canonicalizing producer/consumer ids per generic-not-overfit, aligning the caller's bag variable to the technique's intrinsic signature (or a dotted-path read against `architectural_analysis`). Rationale: binding fidelity favors canonical ids over per-call renames. | Convention: `generic-not-overfit` rule; `map-vulnerability-domains.md:16-41`, `score-severity.md` | Validated |
| RR-10 | Requirements Refinement | Activity Boundaries | L | F-16 (subfolder READMEs) + F-19 (drop `NN-` resource index) + F-21 (merge the two severity resources, drop `## Overview` wrappers) + F-22 (README links not enumerations) + F-24 (delete tombstones) are documentation/resource hygiene with no structural workflow effect. Rationale: none touches steps, transitions, or the variable contract. | Convention: prism README style; documentation-as-it-is voice; `08-quality-review.md` F-16/F-19/F-21/F-22/F-24 | Validated |
| RS-1 | Requirements Refinement | Schema Construct Choice | H | F-05 wires the three HARD-STOP gates by reusing the already-declared gate variables (set structurally) and gating with boolean `condition`s on the report-generation entry, using `validate` only where no variable fits. | User | Confirmed |
| RS-2 | Requirements Refinement | Technique Selection | M | F-10/F-11: split `dispatch-sub-agents` by protocol phase into per-phase ops AND collapse same-activity redundant verify/merge re-bindings, preserving the AP-73 roster exception for the two distinct `dispatch-*-agent` bindings. | User | Confirmed |
| RS-3 | Requirements Refinement | Technique Selection | M | F-20: relocate §2 (static-analysis grep/cargo procedures) → `search-pattern-catalog`, §3 (manual-review PASS/FAIL decision criteria) → `apply-checklist`, §5 (execution protocol/rules) → `execute-ensemble-pass`; `audit-prompt-template.md` survives as a THIN reference holding §1 Setup, §4 Reporting Format, and the §1-§5 taxonomy. | User | Confirmed |
| RS-4 | Requirements Refinement | Schema Construct Choice | L | Version bump is a MINOR increment (4.16.0 → 4.17.0) — no schema or contract break; all fixes are content/structure refactors. | User | Confirmed |

## Wrap-Up

14 assumptions — all validated or confirmed; none corrected, invalidated in substance, or deferred.

- RR-1 through RR-10 (10): reconciliation assumptions settled autonomously by re-reading the target files against the schema and conventions (the review recalibration had already grounded them). RR-3 records the F-01 finding as invalidated (false positive) — the code placement was already correct.
- RS-1 through RS-4 (4): genuine design judgements confirmed by the user via the assumption-interview loop, each matching the agent's recommended position — RS-1 hybrid gate enforcement (condition + validate), RS-2 split-and-collapse the dispatch/verify/merge bindings, RS-3 §2/§3/§5 relocation with a surviving thin `audit-prompt-template.md`, RS-4 minor version bump 4.16.0 → 4.17.0.

Takeaway: the recalibrated finding set is a fully settled change specification — no open questions carry into scope-and-draft.
