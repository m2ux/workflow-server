# Change Block Index — Markdown Skills Migration

**Activity:** post-impl-review (session SUQLKL)
**Date:** 2026-05-29
**Diff scope (source side):** `1d76647..HEAD` on `feat/125-markdown-skills-migration`
**Diff scope (content side):** `workflows..HEAD` on `feat/125-markdown-skills-content`

Hunks are grouped per file × commit. The `#` column is a row index for this review document only — it is not a resource or technique ID. Resources and techniques are addressed by id (frontmatter `name` slug or `metadata.id`); the legacy `NN-` numeric prefix on flat resource filenames is deprecated and not part of any current lookup contract. Use `git show <commit> -- <path>` to view a row's underlying diff.

## How to use this index

- Rows below the section heading "Source-side change blocks" sit on `feat/125-markdown-skills-migration` in `/home/mike1/projects/work/workflow-server/2026-05-28-markdown-skills-impl/`.
- Rows below "Content-side change blocks" sit on `feat/125-markdown-skills-content` in `/home/mike1/projects/work/workflows/2026-05-28-markdown-skills-impl/`.
- Each rationale paragraph names the design intent and the surface area the change owns. Confirming a row attests that the change does what the rationale says.

---

## Source-side change blocks (feat/125-markdown-skills-migration)

| # | Commit | Path | Lines | Rationale |
|---|--------|------|-------|-----------|
| 01 | e2a5988 | src/loaders/markdown-skill-loader.ts | +722 (new file) | New markdown skill loader. Owns the SKILL.md → `Skill` projection: YAML frontmatter (`name`, `description`, `metadata.version`), H2 sections (`## Capability`, `## Inputs`, `## Protocol`, `## Operations`, `## Outputs`, `## Rules`, `## Errors`), op-as-child-files assembly (sibling `<op>.md` files keyed by basename, parsed via the canonical `## Inputs / ## Output / ## Procedure / ## Errors / ## Rules` shape with `## Procedure` required). Exposes `tryLoadMarkdownSkill`, `tryReadMarkdownSkillRaw`, `readMarkdownSkill` (Result wrapper), `getWorkflowTechniquesDir`. Parse failures raise `MarkdownSkillParseError` internally; the public API returns `null` (loader not-found semantics) and logs via the existing `logWarn` channel — assumption A-016. |
| 02 | e2a5988 | scripts/migrate-skills/migrate.ts | +185 (new file) | Idempotent migration utility. Copies `.engineering/artifacts/planning/2026-05-22-claude-skills-migration/legacy/<workflow>/{techniques,resources}/<slug>/` into `<workflows>/<workflow>/{techniques,resources}/<slug>/` (preserving SKILL.md + sibling op-child files). Prints per-workflow and summary counts; exits non-zero on missing inputs. Uses `node:fs` sync calls deliberately — script is short-lived and ordering matters per slug. |
| 03 | e2a5988 | scripts/migrate-skills/README.md | +79 (new file) | Operator-facing documentation for the migrate-skills script: inputs (legacy source, workflows target), outputs (per-workflow directory layout), idempotency contract (re-runs overwrite with byte-identical content; no synthesised files), how to rerun via `tsx` or the `npm run migrate-skills` alias (added in commit 31487f6 via package.json). |
| 04 | e2a5988 | package.json | +1 line | Add `migrate-skills` npm script alias (`tsx scripts/migrate-skills/migrate.ts`) so the migration utility is discoverable from `npm run`. |
| 05 | e2a5988 | tests/fixtures/markdown-skills/meta/techniques/agent-conduct/SKILL.md | +23 (new) | Test fixture — meta-side rules-only technique. Used by PR126-TC-04 (precedence falls back to meta when no workflow-local override exists). Tagged with `description: meta-version` so the test can match against `/meta-version/i`. |
| 06 | e2a5988 | tests/fixtures/markdown-skills/meta/techniques/build-comprehension/SKILL.md | +12 (new) | Test fixture — meta-side stub. Pair partner for fixture 07 below; the work-package override must suppress this version per PR126-TC-05. |
| 07 | e2a5988 | tests/fixtures/markdown-skills/work-package/techniques/build-comprehension/SKILL.md | +12 (new) | Test fixture — workflow-local override of `build-comprehension`. Tagged with `description: workflow-local override` so PR126-TC-05 can confirm override precedence. |
| 08 | e2a5988 | tests/fixtures/markdown-skills/work-package/techniques/cargo-operations/{SKILL.md,check.md,test.md} | +59 (new, 3 files) | Test fixture — op-as-child-files folder. Exercises PR126-TC-03 (operations materialise from child files) and PR126-TC-08 (projection round-trip). `check.md` and `test.md` carry the canonical op shape (Inputs, Output, Procedure, Errors). |
| 09 | e2a5988 | tests/fixtures/markdown-skills/work-package/techniques/malformed-ops/{SKILL.md,broken.md} | +22 (new, 2 files) | Test fixture — op-child file missing `## Procedure`. Exercises PR126-TC-06: the loader must surface a parse failure rather than silently dropping the operation. |
| 10 | 31487f6 | src/loaders/skill-loader.ts | -159, +247 (rewrite) | Routes the public skill-load surface through the markdown reader. Adds `projectSkillToToon(skill)` — a single shared projection function with canonical field ordering (id, version, capability, description, inputs, protocol, output, rules, errors, resources, operations) — assumption A-018. Replaces the cross-workflow scan-all in `readSkill` / `readSkillRaw` with explicit workflow-local → `meta` precedence; techniques are resolved by id only (folder slug or frontmatter `id`), with an optional explicit-prefix form `meta/<id>` to target the shared layer (Task B4). Removes the `parseActivityFilename as parseSkillFilename` alias (per Task B2). Retains a legacy TOON loader gated by `SKILL_LOADER_LEGACY_TOON=true` as a brief-window safety fallback (assumption A-007; removed in Phase C). Listing helper `listWorkflowSkillIds` enumerates technique folders by id; the deprecated numeric-prefix `skills/` filenames are only consulted when the legacy flag is on and the markdown side is empty. |
| 11 | 2a7d4be | src/loaders/resource-loader.ts | -141, +175 (rewrite) | Flips resource loader to markdown-only with id-only lookup as the canonical contract. Resolution prefers the folder shape `<slug>/SKILL.md`, matching by directory slug or by frontmatter `id`; an id-style ref that has no folder match is then resolved against the deprecated flat shape via filename-stem and frontmatter-id scan (the `NN-` numeric prefix on those filenames is transitional and is not part of the lookup contract). `parseResourceFilename` regex now only matches `.md`; the `.toon` branch is gone. New `findFolderResource` performs the id-only folder match. `listResources` walks both shapes and emits id-keyed entries; flat-shape entries are surfaced for backward compatibility only. The `guides/` legacy fallback in `getResourceDir` is preserved (assumption A-013, out of scope). |
| 12 | 3534906 | tests/skill-loader.test.ts | -39, +132 (rewrite) | Replaces the legacy TOON-fixture tests with the markdown-loader test suite per the test plan. Three blocks: (a) real-content checks against `workflows/meta/techniques/{agent-conduct,workflow-engine}` (TC-16), (b) fixture-based PR126-TC-03 through TC-08 and TC-15 (precedence, op-as-child-files, projection round-trip, alias regression guard), (c) tempdir edge cases (not-found, missing frontmatter, minimal valid SKILL.md). |
| 13 | 3534906 | workflows (submodule pointer) | 1 line | Submodule pointer update to advance the workflows worktree to the head of `feat/125-markdown-skills-content` so the source-side branch resolves the new techniques/resources layout in tests and at runtime. |
| 19 | 3dbf648 | scripts/migrate-skills/translate.ts | +876 (new file) | TOON-to-markdown translator for the remaining 9 workflows. Reads `<workflow>/skills/NN-<slug>.toon` and emits `<workflow>/techniques/<slug>/SKILL.md` per legacy-plan §5.2 (capability, inputs, protocol with ordinal phase headings, outputs (renamed from `output`), rules, errors). Reads `<workflow>/resources/NN-<slug>.{md,toon}` and emits `<workflow>/resources/<slug>/SKILL.md` (freeform resource — no `metadata.ontology`/`metadata.kind`). Rewrites in-body resource references (numeric IDs like "resource 27", slug-form like "resource gitnexus-reference", and path-shaped like "resources/27-gitnexus-reference.md") to file-relative markdown hyperlinks at point-of-use; resources declared but not body-referenced fall back to a trailing `## Resources` safety-net section. Idempotent. Supports `--cleanup` (delete legacy flat files post-translation) and `--write-manifest` (emit `<workflow>/.migration-manifest.json` with the slug↔legacy_id map). |
| 20 | 3dbf648 | workflows (submodule pointer) | 1 line | Submodule pointer update to advance the workflows worktree to commit `a8907bc` on `feat/125-markdown-skills-content`, which carries the migration of all 9 remaining workflows to the markdown folder-shape (see content-side rows 21-29). Tests run green against this pointer (329 passed, 4 skipped); typecheck is clean. |

## Content-side change blocks (continued, feat/125-markdown-skills-content)

| # | Commit | Path scope | Lines | Rationale |
|---|--------|------------|-------|-----------|
| 21 | a8907bc | `cicd-pipeline-security-audit/{techniques,resources}/<slug>/SKILL.md`, `README.md` | 9 techniques + 5 resources + README update | All 9 skill TOON files translated to techniques/<slug>/SKILL.md; all 5 resource markdown files moved to resources/<slug>/SKILL.md with synthesized canonical frontmatter (`name`, `description`, `metadata.order`, `metadata.legacy_id`). README skill+resource tables updated to link to the new folder-shape paths. Legacy `skills/` and `resources/*.md` files deleted. |
| 22 | a8907bc | `prism/{techniques,resources}/<slug>/SKILL.md` | 13 techniques + 61 resources | The largest single-workflow migration. The 13 prism skills (structural-analysis, full-prism, …, adaptive-analysis) become techniques; the 61 prism resources (L12 family, evaluation-related, prism-specific) become freeform resources retaining any prior domain frontmatter under `metadata.*`. The numeric `"00"` ref in structural-analysis was rewritten to `[l12](../../resources/l12/SKILL.md)` automatically. |
| 23 | a8907bc | `prism-audit/techniques/compose-audit-prompt/SKILL.md` | 1 technique | Single-skill workflow; the orchestrating compose-audit-prompt technique landed verbatim. No resources to migrate. |
| 24 | a8907bc | `prism-evaluate/{techniques,resources}/<slug>/SKILL.md`, `README.md` | 3 techniques + 2 resources + README update | The evaluation workflow's 3 skills and 2 resources translated; README's resource table updated to point to the new SKILL.md folder paths. |
| 25 | a8907bc | `prism-update/techniques/<slug>/SKILL.md` | 5 techniques | The prism-update workflow's 5 skills translated. The `verify-prism-consistency` technique body — which textually references the prism workflow's file layout — was updated from `prism/skills/03-plan-analysis.toon` to `prism/techniques/plan-analysis/SKILL.md` so its consistency-check prose matches the post-migration shape. No resources to migrate. |
| 26 | a8907bc | (none — remediate-vuln has no local content) | 0 lines | This workflow defines no local skills or resources; it composes via cross-workflow references into `meta` and `work-package`. Nothing to translate. |
| 27 | a8907bc | `substrate-node-security-audit/{techniques,resources}/<slug>/SKILL.md` | 18 techniques + 9 resources | The substrate audit workflow's 18 skills (execute-audit, score-severity, …, write-gap-analysis) and 9 resources (start-here, severity-rubric, calibration, etc.) translated. Largest migration after prism. |
| 28 | a8907bc | `work-packages/{techniques,resources}/<slug>/SKILL.md` | 6 techniques + 7 resources | The portfolio-level work-packages workflow (note the plural; distinct from the singular work-package work-package workflow) migrated: 6 orchestration skills and 7 planning/roadmap templates. |
| 29 | a8907bc | `workflow-design/{techniques,resources}/<slug>/SKILL.md`, `workflow.toon`, `README.md`, `activities/README.md` | 2 techniques + 5 resources + 4 file updates | The workflow-design workflow's 2 primary skills and 5 resources translated. workflow.toon's two path-shaped resource refs (`resources/03-update-mode-guide.md` and `resources/04-review-mode-guide.md`) rewritten to bare slug form (`update-mode-guide`, `review-mode-guide`) — the only workflow.toon path-ref rewrite needed across all 9 workflows. README skill+resource tables and the activities README in-body reference updated to the new folder-shape paths. |

## Content-side change blocks (feat/125-markdown-skills-content)

| # | Commit | Path scope | Lines | Rationale |
|---|--------|------------|-------|-----------|
| 14 | f9a673c | `meta/techniques/<slug>/SKILL.md` + sibling op-child files (8 techniques: agent-conduct, atlassian-operations, github-cli-protocol, gitnexus-operations, harness-compat, knowledge-base-search, version-control, workflow-engine) | +~1800 lines | Cross-workflow shared technique layer. Each technique is a folder with a `SKILL.md` index (frontmatter + Capability + optional Operations table linking to child files + Rules) and one `<op>.md` file per operation in the canonical Inputs/Output/Procedure/Errors/Rules shape. Sourced verbatim from the 2026-05-22 planning-folder `legacy/meta/techniques/` tree via the migrate-skills script. |
| 15 | f9a673c | `meta/resources/<slug>/SKILL.md` (4 resources: activity-worker-prompt, bootstrap-protocol, workflow-canonical, workflow-orchestrator-prompt) | +~400 lines | Cross-workflow shared resource layer. The ontology definition lands at `meta/resources/workflow-canonical/SKILL.md` (per Task A2 acceptance criteria). |
| 16 | 538de1d | `work-package/techniques/<slug>/SKILL.md` + sibling op-child files (27 techniques) | +~3800 lines | Work-package-local technique layer. Same canonical shape as commit 14. Six techniques use the op-as-child-files pattern (`cargo-operations`, `dco-provenance`, `gitnexus-operations`, `manage-artifacts`, `manage-git`, `validate-build`). |
| 17 | 538de1d | `work-package/resources/<slug>/SKILL.md` (22 resources) | +~12000 lines | Work-package-local resource layer. Same shape as commit 15. Includes the rust-substrate code review and tdd-concepts-rust resources, both large reference documents. |
| 18 | 8d8591f | `README.md`, `meta/README.md` | +~40 lines | Documents the new `techniques/` and `resources/` directory layout and the workflow-local → meta precedence (Task A3). |

---

## Notes on size

The content-side rows (14-17) cover ~170 new files. Per-file enumeration would balloon the index without adding review value — every file is a copy of a planning-folder file already reviewed when the legacy tree was authored on 2026-05-22. The check at this stage is structural (correct paths, file present where the loader expects it), which is exercised by the live test suite running against the submodule pointer.

If the manual diff reviewer wants per-file granularity for a specific content slug, indicate the slug in the block-interview checkpoint and the agent will produce a fine-grained sub-index for it.
