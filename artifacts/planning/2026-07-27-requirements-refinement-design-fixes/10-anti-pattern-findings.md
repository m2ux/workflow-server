# Anti-Pattern Findings — `requirements-refinement`

**Mode:** update · **Date:** 2026-07-27
**Pass:** anti-patterns
**Target:** `requirements-refinement` v1.2.0

Full catalog walk (every `### AP-XX. name` subsection plus the MR authoring entries) against the committed state `workflow/requirements-refinement` @ `37988f9a` — 21 files: `workflow.yaml`, 5 activity YAMLs, 8 technique files, 4 resources, 4 READMEs.

## Findings

| ID | Severity | Finding | Location | Fix |
|----|----------|---------|----------|-----|
| A-1 | Medium | **`no-technique-resource-dual-home`** — both `## Rules` entries restated operative criteria owned by `validation-rubric.md`, which the same technique already cites in Protocol 1–3. `coverage-gaps-are-correctable` was near-verbatim against the rubric's Source Coverage bullet; `non-sequential-identifiers-accepted` was triple-homed, also appearing in `specification-protocol.md#identifier-schemes` | `techniques/validate-specification.md` `## Rules` | **APPLIED** — both rules deleted; the rubric is the single home. The section is now absent, which 350 of 503 corpus technique files also do |
| A-2 | Medium | **`no-rule-protocol-restatement`** — rule `every-normative-statement-is-mapped` restated the technique's own Protocol 4 and duplicated `validation-rubric.md#source-coverage`, adding no invariant the steps do not convey | `techniques/analyze-source.md` `## Rules` | **APPLIED** — rule deleted; `analysis-records-intended-changes-only` retained as a genuine cross-cutting boundary |
| A-3 | Medium | **`no-technique-resource-dual-home`** — the normative-statement vocabulary (`SHALL`/`MUST`/`SHOULD`/`MAY` obligation, constraint, or rule) was written out in Protocol 4 while `validation-rubric.md` and `requirements-analysis-report.md` both already define it — three homes for one vocabulary | `techniques/analyze-source.md` Protocol 4 | **APPLIED** — replaced with a cite to [Source Coverage](../resources/validation-rubric.md#source-coverage) in house style; anchor verified resolvable by `check-resource-anchors` |
| A-4 | Low | **`no-one-step-rules`** — rule `one-advance-per-correction-pass` constrained only Protocol 4 and restated what the `correction_iteration` Output description already defines | `techniques/update-specification.md` `## Rules` | **APPLIED** — rule deleted; the Output description is the sole definition |
| A-5 | Medium | **`no-bind-mechanics-as-prose`** (+ **`numbered-protocol-phases`**) — Protocol 1 bullet 1 instructs the agent to capture `{source_path}` and `{target_doc_path}` "from the user request"; both are declared container Inputs and `required: true` workflow variables, so the bullet teaches how to obtain an already-bound value and closes no real binding gap. The phase additionally carries a second distinct outcome (basename derivation) | `techniques/intake-sources.md` Protocol 1 | Drop the capture bullet, retitle the phase to the basename derivation, and align the Capability sentence — **NOT applied: narrows the technique's declared intake boundary, so it is a design call rather than an audit edit** |
| A-6 | Medium | **`readme-orients-not-transcribes`** — the index table's `Transitions to` column transcribes `transitions[]` and its `Produces` column transcribes the server-synthesized artifact contract; both must be edited when the YAML changes. 4 of the 6 sibling `activities/README.md` tables use a single orientation `Role` column instead | `activities/README.md` table | Collapse to `\| # \| Activity \| Role \|` — **NOT applied: `preserve-readme-content` (AP-80) requires a preservation audit and explicit confirmation before shrinking README content, and `{headless_mode}` is true** |
| A-7 | Medium | **`session-interaction-in-technique`** (+ **`technique-stage-agnostic`**) — Protocol 2 instructs "surface the diagnostic and stop": it requires a human channel the technique does not own, and asserts a route the activity owns. No failure arm exists in the activity graph for an unreadable source | `techniques/intake-sources.md` Protocol 2 | Emit a declared readability output and let `01` gate on it; move the stop to a transition. **Residual, carried from the pre-commit pass** |
| A-8 | Medium | **`checkpoint-requires-decision`** — `failure-acknowledged` has a single option with no effect on a terminal activity: every option leads to the same place, sets no variable, and exists only for acknowledgement | `activities/06-report-failure.yaml` | Convert to `action: message`, or record the acknowledgement in an effect. **Held as open judgement A-5** |
| A-9 | Medium | **`structure-backed-constraints`** — canonical-document integrity and the no-version-control boundary are text-only across three homes with zero `action: validate` in the workflow | `workflow.yaml` `rules.activity[2]` · `techniques/TECHNIQUE.md` · `README.md` | Add `action: validate` on the write target, or reclassify as guidance. **Held at Gate 2** |
| A-10 | Medium | **`single-rule-authority`** ×2 and **`no-rule-protocol-restatement`** ×3 — five `workflow.yaml` `rules.activity` entries duplicate the technique contract or restate technique Protocol; the `[0]` identifier list has already drifted to 4 of 7 schemes | `workflow.yaml` `rules.activity[0]–[4]` | Delete the five entries. **Held — Gate 2 removals with no impact row** |
| A-11 | Low | **`cited-home-owns-claim`** — [F-9](03-follow-ups.md) attributes the guard's "21 baselined violations no longer present" to this branch, reasoning that the parent-repo baseline cannot shrink because "the corpus still carries them until this branch merges". Re-derived this pass, that reasoning does not hold: the same 21 report as absent when the guard scans the pre-change tree, and they belong to `work-package` (14), `meta` (3), `workflow-design` (3) and `codebase-wiki` (1) — **zero** to `requirements-refinement`. They are already gone from the merged corpus, so merging this branch will not change them | `03-follow-ups.md` F-9 rationale | Restate F-9's basis: the 21 are pre-existing baseline staleness owned by four other workflows, unrelated to this change; the `--update-baseline` action it recommends stays correct. **APPLIED to the register** |

**Finding count:** 11 · **5 applied** (A-1–A-4, A-11) · **6 standing** (A-5, A-6 newly surfaced; A-7–A-10 residual or held)

## Notes

- Method: every catalog subsection was applied as written; the Tool-Technique-Doc entries were checked against the actual harness surface rather than skimmed. This workflow authors no tool names, no bootstrap claims, and no tool-call recipes, so `consistent-tool-names`, `no-false-resource-delivery`, `complete-bootstrap-path`, `describe-tool-value`, `no-redundant-tools`, `no-tool-usage-prescription` and `no-delivery-mechanism-narration` have no surface here — a genuine non-applicability, not a clean-by-omission claim.
- Entries checked with a clean result on structural evidence: `no-inline-content`, `schema-is-constraint`, `no-invented-naming`, `checkpoint-not-prose`, `decision-not-prose`, `artifact-not-buried`, `variable-for-approval`, `mode-as-state`, `procedure-in-protocol`, `technique-inputs-declared`, `bound-step-no-description` (no bound step carries `description` or `name`), `no-monolith-masking-steps`, `no-duplicate-technique-steps` (each of the six techniques binds at exactly one step), `no-hand-authored-artifacts` (no activity declares `artifacts[]`), `techniques-list-disjoint` and `hoist-universal-techniques` (`variable-binding` is hoisted to `workflow.techniques.activity`; no activity declares `techniques[]`), `no-activity-prose-rules` (no activity carries `rules:`), `no-set-of-technique-output`, `no-valueless-control-set`, `no-intra-step-input-set` (the workflow declares no `action: set` at all), `rule-audience-bucket`, `avoidance-voice-in-definitions`, `io-agnostic-contract`, `canonical-artifact-ids`, `artifact-name-in-io`, `no-opaque-artifact-path-array`, `no-resource-caller-backlink` (no resource names a caller), `no-redundant-link-label`, `brace-output-references`, `brace-declared-ids`, `anchored-protocol-references` (validator-confirmed), `hoist-shared-inputs`, `escape-literal-dollar`, `snake-case-symbols`, `boolean-id-shape` (`*_confirmed`, `*_exists`, `*_complete`, `has_*` are affirmative predicates), `collection-id-shape`, `io-id-shape`, `rule-slug-shape`, `capability-group-placement`, `statement-not-question`, `link-named-artifacts` (all seven artifact-naming messages carry `[label]({path})` with no hard-coded `NN-`), `no-next-step-narration`, `no-caption-only-message`, `runtime-rules-only`, `omit-null-sections`, `one-decision-one-checkpoint`, `exception-only-verdict-tables`, `link-dont-copy-sections`, `canonical-fact-home`, `resource-fills-not-does`, `technique-outputs-declared`, `duplicate-shared-capability`, `no-derived-state-shadow`, `pass-orchestration-in-technique`, `platform-semantics-in-capability`, `procedure-in-capability`, `procedure-in-io-contract`, `technique-ref-in-io-contract`, `no-engine-mechanics-as-rules`, `no-template-creation-guide`, `variable-description-one-line`, `bag-value-as-literal`, `unproduced-value-read`, `worktree-root-placeholders`.
- `contract-not-procedure` (Protocol 5) and `alternate-ops-as-protocol-sequence` / `rule-as-protocol-step` (Protocol 1–3) fire and are homed in the [expressiveness satellite](08-expressiveness-findings.md) as E-1 and E-4 rather than duplicated here.
- `loop-not-prose` does **not** fire on the correction cycle: the cycle spans two activities and a `kind: loop` step is activity-scoped, so no loop construct substitutes for the transition back-edge. The absent termination guarantee is scored under Encode Constraints as Structure in the [principle satellite](10-principle-findings.md) (P-1).
