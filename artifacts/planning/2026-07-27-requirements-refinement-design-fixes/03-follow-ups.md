# Follow-Ups

> Design session · `requirements-refinement` · updated 2026-07-27

| ID | Surfaced at | Item | Owner / next step | Status |
|----|-------------|------|-------------------|--------|
| F-1 | `requirements-refinement` (assumption A-1) | [format conventions](01-format-conventions.md) line 38 states that a retry limit in a transition condition reads the declared limit variable — a `simple` condition's `value` admits only a literal scalar, so the convention as written is not expressible | Correct the line to state the literal-plus-single-home rule before drafting relies on it | open |
| F-2 | `scope-and-draft` (drafting `techniques/intake-sources.md`) | The G6 row directing that technique to declare the inputs its Protocol reads conflicts with anti-pattern `hoist-shared-inputs` — all three values it reads (`source_path`, `target_doc_path`, `planning_folder_path`) are already declared on the container `TECHNIQUE.md`, and redeclaring them on the leaf is the anti-pattern the catalogue names | Only the A-8 path output was applied; quality-review to confirm the non-redeclaration reading or direct the redeclaration | open |
| F-3 | `scope-and-draft` (drafting `activities/01-intake-and-analyze.yaml`) | G3's fix for the sole transition carrying both a `condition` and `isDefault: true` is [impact](05-impact-analysis.md#3-removals-inventory) row 13, a Gate 2 row; dropping the `condition` would also strand `analysis_confirmed` as a fifth variable with no structural reader, extending A-3 | Arm left at baseline; Gate 2 settles the shape under A-7 — the self-back-edge alternative keeps `analysis_confirmed` read, the `transitionTo` alternative adds it to the A-3 removal batch | open |
| F-4 | `scope-and-draft` (drafting `techniques/validate-specification.md`) | Protocol 5 still states the four verdict predicates that the rewritten Output descriptions now own; the clean fix collapses those four bullets to one reference, which is a content removal no impact row inventories | Inventoried at `validate-and-commit` as [impact](05-impact-analysis.md#3-removals-inventory) row 21, pending Gate 2 approval; the collapse itself stays unapplied until then | done |
| F-5 | `scope-and-draft` (G2 option effects) | The four effect-less gate options ([impact](05-impact-analysis.md#3-removals-inventory) rows 14–15) are unchanged this pass, so G2 and G3 stay open on them | Gate 2 resolves A-5 and A-7; the chosen effects land in a follow-up drafting pass | open |
| F-6 | `scope-and-draft` (file count) | The manifest modifies 16 files against the impact analysis's 15 — `activities/README.md` moved from possibly-touched to modified because the announcements no longer match its Produces column | Reconciled at `validate-and-commit` — the [impact analysis](05-impact-analysis.md#1-impact-classification) absorbed `activities/README.md` as the 16th directly-modified file and its Summary now reads 16 of 21 | done |
| F-7 | `quality-review` ([conformance findings](08-conformance-findings.md)) | Four resource files lack the sibling frontmatter shape carried by 141 of 142 corpus resources; they load correctly by filename-derived id and sit outside the confirmed 16-file scope, so no fix was applied | Deferred past this session's scope — a separate pass adds the frontmatter to the four resources | open |
| F-8 | `quality-review` ([expressiveness findings](08-expressiveness-findings.md)) | AP-124/AP-121: `techniques/update-specification.md` carries a Protocol phase whose only content is mode selection, which belongs in the binding rather than the protocol | Deferred past this session's scope — collapsing the phase changes the technique's protocol shape beyond the approved change goals | open |
| F-9 | `validate-and-commit` (binding-fidelity guard) | The drift guard reports 0 new violations and 21 baselined violations no longer present. **Basis corrected at `post-update-review`:** those 21 are not this branch's doing — they belong to `work-package` (14), `meta` (3), `workflow-design` (3) and `codebase-wiki` (1), zero to `requirements-refinement`, and the guard reports the same 21 as absent when it scans the pre-change tree. They are pre-existing baseline staleness that merging this branch will not affect, so the shrink is not gated on the merge. Separately, the workflow's only baseline entry (`source_coverage_complete` dead-output) is still live and independently confirms A-3 | Run `check-binding-fidelity.ts --update-baseline` in the parent repo whenever convenient, so the 21 stale entries stop masking future regressions; it does not need to wait for this branch | open |
| F-10 | `validate-and-commit` ([planning README](README.md) conformance) | The Progress table carries the seed profile's unprefixed link targets verbatim, so eight links to produced artifacts are broken (`format-conventions.md` vs the actual `01-format-conventions.md`, and likewise for design-specification, assumptions-log, impact-analysis, scope-manifest, drafting-plan, draft-attestation, file-review-note); three further links point at artifacts the review path never produced. The Links table below it uses the correct prefixed paths | Repaired by the orchestrator at the `validate-and-commit` boundary — verified at close: zero unprefixed Progress targets remain | done |
| F-11 | `validate-and-commit` ([planning README](README.md) header) | The header **Status:** read `Audited — 15 findings, 0 Critical; awaiting Gate 2 approval to commit`, which was free prose rather than one of the four lifecycle values the seed profile declares (`Planning`, `Drafting`, `Reviewing`, `Complete`) | Closed at `post-update-review` — the header now states the submitted-for-review lifecycle and the Links table carries [#318](https://github.com/m2ux/workflow-server/pull/318); finding detail sits in the Executive Summary rather than the header field | done |
| F-12 | `post-update-review` ([expressiveness](08-expressiveness-findings.md) E-5 / [anti-patterns](10-anti-pattern-findings.md) A-5) | `techniques/intake-sources.md` Protocol 1 bullet 1 instructs capturing `{source_path}` and `{target_doc_path}` "from the user request", but both are declared container Inputs and `required: true` workflow variables, so the bullet is bind mechanics closing no real gap (`no-bind-mechanics-as-prose`); the phase also carries a second distinct outcome. Not auto-applied because dropping it narrows the technique's declared intake boundary and its Capability sentence | Human design call: either drop the capture bullet, retitle the phase to the basename derivation and align Capability, or keep it and record the intake boundary as intentional. Interacts with F-2 above, which covers the same file | open |
| F-13 | `post-update-review` ([conformance](08-conformance-findings.md) C-3 / [anti-patterns](10-anti-pattern-findings.md) A-6) | `activities/README.md`'s index table carries `Produces` and `Transitions to` columns that transcribe `transitions[]` and the server-synthesized artifact contract (`readme-orients-not-transcribes`); 4 of 6 sibling `activities/README.md` tables use a single `Role` column instead | Blocked on `preserve-readme-content` (AP-80), which requires a preservation audit and explicit user confirmation before shrinking README content — `{headless_mode}` is true, so the audit could not confirm. Present the removal for approval, then collapse to `\| # \| Activity \| Role \|` | open |
