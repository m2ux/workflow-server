# Design Principle Findings — `requirements-refinement`

**Mode:** update · **Date:** 2026-07-27
**Pass:** principles
**Target:** `requirements-refinement` v1.2.0

Post-commit audit of `workflow/requirements-refinement` @ `37988f9a` against each stance in `design-principles`. Only the principles whose stance this workflow's authored content can bear on are scored; the classification is Pass / Partial / Violation against the positive stance, not a re-derivation of prohibited-pattern Detect.

## Findings

| ID | Severity | Finding | Location | Fix |
|----|----------|---------|----------|-----|
| P-1 | High | **9. Encode Constraints as Structure — Violation.** The correction cycle's termination rests on `correction_iteration` advancing, but nothing structural advances it: the cycle is a transition back-edge (04 → 03), `maxIterations` is a `kind: loop` field and no loop is declared, and the advance lives in the `correction_iteration` Output description plus Protocol 4's `emit`. A pass that fails to advance the counter leaves `correction_iteration < 3` true forever | `activities/04-validate-specification.yaml` transitions · `techniques/update-specification.md` Outputs / Protocol 4 | No in-scope fix exists: a `kind: loop` step is activity-scoped and cannot bound a cycle spanning two activities, and merging or renumbering activities is explicitly out of scope. An `action: set` would not help — action verbs are agent-executed, so they carry no stronger guarantee than a declared output. **Carry as a documented structural limit for reviewer attention** |
| P-2 | High | **9. Encode Constraints as Structure — Violation.** Canonical-document integrity ("never edits the canonical document in place", "performs no version-control operations") is asserted in three rule homes with zero `action: validate` anywhere in the workflow; the guarantee is defeated by ignoring the text | `workflow.yaml` `rules.activity[2]` · `techniques/TECHNIQUE.md` `artifacts-confined-to-planning-folder` · `README.md` | Add an `action: validate` on the write-target path at each persisting step, or reclassify as non-critical guidance. **Held at Gate 2** |
| P-3 | Medium | **6. One Authoritative Home — Partial.** Five `workflow.yaml` `rules.activity` entries duplicate or restate content owned elsewhere: `[0]` protocol-preservation dual-homed with `TECHNIQUE.md` (and its identifier list has already drifted to 4 of the 7 schemes in `specification-protocol.md`), `[2]` planning-folder invariant dual-homed with `TECHNIQUE.md`, `[1]` restates `intake-sources` and `analyze-source` Protocol 3, `[3]` restates `update-specification` Protocol 2, `[4]` restates four other homes | `workflow.yaml` `rules.activity[0]–[4]` | Delete the five entries; the technique contract and Protocols are the enforced homes. **Held — Gate 2 removals with no impact row inventorying them** |
| P-4 | Medium | **14. Single Source of Truth — Partial.** Three declared variables are written but read by no gate: `source_coverage_complete` (set by `validate-specification` Protocol 5, consumed only inside that same file when deriving `validation_passed`), `sources_confirmed` (set by the `sources-confirmed` checkpoint effect), and `finalization_confirmed` (set by the `finalization-confirmed` checkpoint effect on a terminal activity). `check-binding-fidelity` independently confirms the first as a live `dead-output` violation | `workflow.yaml` `variables[]` · `techniques/validate-specification.md` `## Outputs` | Remove each, or give it a structural read. **Held as open judgement A-3** |
| P-5 | Medium | **12. Output Economy — Partial.** The `failure-acknowledged` checkpoint carries one option (`acknowledged`) with no effect, sets no variable, and sits on a terminal activity, so every path through it is identical — the gate records nothing | `activities/06-report-failure.yaml` | Convert to an `action: message` step, or give the option an effect that records the acknowledgement. **Held as open judgement A-5** |
| P-6 | Medium | **23. Close the Loop — Partial.** The `revise` options on `sources-confirmed`, `analysis-confirmed` (both in `01`) and `finalization-confirmed` (in `05`) carry no `effect` and no `transitionTo`. Choosing "Correct the sources" or "Revise the analysis" changes no state and alters no route, so the rework the option offers cannot occur | `activities/01-intake-and-analyze.yaml` · `activities/05-finalize-specification.yaml` | Give each `revise` option a `transitionTo` (self back-edge or a hold activity) or an effect a gate reads. **Held as open judgement A-7** |
| P-7 | Medium | **13. Separate Contract from Procedure — Partial.** `validate-specification.md` Protocol 5 restates the identity criteria of all four declared Outputs; `update-specification.md` numbers two mutually exclusive apply modes as sequential phases | `techniques/validate-specification.md` Protocol 5 · `techniques/update-specification.md` Protocol 1–3 | Detailed in the [expressiveness satellite](08-expressiveness-findings.md) (E-1, E-4). **Residual** |

**Finding count:** 7

## Notes

Principles scored **Pass**, with the evidence that grounds each verdict:

- **5. Maximize Schema Expressiveness** — every user decision is a `kind: checkpoint` step, every route a `transitions[]` entry with a structured `condition`, every artifact a `#### artifact` on its producing technique; no activity hand-authors `artifacts[]`; all 20 `variables[].description` entries are one line (`variable-description-one-line`).
- **6. One Authoritative Home** — three duplications were closed in this pass: both `validate-specification.md` rules (which restated `validation-rubric.md` criteria the same technique already cites — and `non-sequential-identifiers-accepted` was in fact triple-homed with `specification-protocol.md`), `analyze-source.md`'s `every-normative-statement-is-mapped` (which restated its own Protocol 4 plus the rubric), and the normative-statement vocabulary in `analyze-source.md` Protocol 4 (now a cite to `validation-rubric.md#source-coverage`). The residual `rules.activity` duplication is P-3.
- **11. Complete Documentation Structure** — README present at root and in `activities/`, `techniques/`, `resources/`; the root Flow diagram's back-edge glyphs were reconnected in this pass (they previously terminated 11 columns short of the target arrow).
- **17. Document in Positive Present** — definition prose states current behaviour. The negative clauses that remain ("performs no version-control operations", "never edits the canonical document in place", "No unvalidated specification reaches the canonical location") are scope and safety boundaries, not comparisons against a prior design, which `avoidance-voice-in-definitions` places outside its test.
- **20. Keep Orchestration in Structure** / **24. Keep Session Interaction in Activities** — techniques emit values and artifacts; activities own every `message`, checkpoint, and route. One residual exception: `intake-sources.md` Protocol 2's "surface the diagnostic and stop" both interacts with the session and asserts a route, carried in the enforcement satellite.
- **22. Modular Over Inline** — every activity, technique, and resource is its own file; parents reference siblings.
- **25. Bind Sibling Operations as Steps** / **26. Atomic Techniques; Compose at Activities** — no technique Protocol applies another technique for work; each of the six leaves is a single produce path over tools and resources.
- **28. Creation Guide for Generated Documents** — the two templated artifacts map to guides (`requirements-analysis-report.md`, `change-summary.md` each carry a `## Template`).
- **29. Cite Resource Policy; Do Not Restate It** — strengthened this pass; the rubric and protocol resources are now cited rather than paraphrased at the three sites listed above.
- **3. Define Complete Scope Before Execution** / **7. Convention Over Invention** / **10. Non-Destructive Updates** — evidenced by the scope audit (16/16 addressed, no unplanned change) and the conformance pass; no invented naming survives.
