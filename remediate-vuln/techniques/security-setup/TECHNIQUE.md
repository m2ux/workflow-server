---
metadata:
  version: 1.0.0
---

## Capability

Shared contract for initialising a high-sensitivity security fix — the advisory inputs, the private remote and the security feature branch off the private fork, the isolated planning folder, and the isolation invariant that the public remote stays untracked.
