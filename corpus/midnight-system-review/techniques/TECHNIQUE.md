---
metadata:
  version: 1.0.0
---

## Capability

Shared Inputs, Outputs, Rules, and Errors for every technique in this set.

## Inputs

### planning_folder_path

The review's working folder — where every operation reads prior artifacts from and writes its own artifacts into.

### target_repo_path

Path to the midnight-node checkout under review — the codebase every probe, trace, and diff runs against.
