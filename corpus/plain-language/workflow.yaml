$schema: ../../schemas/workflow.schema.json
id: plain-language
version: 1.4.0
title: Plain Language Workflow
description: >-
  Author, rewrite, or audit a document so its readers can find what they need, understand what they find, and use it. Applies the four principles and guidelines of ISO 24495-1, with an optional ASD-STE100 controlled-language overlay for technical documentation.
author: m2ux
tags:
  - plain-language
  - iso-24495-1
  - asd-ste100
  - technical-writing
  - document-authoring
  - document-audit
rules:
  activity:
    - "reader-governs — the document is written for its readers, not its author: every drafting and evaluation decision cites the reader profile and the governing guideline rather than the writer's preference."
techniques:
  activity:
    - variable-binding
variables:
  - name: user_request
    type: string
    description: The user's free-form request that opened this run.
  - name: headless_mode
    type: boolean
    description: Whether the run resolves a soft mid-flow gate to its default without reaching a person.
    defaultValue: true
  - name: source_document_path
    type: string
    description: Path to the existing document a rewrite or audit run reads, empty on an author run.
    defaultValue: ""
  - name: output_path
    type: string
    description: Absolute path the delivered plain-language document is written to.
    defaultValue: ""
initialActivity: intake-and-profile
graph:
  intake-and-profile:
    has-source: source-analysis
    no-source: draft
  source-analysis:
    audit: __terminal__
    rewrite: draft
  draft:
    done: evaluate
  evaluate:
    done: deliver
    revise-again: draft
  deliver:
    done: __terminal__
