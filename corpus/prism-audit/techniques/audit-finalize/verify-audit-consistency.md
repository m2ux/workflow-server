---
metadata:
  version: 1.0.0
---

## Capability

Verify the three audit deliverables exist and are internally consistent — confirming that every cross-document hyperlink resolves to an actual heading.

## Protocol

### 1. Verify Audit Consistency

- Confirm the summary report (`{audit_report_path}`), detailed-findings document (`{detailed_findings_path}`), and trade-off analysis (`{trade_offs_path}`) all exist.
- Check that every cross-document hyperlink resolves to an actual heading.
