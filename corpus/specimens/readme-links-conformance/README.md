# Planning README Links Conformance

Binds the shared planning-README operation twice. The first binding omits the links table, which is the private advisory. The second fills it, which is every other caller.

Walk it as `workflow_id: readme-links-conformance` after the minimum viable workflow has dispatched on the same instance.
