# Contract Composition

Change-surface specimen for inherited contracts delivered once.

Two operations sit under one group. A worker delivery carries each group's
rules as a `contracts` block, and each operation names that block in
`inherits`.

Walk it as `workflow_id: contract-composition` after the MVW has held on the
same instance.
