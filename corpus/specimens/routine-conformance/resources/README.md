# Routine Conformance Resources

> Part of the [Routine Conformance Workflow](../README.md)

One guide, for the one document a run leaves behind.

| Resource ID | Title | Purpose |
|-------------|-------|---------|
| `conformance-report` | Conformance Report | Creation guide: `routine-conformance-report.md` — template and the rules governing what each section may claim, across which measurement each site supplied, what the two passes measured, and how each pass ended |

## Planning artifact to guide map

Which guide owns each persisted filename's shape.

| Bare filename | Guide |
|---------------|-------|
| `routine-conformance-report.md` | [conformance-report](conformance-report.md) |

---

## Why there is only one

The run's product is evidence about the construct: which measurement each site supplied, and what one run did with each. What the measurements found is a column of that evidence rather than a document of its own — persisting it separately would make the measuring look like the point.
