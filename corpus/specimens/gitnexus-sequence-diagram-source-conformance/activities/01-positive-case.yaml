id: positive-case
version: 1.1.0
name: Positive Case
description: Refer to the sequence diagram source over a comparison against a fixed commit.
variables:
  reads:
    - base_ref
    - positive_diff_scope
  writes:
    - name: positive_diagram_source
      type: array
      description: The ordered step trace of each execution flow the change runs through, which the sequence diagram is drawn from.
required: true
steps:
  - kind: routine
    id: trace-fixed-comparison
    routine: gitnexus::sequence-diagram-source
    with:
      diff_scope: "{positive_diff_scope}"
    outputs:
      diagram_source: positive_diagram_source
exits:
  - id: traced
    isDefault: true
outcome:
  - The run reached the flows the comparison against `{base_ref}` runs through and read the ordered step trace of each, landing the structure a sequence diagram is drawn from
