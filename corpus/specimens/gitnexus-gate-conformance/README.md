# GitNexus Gate Conformance

A specimen of one form: a checkpoint that reaches a user from inside a run declared somewhere else.

A technique is session-blind and carries no checkpoint, so a run that stops for a decision is the one shape only a routine holds. Both runs here take a measurement first and put its result in front of a person before anything irreversible happens — the edit gate reports what a symbol's change would break and stops where the rating is high or critical, or unknown because the walk resolved no caller, and the rename gate shows the edit list before the rename writes it.

Two properties the walk evidences, neither visible from an activity file alone:

- **A spliced gate is an ordinary gate.** The checkpoint is declared in `routines/`, materialised into the activity with the reference site's prefix on its id, and presented, answered and recorded like one written in the activity file.
- **A gate the rating does not reach is not a skipped gate.** The edit gate carries a `when` on the measured rating, so a low-rated symbol passes without asking. The closing activity states which of the two happened, because a seeded default and an accepted gate leave the same value behind.

The rename gate is the one to answer both ways. Refusing it should leave the tree untouched and no diff behind; accepting it should produce both. Point this at a scratch checkout, not at work you care about.

| Activity | Refers to |
|---|---|
| `gate-an-edit` | `pre-edit-impact-gate` |
| `gate-a-rename` | `guarded-rename` |
| `report-conformance` | nothing — states what the two gates decided |
