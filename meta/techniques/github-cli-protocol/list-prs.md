---
metadata:
  version: 1.0.0
---

## Capability

List PRs (read-only — safe via `gh` CLI).

## Inputs

### filters

Optional flags (e.g., `--state open --author @me`)

## Protocol

1. `gh pr list {filters}`.
