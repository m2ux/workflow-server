---
metadata:
  version: 1.0.0
---

## Capability

List issues (read-only — safe via `gh` CLI).

## Inputs

### filters

Optional flags (e.g., `--state open --label bug`)

## Protocol

1. `gh issue list {filters}`.
