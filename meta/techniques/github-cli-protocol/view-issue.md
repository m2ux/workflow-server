---
metadata:
  version: 1.0.0
---

## Capability

View an existing issue (read-only — safe via `gh` CLI).

## Inputs

### identifier

Issue number or URL

## Protocol

1. `gh issue view {identifier}`.
