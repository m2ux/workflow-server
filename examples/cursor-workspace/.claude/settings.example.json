{
  "_comment": "Do not use this file as live Claude settings. deploy-cursor-workspace.sh writes workspace-local .claude/settings.json from settings.template.json and installs scripts/claude/ hooks under the kickoff workspace. This stub remains only so older docs that mention settings.example.json still resolve.",
  "permissions": {
    "allow": [
      "mcp__workflow-server__*"
    ]
  },
  "enabledMcpjsonServers": [
    "workflow-server",
    "concept-rag",
    "atlassian",
    "gitnexus"
  ]
}
