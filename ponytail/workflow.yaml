$schema: ../../schemas/workflow.schema.json
id: ponytail
version: 2.0.0
title: Ponytail Lean-Coding Workflow
description: Drive a coding task, change, or codebase toward the leanest solution that still clears a non-negotiable safety floor, tracking every deliberate simplification as ponytail debt.
author: m2ux
tags:
  - refactoring
  - over-engineering
  - code-review
  - simplification
  - yagni
rules:
  universal:
    - "safety-floor-never-simplified: No rung is climbed at the cost of an obligation the ladder places on its safety floor."
    - "understand-before-climb: The real end-to-end flow a change touches is read and traced before a rung is selected."
techniques:
  activity:
    - variable-binding
variables:
  - name: task_description
    type: string
    description: The coding task, change, or audit target to be made lean
    required: true
  - name: target_path
    type: string
    description: Path to the code or repo under the lazy lens
    defaultValue: "."
  - name: artifact_dir
    type: string
    description: Directory the pass writes its artifacts into
    defaultValue: .ponytail
  - name: lazy_intensity
    type: string
    description: "Strictness of the lazy lens: lite, full, or ultra"
    defaultValue: full
  - name: pass_scope
    type: string
    description: "Breadth of the pass: change (diff-scoped) or repo (whole-tree)"
    defaultValue: change
  - name: safety_floor_cleared
    type: boolean
    description: Whether the minimal solution clears the safety floor
    defaultValue: false
  - name: has_debt_markers
    type: boolean
    description: Whether the harvest found any ponytail marker
    defaultValue: false
initialActivity: intake-and-scope
