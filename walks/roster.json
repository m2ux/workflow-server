{
  "walked": [
    "workflow-design",
    "work-package",
    "prism-evaluate",
    "prism",
    "workflow-authoring",
    "midnight-system-review",
    "work-packages",
    "ponytail",
    "plain-language",
    "requirements-refinement",
    "prism-update",
    "codebase-wiki",
    "prism-audit",
    "meta",
    "fan-conformance"
  ],
  "notWalked": [
    {
      "id": "remediate-vuln",
      "reason": "Six minutes on its own; 21 of 60 walks die on a branch whose checkpoint or transition does not resolve. 92 of its 99 options belong to work-package activities the work-package walk covers; the 7 that are its own go on the uncovered list."
    },
    {
      "id": "routine-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. The all-workflows drift walk still enters it, and the routine walk drives its two reference sites directly."
    },
    {
      "id": "cicd-pipeline-security-audit",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. The all-workflows drift walk still enters it."
    },
    {
      "id": "substrate-node-security-audit",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. The all-workflows drift walk still enters it."
    },
    {
      "id": "mvw",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. The all-workflows drift walk still enters it. What it evidences is that a live instance can dispatch one activity through one routine and one technique, which serving guards and a sidecar MVW walk measure."
    },
    {
      "id": "namespace-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. What it evidences is the delivery — each activity binds the same library operation under a different spelling of its namespace — so the reference guards and the all-workflows drift walk are what measure it."
    },
    {
      "id": "gitnexus-diff-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. What it evidences is that a library's runs over a diff resolve, splice and land their outputs in the referring workflow, which the reference guards and the all-workflows drift walk measure."
    },
    {
      "id": "gitnexus-graph-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. What it evidences is that a run referring to another run composes — the inner body splices inside the outer, carrying both reference ids — which the reference guards and the all-workflows drift walk measure."
    },
    {
      "id": "gitnexus-gate-conformance",
      "reason": "Declares no checkpoint of its own; its two gates arrive from the gitnexus runs it refers to, spliced in at load. Those four options are reachable and no other workflow refers to those runs, so they are measured nowhere — the one entry here that records outstanding coverage rather than its absence."
    },
    {
      "id": "gitnexus-taint-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. What it evidences is that a library run iterating an optional graph layer lands its partition and the flag saying whether the layer was there, which the reference guards and the all-workflows drift walk measure."
    },
    {
      "id": "gitnexus-layer-conformance",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. What it evidences is that a library operation bound directly as an activity step lands its declared answer whether or not the graph holds the layer it reads, which the reference guards and the all-workflows drift walk measure."
    },
    {
      "id": "contract-composition",
      "reason": "Declares no checkpoint, so a coverage walk covers nothing. The all-workflows drift walk still enters it. What it evidences is that a worker delivery names a group contract once and each operation inherits it, which serving guards and a sidecar change-surface walk measure."
    }
  ]
}
