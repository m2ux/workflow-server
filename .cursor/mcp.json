{
  "mcpServers": {
    "workflow-server": {
      "command": "node",
      "args": [
        "${workspaceFolder}/dist/index.js",
        "--transport=stdio"
      ],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
